# Codex Current State

Last updated: 2026-07-07

## Canonical Root

`/home/mrwatson/projects/predmarket-alpha`

This is the active prediction-market research root and macro command center.

## Current Landing

- 2026-07-07 sports consensus 04:45Z probe clearance: ran the due blocker-clearance cycle after the `2026-07-07T04:45:00Z` consensus settlement/probe clock, then refreshed event velocity ETA, Claude/Fable advice audit, and the non-due blocker clock. The run completed through existing gates with no manual promotion. Consensus falsification advanced to `610` joined labels, `56` independent labels, and `17` OOS labels, but still has `0` tested hypotheses and `0` FDR survivors. The nearest consensus bucket is `sports_consensus_price_bucket_bias_bucket_0.30_0.50` with `1` OOS label deficit and next probe `2026-07-07T05:10:00Z`. Fable audit remains implementation-complete (`10/10`) and evidence `8/10`, open only on `CLAUDE-005` ATP forward-OOS and `CLAUDE-008` sports consensus event velocity. ATP remains `atp_proxy_evidence_gate_blocked_forward_oos` despite `632` settled labels because the donor forward-OOS gate is still short. Paper still has `18` usable rows from prior eligible paper signals, live remains `0` eligible. No thresholds changed, no labels were inferred, no EV/paper/live promotion occurred, and no account/order path was touched. Tracked macro-output churn from the refresh was intentionally not committed. Tranche note: `docs/codex/tranches/2026-07-07-sports-consensus-0445-probe-clearance.md`.

- 2026-07-07 Fable World Cup/ATP replay path-config split: removed machine-specific `/home/mrwatson/manual_drops` and `/home/mrwatson/projects` defaults from the World Cup observation/falsification/independence diagnostics, ATP evidence gate donor paths, sports proxy replay label path, and sports blocker-clearance ATP repo pointer. Defaults now route through `manual_drop_path()` / `project_path()` while preserving CLI flags, Make targets, and evidence semantics. Added regression coverage scanning these surfaces. No thresholds changed, no labels were inferred, no EV/paper/live promotion occurred, and no account/order path was touched. Verification: `tests/test_kalshi_path_defaults.py` passed; touched-file Ruff clean; runtime hardcoded-root scan clean; affected Make dry-runs passed. Tranche note: `docs/codex/tranches/2026-07-07-fable-world-cup-atp-replay-path-config.md`.

- 2026-07-07 Fable core capture path-config split: removed machine-specific `/home/mrwatson/manual_drops` and `/home/mrwatson/projects` defaults from core capture/reference surfaces: universe scan raw snapshots, manual Kalshi drops, sports no-vig consensus reference build, Type 2 reference/disposition inputs, paper settlement reconciliation, near-resolution flow terms capture, and ghost-listing depth diagnostics. Defaults now route through `manual_drop_path()` while preserving all CLI overrides and Make contracts. Added regression coverage scanning the touched runtime files. No thresholds changed, no labels were inferred, no EV/paper/live promotion occurred, and no account/order path was touched. Verification: `tests/test_kalshi_path_defaults.py` passed; touched-file Ruff clean; runtime hardcoded-root scan clean; affected Make dry-runs passed. Tranche note: `docs/codex/tranches/2026-07-07-fable-core-capture-path-config.md`.

- 2026-07-07 Fable sports-evidence path-config split: removed machine-specific `/home/mrwatson/manual_drops` and `/home/mrwatson/projects` defaults from the ATP proxy observation loop, sports proxy observation loop, sports microstructure observation loop, sports proxy feature-model falsification, and sports proxy CCD replay. These evidence loops now derive donor project paths and manual-drop observation/label/orderbook paths through `project_path()` / `manual_drop_path()` while preserving all CLI flags, Make targets, packet schemas, and gate behavior. Added a regression test scanning the sports evidence script set for those local roots. No thresholds changed, no labels were inferred, no EV/paper/live promotion occurred, and no account/order path was touched. Verification: `tests/test_kalshi_path_defaults.py` passed; touched-file Ruff clean; sports evidence Make dry-runs passed; hardcoded-root scan clean. Tranche note: `docs/codex/tranches/2026-07-07-fable-sports-evidence-path-config.md`.

- 2026-07-07 Fable sports-consensus path-config split: removed machine-specific `/home/mrwatson/manual_drops` and `/home/mrwatson/projects` defaults from the sports sharp-consensus script family. Consensus preflight, observation, falsification, ATP/NFL/NBA/soccer adapters, provider audit, and soccer Asian diagnostic now derive current-reference, label, observation, raw Odds API, and donor ATP paths from `manual_drop_path()` / `project_path()` while preserving every CLI flag and Make contract. Added a regression test scanning all `scripts/kalshi_sports_consensus_*.py` files for those local roots. No thresholds changed, no labels were inferred, no EV/paper/live promotion occurred, and no account/order path was touched. Verification: path-default tests and representative consensus Make targets passed; touched-file Ruff clean. Tranche note: `docs/codex/tranches/2026-07-07-fable-sports-consensus-path-config.md`.

- 2026-07-07 Fable donor-bridge root-config split: removed machine-specific donor paths from `predmarket.source_inventory` and the external artifact wrapper default. `DEFAULT_SOURCE_REPOS` now derives sibling repo paths from `project_path()` and manual-drop artifacts from `manual_drop_path()`, while `predmarket.external_artifact_wrappers.DEFAULT_WRAP_ROOT` now uses `manual_drop_path("predmarket_external_artifacts")`. Descriptor outputs remain strings, so downstream source inventory, preflight, prior-only donor, and wrapper schemas are unchanged. Added a regression test proving the donor bridge no longer hardcodes `/home/mrwatson/manual_drops` or `/home/mrwatson/projects`. No thresholds changed, no labels were inferred, no EV/paper/live promotion occurred, and no account/order path was touched. Verification: path-default and paper-autonomous source-inventory tests passed; touched-file Ruff clean. Tranche note: `docs/codex/tranches/2026-07-07-fable-donor-bridge-root-config.md`.

- 2026-07-07 Fable root-config split: removed machine-specific `/home/mrwatson/manual_drops` and `/home/mrwatson/projects` defaults from the Makefile and from `predmarket.shared_helpers`. New root variables `PREDMARKET_MANUAL_DROPS_ROOT ?= $(HOME)/manual_drops` and `PREDMARKET_PROJECTS_ROOT ?= $(HOME)/projects` now drive the Make evidence-acquisition, donor, tick, line-move, resolved-archive, consensus, paper-settlement, and sports proxy defaults; helper defaults now use `Path.home() / "manual_drops"` and `Path.home() / "projects"`. This preserves current behavior on the workstation while letting another clone relocate data roots without editing 75 Make variables. Added regression tests proving the Makefile no longer hardcodes those local roots and helper defaults are home-relative. No thresholds changed, no labels were inferred, no EV/paper/live promotion occurred, and no account/order path was touched. Verification: path-default tests passed; Make dry-runs for `kalshi-sports-consensus-preflight`, `kalshi-tick-recorder`, `kalshi-resolved-archive-backfill`, and `kalshi-sports-blocker-clearance-cycle` expand successfully. Tranche note: `docs/codex/tranches/2026-07-07-fable-makefile-root-config.md`.

- 2026-07-07 Fable audit demotion-semantics split: fixed the Claude/Fable advice audit so the stricter near-resolution informed-flow price-implied-null rejection is counted honestly. A running falsification surface with `testable_candidate_count=3` and `research_candidate_count=0` is now treated as implementation-satisfied statistical rejection, not as a missing implementation; the downstream replay chain is likewise satisfied when it is blocked only because the upstream flow gate produced no research candidate. Regenerated prior-only donor artifacts and the Fable audit. Latest audit is `claude_advice_audit_ready_with_open_clock_or_statistical_items`, `10/10` implementation items satisfied, `8/10` evidence items satisfied, `0` missing artifacts, `0` warnings, and open evidence clocks only for `CLAUDE-005` ATP forward-OOS and `CLAUDE-008` sports consensus event velocity. Latest consensus falsification has `574` joined labels, `54` independent labels, `17` OOS labels, `0` tested hypotheses, `0` FDR survivors, and nearest bucket `sports_consensus_price_bucket_bias_bucket_0.30_0.50` needs `2` more OOS labels at `2026-07-07T04:45:00Z`; ATP remains `8/10` forward-OOS with next probe `2026-07-07T06:00:00Z`. No thresholds were lowered, no labels inferred, no EV/paper/live promotion occurred, and no account/order path was touched. Verification: Claude-audit/blocker focused tests `8 passed`; touched-file Ruff check/format clean. Tranche note: `docs/codex/tranches/2026-07-07-fable-audit-demotion-semantics.md`.

- 2026-07-07 historical consensus backfill split: closed the Fable gap where historical sharp-consensus divergence had only a skew/access feasibility check and no replay surface. New `make kalshi-sports-historical-consensus-backfill` consumes a normalized replayable historical no-vig consensus archive, exact Kalshi settled-market labels, and Kalshi candlesticks, then enforces exact `contract_ticker`, side, provider snapshot skew, Kalshi quote skew, and public Kalshi settlement joins before routing rows through the existing `sports_consensus_falsification` 30-cell OOS/FDR grid. Latest real run after `make kalshi-sports-historical-consensus-feasibility`: feasibility is `kalshi_sports_historical_consensus_feasibility_ready_paid_access_unverified`; backfill is `kalshi_sports_historical_consensus_backfill_blocked_missing_historical_archive` because `/home/mrwatson/manual_drops/kalshi_sports_historical_consensus/kalshi_sports_historical_consensus_latest.json` does not exist yet. The runner still records hashed Kalshi archive inputs: settled markets `14af356170bc2b1fc6b09b667a8bff79aa72717203183dc5c6f224b9344f6937`, candlesticks `bbf1a6d095bebd0540b704fd3a33c42f4c077c854df49dac5b5e105f8e79d0a8`. Current counts: `0` historical consensus rows, `0` valid observations, `0` tested hypotheses, `0` FDR survivors. Verification: historical/feasibility/resolved-archive focused tests `14 passed`; touched Python Ruff clean. No thresholds were lowered, no sportsbook settlement labels were used, no EV/paper/live promotion occurred, and no account/order path was touched. Tranche note: `docs/codex/tranches/2026-07-07-historical-consensus-backfill.md`.

- 2026-07-07 Fable tick recorder rotation split: hardened the read-only Kalshi sports tick recorder before credentials are available. `scripts/kalshi_tick_recorder.py` now writes monotonic receive timestamps (`received_monotonic_ns`) into each append-only JSONL row, rotates JSONL files by configurable byte cap (`KALSHI_TICK_RECORDER_ROTATE_BYTES`, default `250000000`), and reports per-file `path`, `sha256`, `byte_count`, `jsonl_file_count`, and `rotation_count` in the safety artifact. Existing WebSocket reconnect/resubscribe behavior remains covered by `tests/test_kalshi_websocket.py`; new tick-recorder tests prove rotated append-only JSONL files and per-file hashes. Real run remains honestly blocked on external read-only Kalshi auth: `KALSHI_TICK_RECORDER_DURATION_SECONDS=1 KALSHI_TICK_RECORDER_ROTATE_BYTES=1024 make kalshi-tick-recorder` exits `0` with status `kalshi_tick_recorder_blocked_missing_or_invalid_auth`, `250` sports tickers selected, `0` recorded lines, `0` rotations, and error `Kalshi private key is required: set venues.kalshi.api_secret or KALSHI_API_SECRET`. No probabilities, labels, EV, paper stake, live/account/order paths, or threshold changes were introduced. Verification: tick/websocket focused tests `25 passed`; touched-file Ruff clean; py-compile clean. Tranche note: `docs/codex/tranches/2026-07-07-fable-tick-recorder-rotation.md`.

- 2026-07-07 Fable capture-target collector operationalization split: moved stale-quote raw evidence capture from separate optional commands into the default always-on collector target set. `scripts/kalshi_always_on_collector.py` now defines first-class `line_moves` and `ticks` targets, exposes capture and gap/error counts in JSON/MD/CSV reports, and tightens cadence to a 60-second `high_frequency_capture_interval` when capture targets are active. `KALSHI_ALWAYS_ON_COLLECTOR_TARGETS` now defaults to `line_moves,ticks,sports_consensus,sports,crypto`, so Fable T2 evidence capture is scheduled before downstream analysis exists. Real capture-only verification used `KALSHI_ALWAYS_ON_COLLECTOR_TARGETS=line_moves,ticks KALSHI_TICK_RECORDER_DURATION_SECONDS=1 make kalshi-always-on-collector-once`: collector status `kalshi_always_on_collector_ready`, `2/2` safe artifacts, cadence `60s`, total capture count `177`, total gap/error count `0`. Line-move logger was `ready_with_deltas` with `4` provider snapshots, `102` events, `242` deltas, `177` line moves, `0` provider errors. Tick recorder remained honestly blocked on missing read-only Kalshi auth (`KALSHI_API_SECRET`/`venues.kalshi.api_secret`), selected `250` sports tickers, and recorded `0` lines. No probabilities, labels, EV, paper stake, live/account/order paths, or threshold changes were introduced. Verification: focused collector/tick/line-move tests `18 passed`; py-compile clean. Tranche note: `docs/codex/tranches/2026-07-07-fable-capture-target-collector-operationalization.md`.

- 2026-07-07 Fable root audit relocation split: moved historical audits, remediation notes, advice files, and completion reports out of the repository root into `docs/audits/`, recovered the active `Fable_5_Repo_Advice_7.6.2026.md` memo from the split stash into `docs/audits/`, then updated moved-report references in Codex notes. Root text/markdown files are now limited to operational entry points and active project docs: `AGENTS.md`, `README.md`, requirements files, and `kalshi_ev_discovery_plan.md`. This is documentation hygiene only: no source/runtime/statistical behavior changed, no thresholds changed, no labels inferred, no EV/paper/live promotion, and no account/order path touched.

- 2026-07-07 Fable path/config cleanup split: removed machine-specific path defaults from the Fable-critical evidence acquisition surfaces without changing their CLI or Make contracts. `predmarket.shared_helpers` now exposes `configured_path`, `manual_drop_path`, and `project_path`; labeled OOS observation/label paths, passive-liquidity paper-fill state, tick recorder JSONL, line-move delta state, and resolved-archive raw snapshots now derive from `PREDMARKET_MANUAL_DROPS_ROOT` with script-specific env overrides still taking precedence. `scripts/kalshi_always_on_collector.py` already had no `/home/mrwatson` hardcoded path and now has a regression test proving that remains true. This is path policy only: no thresholds changed, no labels inferred, no EV/paper/live promotion, and no account/order path touched. Verification: path/collector focused tests `40 passed`. Tranche note: `docs/codex/tranches/2026-07-07-fable-path-config-cleanup.md`.

- 2026-07-07 price-implied null hardening split: implemented Fable's correction that near-resolution informed-flow must beat Kalshi's own price-implied baseline, not a 50/50 coin flip. `scripts/kalshi_near_resolution_informed_flow_evidence_gate.py` now computes `price_implied_correct_probability` from executable selected-side ask prices, falls back only to timestamped midpoint, uses exact Poisson-binomial survival for non-identical price-implied null probabilities, records the legacy coin-flip p-value only for audit, and blocks rows with missing price-implied null evidence. `make kalshi-near-resolution-informed-flow-evidence-gate` now exposes `KALSHI_NEAR_RESOLUTION_FLOW_MICROSTRUCTURE_PATH` for replayable source selection. Real replay against the canonical latest microstructure artifact produced `near_resolution_informed_flow_falsification_ready_no_research_candidate`: `6021` flow rows, `0` research candidates, and the prior settlement-directional lane is demoted because its OOS accuracy `0.85` is below its mean price-implied null `0.8932258065`; one evaluation is blocked on missing price-implied null. No thresholds were lowered, no paper/live promotion occurred, and no account/order path was touched. Verification: focused price-null tests `3 passed`; direct canonical replay exits `0`. Tranche note: `docs/codex/tranches/2026-07-07-price-implied-null-hardening.md`.

- 2026-07-07 Kalshi tick recorder split: added the Fable-requested read-only Kalshi WebSocket tick/orderbook capture surface on `codex/fable-tick-recorder`. `predmarket/kalshi_websocket.py` now supports raw receive-timestamped messages and multi-channel subscriptions, while new `make kalshi-tick-recorder` records append-only sports `ticker,orderbook_delta` JSONL under `/home/mrwatson/manual_drops/kalshi_ticks/` and writes only safety/provenance macro artifacts in-repo. Latest real run is honest and blocked on missing read-only Kalshi auth: `kalshi_tick_recorder_blocked_missing_or_invalid_auth`, `250` sports tickers selected, channels `ticker,orderbook_delta`, `0` recorded lines, `jsonl_sha256=null`, and error `Kalshi private key is required: set venues.kalshi.api_secret or KALSHI_API_SECRET`. Safety flags remain research-only: no database writes, no provider/paid calls, no account/order paths, no market execution, no staking/sizing guidance. Verification: focused tick/websocket tests `24 passed`; touched-file Ruff clean; real `make kalshi-tick-recorder KALSHI_TICK_RECORDER_DURATION_SECONDS=1` exits `0`. Next action is external credential/config repair for read-only Kalshi market-data auth, not code loosening. Tranche note: `docs/codex/tranches/2026-07-07-kalshi-tick-recorder.md`.

- 2026-07-06 historical consensus feasibility split: added `make kalshi-sports-historical-consensus-feasibility` on `codex/fable-historical-consensus-feasibility` to gate any future historical sharp-consensus divergence backfill before it can enter evidence. The target models The Odds API historical cadence as `300s` snapshots and a nearest-snapshot request strategy, producing max expected absolute skew `150s`, which passes the configured `<=180s` timestamp gate. Latest status is `kalshi_sports_historical_consensus_feasibility_ready_paid_access_unverified`; historical endpoint cost is recorded as `10` credits per region/market, `paid_access_verified=false`, and no paid endpoint probe is run by default. Next action remains external: verify paid historical provider access before any historical divergence backfill. No historical backfill, labels, probabilities, paper stake, or live paths were produced.

- 2026-07-06 resolved archive backfill split: added the Kalshi-only settled sports archive collector/replayer as a separate Fable tranche on `codex/fable-resolved-archive-backfill`. New `make kalshi-resolved-archive-backfill` reads hashed raw Kalshi settled-market and candlestick snapshots from `/home/mrwatson/manual_drops/kalshi_resolved_archive_backfill/`, builds entry-price-at-horizon observations, and feeds only the pre-registered price-bucket bias family into OOS/FDR falsification. Latest replay used no provider/API calls and reports `kalshi_resolved_archive_backfill_ready_no_fdr_survivors`: `1200` raw settled markets, `1184` exact independent Kalshi labels, `3552` horizon observations, `2` tested bucket-bias hypotheses, max hypothesis OOS `188`, and `0` FDR survivors. Raw market sha256 `14af356170bc2b1fc6b09b667a8bff79aa72717203183dc5c6f224b9344f6937`; raw candlestick sha256 `bbf1a6d095bebd0540b704fd3a33c42f4c077c854df49dac5b5e105f8e79d0a8`. This is evidence-only: no threshold lowering, no post-hoc buckets, no sportsbook settlement labels, no paper/live promotion, and no account/order paths.

- 2026-07-06 sports line-move capture split: added a small Fable-directed stale-quote evidence slice on `codex/fable-line-move-capture`. New `make kalshi-sports-line-move-delta-logger` records delta-only sharp sportsbook current-odds changes to append-only JSONL under `/home/mrwatson/manual_drops/kalshi_sports_line_moves/` and writes only safety/provenance metadata under `docs/codex/macro/`. Default sport coverage is now `baseball_mlb`, `tennis_atp_wimbledon`, `soccer_fifa_world_cup`, and `americanfootball_nfl`; generic `tennis_atp` safely falls back to Wimbledon. Latest real run: `kalshi_sports_line_move_delta_logger_ready_with_deltas`, `4` provider snapshots, `104` events (`20` MLB, `4` Wimbledon, `5` World Cup, `75` NFL), `45` delta rows, `41` line moves, `0` provider errors, JSONL sha256 `b331c748335ac5167094c40af329b8a3c4134ba2f4c973bc865827cbf82231c6`. This is capture-only: no probabilities, signals, EV, paper stake, live/account/order paths, or sportsbook-derived settlement labels.

- 2026-07-06 sports consensus 21:10Z probe clearance: ran the due blocker-clearance heartbeat after the `2026-07-06T21:10:00Z` consensus settlement/probe clock. First pass exposed a real public Kalshi throttling failure: `scripts/kalshi_sports_proxy_observation_loop.py` crashed on HTTP 429 while fetching settled markets. Added bounded retry/backoff for 429 responses and a focused regression test, then reran `make kalshi-sports-blocker-clearance-cycle KALSHI_SPORTS_BLOCKER_CLEARANCE_RUN_DUE=1` successfully. Refreshed `make kalshi-sports-event-velocity-eta`, `make kalshi-claude-advice-audit`, and the final non-due clearance report. Latest consensus observation loop has `338` label rows, `0` due distinct contracts, and next public probe `2026-07-06T22:00:00Z`. Latest consensus falsification has `320` joined labels, `40` independent labels, `12` OOS labels, `0` tested hypotheses, `0` FDR survivors, and nearest deficient model `kalshi_vs_consensus_fade_overpriced_threshold_0.005` still at `5/10` OOS labels. Therefore no probability/paper/live promotion occurred. Latest blocker-clearance cycle is `sports_blocker_clearance_cycle_waiting_for_next_clock`, `0` due tasks, `2` waiting tasks, next clock `2026-07-06T22:00:00Z`; ATP remains `8/10` forward-OOS with next clock `2026-07-07T06:00:00Z`. Audit remains `8/10` evidence-satisfied and `10/10` implementation-satisfied with open evidence ids `CLAUDE-005` and `CLAUDE-008`. Paper remains `9` usable rows / `$1798.4` stake and live remains `0` eligible. No thresholds were lowered, no labels inferred, and no account/order/live paths were touched. Verification: blocker/collector/audit/sports-observation focused tests `30 passed`; touched-file Ruff clean; py-compile clean. Tranche note: `docs/codex/tranches/2026-07-06-sports-consensus-2110-probe-clearance.md`.

- 2026-07-06 Claude advice implementation/evidence split: cleared a false implementation blocker in the active Claude advice audit. `scripts/kalshi_claude_advice_audit.py` now reports both current evidence maturity (`status`) and whether the repo has built the mechanism Claude asked for (`implementation_status`). `scripts/kalshi_sports_blocker_clearance_cycle.py` now carries that implementation summary forward too, so the scheduler distinguishes completed implementation from external settlement clocks. Latest regenerated audit is still honest on evidence, `claude_advice_audit_ready_with_open_clock_or_statistical_items`, `8/10` evidence-satisfied with `CLAUDE-005` and `CLAUDE-008` clock-bound, but now records `implementation_satisfied_count=10/10`, `implementation_open_requirement_ids=[]`, and `implementation_status_counts={"satisfied": 10}`. Latest blocker-clearance cycle is `sports_blocker_clearance_cycle_waiting_for_next_clock`, `implementation_satisfied_count=10`, `implementation_open_requirement_ids=[]`, `0` due tasks, `2` waiting tasks, next clock `2026-07-06T21:10:00Z`. The remaining evidence clocks are unchanged and non-forcible: sports consensus rule/bucket accumulation waits for `2026-07-06T21:10:00Z` with nearest hypothesis OOS deficit `5`; ATP forward-OOS is `8/10` with next exact probe `2026-07-07T06:00:00Z`. No thresholds were lowered, no labels inferred, no candidate promoted, and no live/account/order path touched. Verification: `make kalshi-claude-advice-audit` exits `0`; `make kalshi-sports-blocker-clearance-cycle` exits `0`; focused audit/blocker tests `7 passed`; touched-file Ruff clean; py-compile clean. Tranche note: `docs/codex/tranches/2026-07-06-claude-advice-implementation-vs-evidence-audit.md`.

- 2026-07-06 always-on collector completion-time cadence: fixed a real unattended-collection scheduling defect surfaced while pursuing the Claude sports-advice blockers. `scripts/kalshi_always_on_collector.py` now records `completed_utc` and computes cadence from cycle completion time instead of `generated_utc`, so long-running public-data refresh cycles no longer emit a stale `next_run_utc` in the past. A real post-fix collector run proves the behavior: `generated_utc=2026-07-06T19:36:22Z`, `completed_utc=2026-07-06T19:41:16Z`, `next_run_utc=2026-07-06T19:42:16Z`, `next_run_utc > completed_utc`, `2/2` targets passed, `2/2` artifacts safe. Sports remains correctly clock-bound, not software-blocked: consensus rule-bucket accumulation next probe `2026-07-06T21:10:00Z`, ATP forward-OOS `8/10` with next exact clock `2026-07-07T06:00:00Z`. Verification: collector/blocker/audit focused tests `16 passed`; touched-file Ruff clean; py-compile clean; real `make kalshi-always-on-collector-once`, `make kalshi-sports-event-velocity-eta`, `make kalshi-claude-advice-audit`, and `make kalshi-sports-blocker-clearance-cycle` exit `0`. No thresholds were lowered, no labels inferred, and no account/order/live path touched. Tranche note: `docs/codex/tranches/2026-07-06-always-on-collector-completion-cadence.md`.

- 2026-07-06 sports blocker clearance cycle: cleared the remaining operational ambiguity in the Claude sports-advice blockers. Added `scripts/kalshi_sports_blocker_clearance_cycle.py` and `make kalshi-sports-blocker-clearance-cycle`, a research-only control-plane runner that reads the Claude audit, ATP evidence gate, and sports event-velocity ETA, then either emits the exact next clock or, when explicitly armed with `KALSHI_SPORTS_BLOCKER_CLEARANCE_RUN_DUE=1`, runs only the allowed public-data refresh sequence and re-audits. Current refreshed state: Claude audit is `8/10` satisfied and `2/10` clock-bound; consensus rule-bucket accumulation needs `5` more OOS labels at `2026-07-06T21:10:00Z`; ATP forward-OOS is now `8/10` resolved after refreshing `atp-oracle`, with next exact clock `2026-07-07T06:00:00Z`, adverse preliminary true-CLV (`mean=-5.06pp`, lower `-11.27pp`), and `0/25` executable-liquidity candidates. The new blocker-clearance artifact is `sports_blocker_clearance_cycle_waiting_for_next_clock`, `0` due tasks, `2` waiting tasks, next clock `2026-07-06T21:10:00Z`. No thresholds were lowered, no sportsbook labels were used as settlements, no EV/paper/live promotion changed, and no account/order path was touched. Verification: focused blocker-clearance tests `4 passed`; script py-compile clean; touched-file Ruff clean; `make kalshi-sports-event-velocity-eta`, `make kalshi-claude-advice-audit`, and `make kalshi-sports-blocker-clearance-cycle` exit `0`. Tranche note: `docs/codex/tranches/2026-07-06-sports-blocker-clearance-cycle.md`.

- 2026-07-06 Claude advice compliance audit: added a machine-readable control-plane audit for `docs/audits/Claude_Sonnet_5_Repo_Advice_7.3.26.md` so the active goal is now measured requirement-by-requirement instead of by narrative landing notes. New target `make kalshi-claude-advice-audit` writes `latest-kalshi-claude-advice-audit.{json,md,csv}` and checks 10 concrete advice requirements: near-resolution informed-flow candidate generation, informed-flow full gate chain, passive real paper-fill labels, passive FDR gate, ATP forward-OOS clock, World Cup outcome-family independence/correlation policy, prior-only donor guardrail, event-velocity forecast, live-hardening avoidance while zero eligible, and threshold integrity. Latest real audit status is `claude_advice_audit_ready_with_open_clock_or_statistical_items`: `8/10` satisfied, `2/10` clock-bound, `0` warnings, `0` unsafe artifacts, `0` missing artifacts. Satisfied evidence includes informed-flow candidate + gate chain (`ev_usable=24`, `paper_usable=10`), passive fill labels (`1826/30` valid, `597/3` fills) with no survivor correctly treated as a statistical rejection, World Cup parallel outcome clocks with portfolio clustering by match, donor priors with `0` label/paper/live credit, live `0` eligible, and preserved thresholds. Open clocks: `CLAUDE-008` event velocity next probe `sports_consensus_rule_bucket_accumulation` at `2026-07-06T03:00:00Z`, and `CLAUDE-005` ATP forward-OOS `2/10` with next probe `2026-07-06T06:00:00Z`. This audit is research-only and does not promote candidates, compute EV, size stake, lower thresholds, or touch execution. Verification: focused audit tests `3 passed`; touched-file Ruff clean; `make kalshi-claude-advice-audit` exits `0`; `make lint-baseline-check` exits `0`; `make quality` exits `0` with existing advisory Ruff/deptry backlog. Tranche note: `docs/codex/tranches/2026-07-06-claude-advice-compliance-audit.md`.

- 2026-07-06 sports next-probe blocker clearance: cleared the remaining machine-scheduling ambiguity in the sports evidence blocker map. `scripts/kalshi_sports_event_velocity_eta.py` now emits a first-class `summary.next_probe_surface` alongside `next_due_surface`, choosing the earliest future deficient non-rollup surface and ignoring stale past probe timestamps. `scripts/kalshi_sports_evidence_cycle_report.py` propagates that field and names it in `next_action`, so the top-level sports report now says exactly what to wait for instead of requiring row-table inference. Latest regenerated state: event velocity is `sports_event_velocity_eta_ready_with_label_deficits`, `next_due_surface=null`, `next_probe_surface=sports_consensus_rule_bucket_accumulation`, model `sports_consensus_price_bucket_bias_bucket_0.50_0.70`, next probe `2026-07-06T03:00:00Z`, OOS `5/10`, OOS deficit `5`; evidence cycle is `sports_evidence_cycle_ready_with_label_progress` and carries the same next-probe surface. Paper remains `10` usable unresolved rows, next paper close `2026-07-06T03:00:00Z`; passive maker remains evidence-tested with `0` FDR survivors; live remains `0` eligible. This tranche does not lower thresholds, infer labels, change paper/live promotion, compute new probabilities, or touch execution. Verification: focused ETA/evidence-cycle tests `16 passed`; touched-file Ruff clean; `make kalshi-sports-event-velocity-eta` and `make kalshi-sports-evidence-cycle-report` exit `0`; `make lint-baseline-check` exits `0`; `make quality` exits `0` with existing advisory Ruff/deptry backlog. Tranche note: `docs/codex/tranches/2026-07-06-sports-next-probe-blocker-clearance.md`.

- 2026-07-06 sports blocker ETA and burn-in clearance: cleared three remaining non-statistical blocker ambiguities while preserving all OOS/FDR, cost, capacity, correlation, decay, and live-mode gates. First, `scripts/kalshi_sports_event_velocity_eta.py` now emits an explicit `sports_consensus_rule_bucket_accumulation` ETA row for the nearest deficient no-vig consensus hypothesis cell instead of allowing the global `sports_consensus_all` rollup to hide the real blocker. Latest rule/bucket row is `sports_consensus_price_bucket_bias_bucket_0.50_0.70`, `5/10` OOS labels, OOS deficit `5`, `270` accumulation opportunities across `103` distinct pending contracts, `33` nearest-hypothesis opportunities, next probe `2026-07-06T03:00:00Z`. Second, `scripts/kalshi_sports_evidence_cycle_report.py` now treats the legacy passive-liquidity proxy-only blocker as superseded once real paper-fill labels exist and paper-fill falsification is ready; latest passive maker state is `passive_liquidity_paper_fill_falsification_ready_no_research_candidates`, `1752` paper fill/timeout labels, `0` FDR survivors. Third, `scripts/kalshi_atp_proxy_evidence_gate.py` now preserves `next_expected_expiration_utc`, and the ETA row carries the old ATP forward-OOS blocker clock: `2/10` forward-OOS resolved, next ATP probe `2026-07-06T06:00:00Z`, still blocked and not promotable. Ran the due paper burn-in after the `02:20Z` close boundary with settlement fetch enabled; no paper rows were due/settled, and the frozen paper state is now `10` usable unresolved rows, `$220.846241` paper stake, next paper close `2026-07-06T03:00:00Z`, portfolio caps observed, realized PnL `0`, retirement `10` active / `0` retired. Consensus falsification remains honestly blocked as `sports_consensus_falsification_blocked_no_testable_hypotheses`: `185` joined labels, `31` independent labels, `10` OOS labels, max hypothesis OOS `5/10`, `0` tested, `0` FDR survivors. Live remains `kalshi_live_blocked`, `470` decisions, `0` eligible, `$0` live stake. Verification: focused ATP/ETA/evidence tests `20 passed`; broader focused sports tests `34 passed`; `make test-unit` `1359 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits `0` (`lint 98/1422`, `format 12/94`); `make quality` exits `0` with existing advisory Ruff/deptry backlog; touched-file Ruff clean; touched-file `git diff --check` clean. Tranche note: `docs/codex/tranches/2026-07-06-sports-blocker-eta-and-burn-in-clearance.md`.

- 2026-07-06 sports blocker control-plane clearance: cleared two software/reporting blockers without weakening any statistical gate. First, paper settlement next-close state is now propagated end to end: `scripts/kalshi_paper_settlement_reconcile.py` computes `next_unresolved_close_time_utc` from unresolved usable paper rows, and both `scripts/kalshi_sports_paper_burn_in_cycle.py` and `scripts/kalshi_sports_evidence_cycle_report.py` surface that clock. Latest paper settlement state is `paper_settlement_reconciliation_waiting_for_close`, `22` usable paper rows, `22` unresolved, `0` due, total paper stake `$1792.624803`, and next unresolved close `2026-07-06T02:20:00Z`; burn-in and evidence-cycle reports now agree on the same close. Second, the sharp sports consensus falsification blocker is no longer vague: `predmarket/sports_consensus_falsification.py` now emits `hypothesis_accumulation_opportunities`, mapping current pending exact Kalshi consensus observations to the pre-registered deficient rule/bucket cells they would feed after public Kalshi settlement. Latest consensus falsification remains honestly blocked as `sports_consensus_falsification_blocked_no_testable_hypotheses`: `185` joined labels, `31` independent labels, `10` OOS labels, `0` tested hypotheses, `0` FDR survivors, nearest deficient model `sports_consensus_price_bucket_bias_bucket_0.50_0.70`, nearest OOS deficit `5`. The newly cleared control-plane surface shows `239` current accumulation opportunities across `101` distinct pending contracts, including `32` opportunities aimed at the nearest deficient hypothesis. No thresholds were lowered, no label was inferred from sportsbooks, no EV/paper/live promotion changed, and live remains `0` eligible. Verification: focused tests `25 passed`; `make test-unit` `1357 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits `0` (`lint 98/1422`, `format 12/94`); `make quality` exits `0` with existing advisory Ruff/deptry backlog; touched-file Ruff clean; py-compile clean; selected `git diff --check` clean. Tranche note: `docs/codex/tranches/2026-07-06-sports-blocker-control-plane-clearance.md`.

- 2026-07-06 sports event-velocity due-backlog precision: cleared the remaining calendar-control ambiguity in the sports sharp-consensus lane. `scripts/kalshi_sports_consensus_observation_loop.py` now emits due, already-labeled-due, and not-yet-due observed consensus contracts by sport, and `scripts/kalshi_sports_event_velocity_eta.py` consumes those per-sport counts instead of hiding due settlement backlog behind a future expiration clock or smearing the global due count across every sport. The due clock now also defers to fresh public Kalshi market state: if an archived observation expected expiration is in the past but Kalshi currently reports the market as `active` with a future `close_time` / `expiration_time`, it is not treated as due. The exact public Kalshi settlement probe ran cleanly: `47` observed tickers fetched, `31` finalized/settlement-ready, `0` probe errors, `0` new labels because those `31` were already present in the label archive. Latest observation summary: `0` unresolved due distinct contracts, `31` already-labeled due contracts split `28` MLB / `3` World Cup soccer, `194` existing consensus label rows, next public probe `2026-07-06T02:20:00Z`, next expected expiration `2026-07-06T02:20:00Z`, not-yet-due contracts by sport `{"baseball_mlb": 32, "football_nfl": 32, "soccer_world_cup": 18, "tennis_atp": 20}`. Latest ETA summary: `sports_event_velocity_eta_ready_with_label_deficits`, `next_due_surface=null`, `next_action=kalshi_sports_wait_for_next_settlement_clock`, ETA statuses `{"blocked_atp_forward_oos": 1, "blocked_no_current_nba_consensus_rows": 1, "label_threshold_met": 3, "label_threshold_met_downstream_gates_active": 1, "label_threshold_met_no_fdr_survivor": 1, "waiting_for_next_probe_or_settlement": 4}`, bottleneck counts `{"calendar_or_offseason_no_current_markets": 1, "calendar_settlement_labels": 7, "compute_or_downstream_gates": 2, "external_forward_oos": 1}`, total label deficit `119`, total OOS deficit `49`, actionable calendar label deficit `89`, actionable calendar OOS deficit `31`. The sports evidence-cycle report now surfaces the ETA status-count and bottleneck-count maps directly. No statistical gate was loosened, no non-Kalshi labels were used, no EV/paper/live promotion changed: consensus falsification remains `sports_consensus_falsification_blocked_no_testable_hypotheses` with `185` joined labels, nearest hypothesis OOS deficit `5`, `0` FDR survivors; live remains `0` eligible; current paper state remains `17` usable rows / `$2119.664117` paper stake from already-promoted informed-flow evidence. Verification: focused observation/ETA/evidence-cycle tests `26 passed`; `make test-unit` `1354 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits `0` (`lint 98/1422`, `format 12/94`); `make quality` exits `0` with existing advisory Ruff/deptry backlog; touched-file Ruff clean; py-compile clean; `make kalshi-sports-consensus-observation-loop`, `make kalshi-sports-event-velocity-eta`, and `make kalshi-sports-evidence-cycle-report` exit `0`; selected `git diff --check` clean. Tranche note: `docs/codex/tranches/2026-07-06-sports-event-velocity-due-backlog-precision.md`.

- 2026-07-06 live preflight blocker clearance: cleared two false software/data blockers at the paper-to-live boundary without arming execution. Internal `predmarket-alpha` paper candidates are now treated as control-repo evidence instead of external donor artifacts, so they no longer fail with `source repo artifact did not pass external preflight`; external donor candidates still require strict external-artifact preflight. `make kalshi-live-preflight` now captures unauthenticated public Kalshi market + orderbook snapshots for `paper_usable=true` tickers, so current market/ask/depth blockers are resolved before any credentials or live arming. The latest full `make kalshi-sports-evidence-cycle` run exits `0`; latest live preflight has `447` decisions, `0` live eligible, `$0` live stake, `15` safe external artifacts, and `17/17` informed-flow paper rows with fresh current ask/size. The remaining informed-flow live blockers are only deliberate live-mode gates: `live execution mode is disabled`, `config venues.kalshi.execution_enabled is false`, `KALSHI_LIVE_TRADING_ENABLED is not armed`, and `account balance missing`. Suppressed the derivative `live stake rounds down below one contract` noise when account balance is absent. `make kalshi-live-trader` fallback now uses the same read-only snapshot path and remains `kalshi_live_blocked`. Sports state after the full cycle: `17` paper-usable rows / `$2119.664117` paper stake, consensus preflight `78/80` valid rows with `0` timestamp blockers, consensus falsification still `sports_consensus_falsification_blocked_no_testable_hypotheses` with `185` joined labels and nearest OOS deficit `5`, passive maker has `0` FDR survivors, soccer remains the actionable sharp-provider gap, NBA is deferred, and covered sports are `mlb`, `tennis`, and `nfl`. Verification: focused live-engine tests `12 passed`; `make test-unit` `1348 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits `0`; `make quality` exits `0` with existing advisory Ruff/deptry backlog; touched-file Ruff clean; py-compile clean; `make kalshi-live-preflight`, `make kalshi-live-trader`, `make kalshi-sports-evidence-cycle-report`, and full `make kalshi-sports-evidence-cycle` exit `0`. Tranche note: `docs/codex/tranches/2026-07-06-live-preflight-blocker-clearance.md`.

- 2026-07-06 sports blocker clearance: cleared the remaining software/source ambiguity in the sharp-consensus sports lane without weakening any statistical gate. Added and wired a soccer Asian-sharp provider diagnostic (`make kalshi-sports-consensus-soccer-asian-provider-diagnostic`) that explicitly probes/reads target soccer providers `sbobet`, `singbet`, and `ibc`; the latest current probe at `2026-07-06T00:30:16Z` requested all 3, observed `0` target-provider rows, and observed only `pinnacle`, `betfair_exchange`, `matchbook`, and `smarkets`, so the World Cup provider blocker is now proven as external legal-feed availability rather than repo wiring. Fixed ATP provider accounting so current tennis set-winner markets are not mistaken for a sharp-provider maturity gap: the latest Kalshi ATP inventory is `36` `KXATPSETWINNER` rows, while the available sharp feed is match-winner `h2h`; `scripts/kalshi_sports_consensus_atp_donor_adapter.py` now reports `sports_consensus_atp_donor_adapter_blocked_no_compatible_atp_match_markets` instead of generic `blocked_no_atp_rows`, and provider audit treats tennis as `deferred_no_compatible_current_market`. Latest provider audit: covered sports `mlb,nfl`, deferred sports `tennis,nba`, actionable gap sports `soccer`. Latest consensus falsification remains correctly blocked as `sports_consensus_falsification_blocked_no_testable_hypotheses`: `185` joined labels, max hypothesis OOS `5/10`, nearest pre-registered hypothesis deficit `5`, `0` FDR survivors. Passive maker remains evidence-tested but unpromoted: `3` tested hypotheses, `0` FDR survivors, `1475` valid paper-fill labels, best candidate net EV `-0.0277421875`. Paper has `11` usable rows / `$1679.517429` stake from already-promoted evidence; live remains `kalshi_live_blocked` with `0` eligible rows. Verification: `make kalshi-sports-paper-burn-in-cycle KALSHI_SPORTS_PAPER_BURN_IN_FETCH=1`, `make kalshi-sports-consensus-atp-donor-adapter KALSHI_SPORTS_CONSENSUS_ATP_CAPTURE=1`, `make kalshi-sports-consensus-provider-audit`, and `make kalshi-sports-evidence-cycle-report` exit `0`; focused tests `29 passed`; `make test-unit` `1342 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits `0` (`lint 98/1422`, `format 12/94`); `make quality` exits `0` with existing advisory Ruff/deptry backlog; touched-file Ruff clean; `git diff --check` exits `0` with only the existing Makefile CRLF warning. Tranche note: `docs/codex/tranches/2026-07-06-sports-blocker-clearance-soccer-atp.md`.

- 2026-07-05 sports consensus hypothesis accumulation plan: cleared the remaining ambiguity in the sharp no-vig sports consensus blocker without weakening the gate. `predmarket/sports_consensus_falsification.py` now emits a machine-readable `hypothesis_accumulation_plan` for every pre-registered rule/threshold or price-bucket cell that still lacks enough OOS labels. The latest consensus falsification artifact remains honestly blocked as `sports_consensus_falsification_blocked_no_testable_hypotheses`: global floors are met (`31/30` independent labels, `10/10` OOS labels), but `0` hypotheses are testable, `0` survived FDR, max hypothesis OOS count is `5/10`, plan rows are `30`, and the nearest OOS deficit is `5` on `sports_consensus_price_bucket_bias` bucket `0.50_0.70`. The sports evidence cycle now surfaces `sports_consensus_falsification_max_hypothesis_oos_count`, `sports_consensus_falsification_hypothesis_accumulation_plan_count`, and `sports_consensus_falsification_nearest_hypothesis_oos_deficit`, so future runs can target label accumulation mechanically. Guardrails preserved: no threshold lowering, no post-hoc rules, accumulation plan rows are `research_only=true` and `usable=false`, and live remains `kalshi_live_blocked` with `0` eligible rows. Tranche note: `docs/codex/tranches/2026-07-05-sports-consensus-hypothesis-accumulation-plan.md`.

- 2026-07-05 sports consensus no-testable-hypotheses status: the post-00:00Z consensus probe cleared the global sports no-vig consensus label floor but exposed the narrower blocker. `make kalshi-sports-consensus-observation-loop KALSHI_SPORTS_CONSENSUS_PROBE_OBSERVED=1` added `22` exact consensus label rows, moving total labels to `194`, joined labels to `185`, independent labels to `31/30`, and OOS labels to `10/10`. `predmarket/sports_consensus_falsification.py` now distinguishes this state from true global label shortage: latest falsification is `sports_consensus_falsification_blocked_no_testable_hypotheses`, with `0` tested hypotheses, `0` FDR survivors, max hypothesis OOS count `5/10`, and evaluation status counts `{"blocked_insufficient_oos_labels": 30}`. The next action is rule/bucket-cell label accumulation, not threshold lowering or post-hoc rule creation. Provider accounting remains cleaned up: actionable provider gaps `["soccer"]`, deferred sports `["nba"]`, covered sports `mlb,tennis,nfl`. Live remains `kalshi_live_blocked` with `0` eligible rows. Verification: focused falsification/evidence-cycle tests `14 passed`, touched-file Ruff clean, `make kalshi-sports-consensus-observation-loop KALSHI_SPORTS_CONSENSUS_PROBE_OBSERVED=1`, `make kalshi-sports-consensus-falsification`, and `make kalshi-sports-evidence-cycle-report` exit `0`. Tranche note: `docs/codex/tranches/2026-07-05-sports-consensus-no-testable-hypotheses-status.md`.

- 2026-07-05 provider gap deferred NBA accounting: cleaned up a Claude-advice blocker classification issue in the sports sharp-consensus provider audit. NBA is currently offseason/no-current-row for the active Kalshi sports consensus surface, so it now remains visible as a target sport but is explicitly deferred via `KALSHI_SPORTS_CONSENSUS_PROVIDER_AUDIT_DEFERRED_SPORTS ?= nba` instead of being counted as an active provider maturity gap. `predmarket/sports_consensus_provider_policy.py` now supports config-backed deferred target sports, `scripts/kalshi_sports_consensus_provider_audit.py` accepts `--deferred-target-sports`, and `scripts/kalshi_sports_evidence_cycle_report.py` surfaces deferred/actionable provider gap fields. Latest provider audit: `sports_consensus_provider_audit_ready_with_per_sport_gaps`, covered sports `mlb,tennis,nfl`, deferred sports `nba`, actionable gap sports `soccer`, sport gap count `1`, sport deferred count `1`. Latest sports evidence cycle exposes the same blocker accounting and remains `sports_evidence_cycle_ready_with_label_progress`; consensus falsification is still label-blocked at `29/30` independent and `9/10` OOS with `0` tested hypotheses and `0` FDR survivors; live remains `kalshi_live_blocked` with `0` eligible rows. Verification: focused provider/evidence-cycle tests `18 passed`, touched-file Ruff clean, `make kalshi-sports-consensus-provider-audit` exits `0`, `make kalshi-sports-evidence-cycle-report` exits `0`. Tranche note: `docs/codex/tranches/2026-07-05-provider-gap-deferred-nba-accounting.md`.

- 2026-07-05 manual-drop provenance + always-on sports burn-in: cleaned up landing residue and removed another autonomy blocker from Claude's advice. `predmarket/kalshi_manual_drop_capture.py` no longer writes non-MLB raw snapshots with the legacy `kalshi_mlb_game_series_*` prefix: `KXNFLGAME` now writes `kalshi_nfl_game_series_<stamp>.json`, `KXWCGAME` writes `kalshi_world_cup_game_series_<stamp>.json`, and the snapshot status is now generic `kalshi_manual_drop_public_fetch_ok` / `kalshi_manual_drop_public_fetch_empty`. Fixed `scripts/kalshi_always_on_collector.py` so temp/dry-run output dirs no longer overwrite root macro latest pointers. Changed the unattended collector default from `sports_consensus,crypto` to `sports,crypto`; the default sports target now runs `kalshi-sports-paper-burn-in-cycle` with settlement fetch enabled, covering consensus, nondirectional microstructure, passive paper fills, paper settlement, decay, and live preflight, while the narrower `sports_consensus` target remains available. Real `make kalshi-always-on-collector-once` from canonical output is `kalshi_always_on_collector_ready`: targets `sports` and `crypto`, failed targets `0`, safe artifacts `2/2`, cadence `60s`. The regenerated public Kalshi snapshot paths are now honest: `/home/mrwatson/manual_drops/kalshi/kalshi_nfl_game_series_20260705T233914Z.json` and `/home/mrwatson/manual_drops/kalshi/kalshi_world_cup_game_series_20260705T233915Z.json`. Latest sports state: consensus preflight `66/66` valid with `0` timestamp blockers; consensus falsification still correctly blocked at `29/30` independent and `9/10` OOS labels with `0` tested hypotheses and `0` FDR survivors; passive liquidity now has `3042` paper intents, `1378` valid paper-fill labels, `149` valid paper fills (`154` raw filled count), `3` tested hypotheses, `0` FDR survivors, and best candidate net EV `-0.0332520548`; paper has `22` usable rows and `$3465.373827` stake; live remains `kalshi_live_blocked` with `0` eligible. Verification: focused manual-drop/always-on/sports-consensus tests pass; `make test-unit` `1333 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits 0 (`lint 98/1422`, `format 7/94`); touched-file Ruff clean; real `make kalshi-sports-paper-burn-in-cycle KALSHI_SPORTS_PAPER_BURN_IN_FETCH=1` and real `make kalshi-always-on-collector-once` both exit 0. Tranche note: `docs/codex/tranches/2026-07-05-manual-drop-provenance-and-always-on-sports-burn-in.md`.

- 2026-07-05 sports consensus autonomous refresh burn-in: cleared the last software/source blockers in the sharp no-vig sports consensus lane and made the evidence cycle autonomous instead of env-var dependent. `KALSHI_SPORTS_CONSENSUS_PROBE_OBSERVED` now defaults to `1`, so normal cycles probe already-observed contracts for public Kalshi settlement labels. Added `kalshi-sports-consensus-public-kalshi-refresh` to refresh current public Kalshi snapshots for `KXNFLGAME` and `KXWCGAME`, and rewired `kalshi-sports-consensus-refresh` plus `kalshi-sports-evidence-cycle` so the full chain runs universe scan, NFL/World Cup Kalshi refresh, MLB sharp consensus capture, ATP/NFL/soccer/NBA adapters with capture enabled, and preflight before observing/falsifying. Real post-23:00Z state: consensus preflight is `sports_consensus_preflight_ready` with `76` candidates, `76` valid, `0` blocked, `0` timestamp blockers, and `0` single-book blockers; consensus labels increased to `172`; falsification joins `163` labels and is now only `29/30` independent and `9/10` OOS, with `0` tested hypotheses and `0` FDR survivors because the hard statistical minimum is still one label short on both axes. Event-velocity reports `0` external/source blockers, `2` waiting-evidence blockers, `4` calendar-label blockers, total label deficit `122`, total OOS deficit `50`, and no due surface. Provider audit now has strict consensus coverage for `mlb`, `tennis`, `soccer`, and `nfl`; remaining sport gaps are `2`, with NBA still offseason/no current rows. Paper remains honest: `18` usable near-resolution-flow rows, `$2029.188553` paper stake, `paper_settlement_reconciliation_waiting_for_close`, next paper close `2026-07-05T23:30:00Z`; live remains `kalshi_live_blocked` with `0` eligible rows. Verification: focused tests `24 passed`; `make test-unit` `1330 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits 0 (`lint 98/1422`, `format 7/94`); `make quality` exits 0 with existing advisory Ruff/deptry backlog; touched Python Ruff clean; `make -n kalshi-sports-consensus-refresh` confirms the full autonomous refresh chain; `make kalshi-sports-paper-burn-in-cycle KALSHI_SPORTS_PAPER_BURN_IN_FETCH=1` exits 0 after both 22:30Z and 23:00Z settlement clocks. Tranche note: `docs/codex/tranches/2026-07-05-sports-consensus-autonomous-refresh-burn-in.md`.

- 2026-07-05 sports consensus source-blocker clearance: cleared the stale/current-source blockers in the sharp no-vig sports consensus lane without weakening statistical gates. ATP now has a current The Odds API + current Kalshi `KXATPMATCH` capture path instead of relying on stale Wimbledon donor JSONL; NFL and World Cup Kalshi public game snapshots were refreshed before rebuilding strict adapters; consensus preflight now has `91` candidates, `85` valid, `6` rejected, `0` timestamp blockers, and the only rejected rows are `6` MLB single-book rows blocked by `insufficient_distinct_books`. A bounded soccer Asian-sharp probe for `sbobet`, `singbet`, and `ibc` returned current World Cup events but `0` bookmaker rows, confirming that soccer Asian-sharp maturity remains an external provider-coverage gap rather than a repo wiring bug. The 21:30Z public settlement probe wrote a new exact-label packet and moved consensus labels from `48` to `72`; falsification remains correctly blocked at `18/30` independent labels and `6/10` OOS labels, with `0` tested hypotheses and `0` FDR survivors. ETA accounting now separates true stale/source blockers from waiting-evidence blockers: latest event velocity reports `0` external/source blockers, `4` actionable calendar label blockers, `2` waiting-evidence blockers, no due surface, and next action `kalshi_sports_wait_for_next_settlement_clock`. Verification: focused tests `17 passed`; `make test-unit` `1330 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits 0 (`lint 98/1422`, `format 7/94`); touched-file Ruff clean; `git diff --check` clean apart from existing CRLF warnings. Tranche note: `docs/codex/tranches/2026-07-05-sports-consensus-source-blocker-clearance.md`.

- 2026-07-05 sports blocker clearance + covariance-normalized paper sizing: refreshed the due sports evidence clocks with `KALSHI_SPORTS_CONSENSUS_PROBE_OBSERVED=1 make kalshi-sports-label-accumulation-cycle`, converting several blockers into explicit pass/next-gate states. Label accumulation now reports `sports_label_accumulation_oos_fdr_paper_candidates_ready`: MLB has `340` exact labels / `56` independent labels, World Cup soccer has `1340` exact labels / `136` independent labels and `2` research candidates, and ATP has `382` exact/independent labels; total family label deficit is `0`. Consensus observation was refreshed again after the `20:05Z` settlement clock: the latest consensus lane has `63` valid rows / `375` observations / `48` joined labels, including `8` new labels, but falsification remains honestly blocked at `16` independent labels, `5` OOS labels, `0` tested hypotheses, and `0` FDR survivors, still short of the `30` independent / `10` OOS minimum. World Cup outcome independence is `world_cup_outcome_independence_diagnostic_ready_parallel_outcome_clocks`, clearing the prior independence-review blocker while preserving match-level portfolio clustering. Near-resolution informed flow remains `near_resolution_flow_replay_gates_ready_for_ev_ledger_promotion`: `27` current candidates, `23` positive-depth rows, `14` raw correlation clusters, `12` controlled clusters, `60` OOS labels with `51` correct, q-value `6.16e-08`, and decay survival pass. Fixed a mechanical paper-sizing bug in `predmarket/paper_decision_engine.py`: covariance penalty had been subtracting `lambda * rho * dollar_stake * dollar_stake`, producing dollar-squared penalties such as `$2000` against `$200` positions. It now normalizes by paper bankroll, i.e. `lambda * rho * stake_i * (same_cluster_other_stake / bankroll)`, and passes the actual paper bankroll from the pipeline. Also hardened paper portfolio-cap enforcement so baskets with fewer than the required cluster count block as `max_cluster_share cap infeasible` instead of misleadingly clipping to `ratio=0.0`. Raised the research-only passive-liquidity paper TTL default to `43200` seconds and split historical TTL/cadence mismatches from the active clock; current active TTL mismatch count is `0`. Refined the sports ETA control plane so passive paper-fill surfaces with enough fill/OOS evidence are classified as `label_threshold_met_no_fdr_survivor` / `compute_or_downstream_gates`, not `paper_fill_clock`, and so consensus next-probe clocks use `next_expected_expiration_utc` instead of stale last-probe timestamps. ATP is now correctly classified as `external_forward_oos` after `382` settlement labels but only `2/10` forward-OOS resolved rows. The current ETA summary is `7` label-blocked surfaces, `0` paper-fill-blocked surfaces, total label deficit `148`, total OOS deficit `58`, and no immediate due surface; next consensus probe is `2026-07-05T20:35:00Z`. After refreshing microstructure, terms, EV, paper, settlement, decay, live, and report artifacts, the real paper state is `paper_decision_candidates_ready_with_paper_sized_rows`, `27` paper-usable microstructure-informed-flow rows, total paper stake `$5060.815328`, and `0` portfolio cap breaches. Paper settlement fetch found `0` due unresolved usable rows, so settlement remains legitimately `waiting_for_close`; retirement ledger has `10` active / `0` retired signals, and live preflight remains correctly `kalshi_live_blocked` with `0` live-eligible rows and `$0` live stake. Passive maker is no longer software/clock-blocked: `951` paper fill/timeout labels, `17` paper fills, `3` tested hypotheses, `0` FDR survivors, `0` research candidates, and `real_exchange_fill_label_count=0`. Verification: focused paper/covariance/portfolio-cap tests `84 passed`; focused fill-clock/cycle tests `8 passed`; focused ETA tests `7 passed`; `make test-unit` `1327 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits `0` (`lint 98/1422`, `format 8/94`); `make quality` exits `0` with existing advisory Ruff/deptry backlog; `git diff --check` exits `0` with only existing CRLF warnings. Tranche note: `docs/codex/tranches/2026-07-05-sports-blocker-clearance-paper-covariance.md`.

- 2026-07-05 prior-only donor gate: closed another explicit Claude-advice gap by making donor/cold-start priors a first-class, machine-auditable control-plane artifact instead of an implicit convention. Added `predmarket/prior_only_donor.py`, `scripts/kalshi_prior_only_donor_gate.py`, `tests/test_kalshi_prior_only_donor_gate.py`, `make kalshi-prior-only-donor-gate`, and latest pointers under `docs/codex/macro/latest-prior-only-donor-gate.*`. The gate consumes strict external-artifact preflight plus the safe formula registry, admits donor probabilities only as `hypothesis_generation_only` prior context, and hard-codes zero settlement-label, independent-label, OOS-label, EV, paper, and live credit for every donor row. Real refreshed state: external preflight `15/16` safe artifacts with `974` safe rows; signal formula registry `2` formulas ready for the multiple-testing ledger; prior-only donor gate `prior_only_donor_gate_ready` with `974` prior context rows, `797` eligible prior context rows, `177` blocked rows, `0` settlement label credit, `0` independent label credit, `0` OOS label credit, `0` direct probability promotions, `0` paper-usable rows, and `0` live-eligible rows. Source counts: NFL `830`, ATP `78`, MLB `54`, statarb `8`, NBA `4`. The writer was hardened so tmp test output directories no longer overwrite root macro latest pointers; the real latest artifact was refreshed afterward. Verification: focused prior-only tests `4 passed`; touched-file Ruff check/format and py-compile clean; `make kalshi-external-artifact-preflight`, `make kalshi-signal-formula-registry`, and `make kalshi-prior-only-donor-gate` exit 0; `make test-unit` `1322 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits 0 (`lint 100/1422`, `format 8/94`); `make quality` exits 0 with the existing advisory Ruff/deptry backlog; `git diff --check` exits 0 with only CRLF warnings. Honest Claude implementation estimate after this tranche: roughly `93%` as control-plane/software, still below 100% because exact sports consensus OOS/FDR survival is label-bound, soccer still needs mature Asian-sharp enrichment, NBA has no current rows in season, passive maker evidence still has only `3` paper fills and no accepted FDR survivor, and live remains correctly blocked until paper evidence justifies it. Tranche note: `docs/codex/tranches/2026-07-05-prior-only-donor-gate.md`.

- 2026-07-05 passive-liquidity fill-clock diagnostic + paper-fill unblock: closed the passive-liquidity bottleneck that was hidden behind timeout-only labels. Added `scripts/kalshi_passive_liquidity_fill_clock_diagnostic.py`, `tests/test_kalshi_passive_liquidity_fill_clock_diagnostic.py`, `make kalshi-passive-liquidity-fill-clock-diagnostic`, and sports-cycle wiring so the cycle now audits why passive maker intents do or do not produce paper fills. The diagnostic proved the old paper maker TTL was expiring before later public snapshots could observe touches: initial real state had `462` intents, `226` timeouts, `0` fills, `0` future snapshots inside TTL, and a recommended TTL above the configured clock. `KALSHI_PASSIVE_LIQUIDITY_TTL_SECONDS` is now `3600`, and two nondirectional watch passes produced the first actual paper-fill labels: latest passive paper-fill loop has `942` intents, `461` labels, `3` paper fills, `458` timeouts, and `481` open intents. Passive paper-fill falsification now runs instead of blocking on missing fills: `1` tested hypothesis, `0` FDR survivors, `0` research candidates, `real_exchange_fill_label_count=0`, and all outputs remain research-only/usable=false. Sports evidence cycle reports `29/29` safe artifacts, passive fill-clock diagnostic `ready_with_paper_fills`, paper still `4` usable near-resolution-flow rows with `$33.659219` stake, and live remains blocked with `0` live-eligible rows. Verification: focused fill-clock/cycle tests `7 passed`; touched-file Ruff check/format and py-compile clean; `make kalshi-sports-nondirectional-evidence-watch-once` exits 0 twice under the corrected clock; `make kalshi-sports-evidence-cycle-report` exits 0; `make test-unit` `1313 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits 0 (`lint 100/1422`, `format 8/94`); `make quality` exits 0 with existing advisory Ruff/deptry output; `git diff --check` exits 0 with only line-ending warnings. Tranche note: `docs/codex/tranches/2026-07-05-passive-liquidity-fill-clock-diagnostic.md`. Honest Claude implementation estimate after this tranche: roughly `90%`; remaining gaps are exact consensus OOS/FDR survival from accumulated labels, NBA strict consensus feed, soccer Asian-sharp enrichment, more passive maker paper-fill labels with adverse-selection evidence, repeated post-close paper P&L/decay updates, and live gating only after paper evidence justifies it.

- 2026-07-05 World Cup outcome-independence diagnostic: closed another explicit Claude-advice gap by making soccer outcome-level independence auditable instead of letting exact-contract label counts stand in for independent evidence. Added `scripts/kalshi_world_cup_outcome_independence_diagnostic.py`, `tests/test_kalshi_world_cup_outcome_independence_diagnostic.py`, `make kalshi-world-cup-outcome-independence-diagnostic`, and sports-cycle wiring. The diagnostic separates `exact_contract_ticker`, `event_ticker`, `match_key|outcome_family`, and `match_key` counting units, and requires portfolio correlation to stay at `world_cup_match` even if totals/BTTS/spreads run as parallel hypothesis clocks. Real refreshed state: `world_cup_outcome_independence_diagnostic_ready_candidate_independence_review`; `93` exact-contract labels collapse to `25` event-market labels, `23` match/outcome-family clocks, and only `3` match-level correlation clusters. The current World Cup research candidates therefore require independence review before any downstream EV/paper promotion. `make kalshi-sports-event-velocity-eta` now consumes the diagnostic and marks `world_cup_proxy_directional` as `blocked_world_cup_independence_review` with `7` outcome-family label deficit and `10` OOS deficit, instead of reporting the exact-contract threshold as fully cleared. Sports event-velocity now reports `10/10` safe artifacts, `10` label/OOS-blocked surfaces, `1` paper-fill-blocked surface, and bottleneck counts including `independence_definition_review=1`. Sports evidence cycle now reports `28/28` safe artifacts, World Cup candidate independence review `true`, paper still `4` usable near-resolution-flow rows with `$33.659219` stake, and live remains blocked with `0` live-eligible rows. Verification: focused World Cup/ETA/cycle tests `11 passed`, touched-file Ruff check/format and py-compile clean, `make kalshi-world-cup-outcome-independence-diagnostic`, `make kalshi-sports-event-velocity-eta`, `make kalshi-sports-evidence-cycle-report`, and `make kalshi-signal-factory-status` exit 0; `make test-unit` `1309 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits 0 (`lint 100/1422`, `format 8/94`); `make quality` exits 0 with the existing advisory Ruff/deptry backlog; `git diff --check` exits 0 with only existing CRLF warnings. Tranche note: `docs/codex/tranches/2026-07-05-world-cup-outcome-independence-diagnostic.md`. Honest Claude implementation estimate after this tranche: roughly `88%`; remaining gaps are exact consensus OOS/FDR survival, NBA strict consensus feed, soccer Asian-sharp enrichment, actual passive maker paper fills, repeated post-close paper P&L/decay updates, and live gating only after paper evidence justifies it.

- 2026-07-05 sports event-velocity ETA control plane: closed another explicit Claude-advice gap by making "insufficient labels" a first-class, per-surface ETA/backlog artifact instead of an opaque blocked status. Added `scripts/kalshi_sports_event_velocity_eta.py`, `tests/test_kalshi_sports_event_velocity_eta.py`, `make kalshi-sports-event-velocity-eta`, and sports-cycle wiring so the full cycle refreshes ETA before writing the final report. The artifact is research-only and only reports evidence deficits: no probability, EV, stake, order, or account path. Real refreshed state: `sports_event_velocity_eta_ready_with_paper_fill_deficits`, `11` ETA surfaces, `9` label/OOS-blocked surfaces, `1` paper-fill-blocked surface, next due surface `sports_consensus_all` with `14` due contracts, NBA strict consensus explicitly `blocked_missing_strict_consensus_feed`, passive liquidity `blocked_waiting_for_paper_maker_fills`, near-resolution flow `label_threshold_met_downstream_gates_active`, and World Cup proxy `label_threshold_met`. Sports evidence cycle now reports `27/27` safe artifacts and remains `sports_evidence_cycle_ready_with_label_progress`; live remains blocked with `0` live-eligible rows. Verification: focused ETA/cycle tests `7 passed`; touched-file Ruff check/format and py-compile clean; `make kalshi-sports-event-velocity-eta` and `make kalshi-sports-evidence-cycle-report` exit 0; `make test-unit` `1305 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits 0 (`lint 100/1422`, `format 8/94`). Tranche note: `docs/codex/tranches/2026-07-05-sports-event-velocity-eta.md`. Honest Claude implementation estimate after this tranche: roughly `85%` of the attachment is implemented. Remaining gaps are exact consensus OOS/FDR survival from future labels, NBA strict consensus feed, soccer Asian-sharp enrichment, World Cup outcome-level independence diagnostics, actual passive paper fills, repeated post-close paper P&L/decay updates, and live gating only after paper evidence justifies it.

- 2026-07-05 passive-liquidity paper-fill falsification gate: closed the next Claude-advice gap by adding an explicit falsification ledger for passive maker paper-fill labels. Added `scripts/kalshi_passive_liquidity_paper_fill_falsification.py`, `tests/test_kalshi_passive_liquidity_paper_fill_falsification.py`, `make kalshi-passive-liquidity-paper-fill-falsification`, and sports-cycle wiring so passive liquidity no longer stops at intent/timeout collection. The gate joins persisted paper maker intents, later public-snapshot paper fill/timeout labels, and microstructure snapshots; tests all/YES/NO maker-side buckets with chronological OOS split and BH-FDR; and keeps the boundary strict: paper labels are not exchange fills, `real_exchange_fill_label_count` remains `0`, no calibrated probability/EV/stake/order is emitted, and all outputs stay `usable=false`. Real refreshed state: passive paper-fill loop has `462` intents and `226` valid labels, but `0` paper fills and `226` timeouts, so falsification is correctly `passive_liquidity_paper_fill_falsification_blocked_no_paper_fills` with `0` tested hypotheses and `0` FDR survivors. Sports cycle now reports `26/26` safe artifacts, `4` paper-usable near-resolution flow rows, `$33.659219` paper stake, `0` paper cap breaches, and live preflight remains `kalshi_live_blocked` with `0` live-eligible rows. Verification: focused passive/cycle tests `7 passed`; touched-file Ruff check/format clean; `make test-unit` `1301 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits 0 (`lint 100/1422`, `format 8/94`); `make quality` exits 0 with existing advisory Ruff/deptry backlog; `git diff --check` has only existing line-ending warnings. Tranche note: `docs/codex/tranches/2026-07-05-passive-liquidity-paper-fill-falsification.md`. Honest Claude implementation estimate after this tranche: roughly `82%` of the attachment is implemented. Remaining gaps are exact consensus OOS/FDR survival, NBA strict consensus, soccer Asian-sharp enrichment, event-velocity ETA reporting, World Cup outcome-level independence diagnostics, actual passive paper fills, repeated post-close paper P&L/decay updates, and eventual live gating only after paper evidence justifies it.

- 2026-07-05 paper portfolio cap clipping unlock for near-resolution flow: repaired the paper allocation layer so feasible multi-cluster EV survivors are clipped into portfolio caps instead of hard-blocked. `predmarket/paper_decision_engine.py` now reuses the shared `controlled_cluster_costs` cap math: single-cluster baskets remain zero-stake infeasible under `max_cluster_share=0.35`, while diversified baskets keep nonzero paper stake after proportional clipping. Added a regression test proving a 3-cluster basket remains paper-usable with no cap breaches. Real refreshed state: EV ledger has `18` usable rows including `4` `microstructure_informed_flow` rows with verified official terms; paper decisions now report `paper_decision_candidates_ready_with_paper_sized_rows`, `4` paper-usable rows, `$33.659219` total paper stake, `0` portfolio cap breaches, `1` cap-adjusted row, and `0` cap-blocked rows. Settlement reconciliation is `waiting_for_close`, retirement ledger has `0` retired signals, sports cycle is `ready_with_label_progress`, and live preflight remains `kalshi_live_blocked` with `0` live-eligible rows. Verification: focused paper/autonomous tests `26 passed`; touched-file Ruff check/format clean; `make kalshi-paper-decision-candidates`, `make kalshi-paper-settlement-reconcile`, `make kalshi-signal-decay-retirement KALSHI_SIGNAL_DECAY_RETIREMENT_PAPER_DECISIONS=docs/codex/macro/latest-paper-settlement-reconciliation.json`, `make kalshi-live-preflight`, and `make kalshi-sports-evidence-cycle-report` exit 0. Honest Claude implementation estimate after this tranche: overall advice is roughly `80%` implemented, not 100%; remaining work is exact consensus settlement labels/OOS-FDR survival, NBA strict consensus, soccer Asian-sharp enrichment, passive-liquidity paper-fill FDR, and repeated post-close paper P&L/decay updates.

- 2026-07-05 passive-liquidity paper fill loop + Claude implementation audit: continued following Claude's sports advice by starting the missing passive-liquidity maker evidence clock without pretending public orderbook proxies are exchange fills. Added `scripts/kalshi_passive_liquidity_paper_fill_loop.py`, `tests/test_kalshi_passive_liquidity_paper_fill_loop.py`, `make kalshi-passive-liquidity-paper-fill-loop`, and wired it into `make kalshi-sports-nondirectional-evidence-watch-once`. The loop persists paper maker quote intents outside the repo under `/home/mrwatson/manual_drops/kalshi_passive_liquidity_paper_fills/`, then labels only previously persisted intents from later public snapshots as `paper_filled_from_later_public_touch` or `paper_expired_unfilled_no_public_touch`; same-run future labels are explicitly blocked and `real_exchange_fill_label_count` remains `0`. Sports evidence cycle now treats the loop as the 25th safe artifact and surfaces passive paper intent/open/label counts separately from counterfactual proxy labels. Real run exits 0: microstructure has `3022` historical rows, `881` forward quote labels, `164` passive counterfactual proxy labels, and `7` would-touch proxies; near-resolution informed flow still has research candidates but replay is blocked by current capacity/depth; passive paper-fill loop is `passive_liquidity_paper_fill_loop_blocked_no_paper_intents` because the current 120-row near-resolution sample has no two-sided books and no one-sided ask above `1c` that can support a conservative passive bid. Latest sports cycle: `25/25` safe artifacts, `0` passive paper intents, `0` passive paper fill labels, `0` live-eligible rows. Verification: focused passive/cycle tests `6 passed`; touched-file Ruff clean; py-compile clean; `make kalshi-sports-nondirectional-evidence-watch-once` and `make kalshi-sports-evidence-cycle-report` exit 0. Tranche note: `docs/codex/tranches/2026-07-05-passive-liquidity-paper-fill-loop.md`. Honest Claude implementation estimate after this tranche: sports sharp-consensus wrapping remains `80%` by target sport count (`4/5`) and mature provider-policy coverage remains `60%` (`3/5`); near-resolution informed-flow plumbing is ~`90%` but currently blocked by live capacity/depth; passive liquidity moves from proxy-only to ~`55%` implemented because the stateful paper-intent/label loop exists but has no current paper intents/labels and no FDR acceptance ledger from paper fills yet; the overall Claude advice set is roughly `75%` implemented, with the remaining 25% concentrated in label accumulation, NBA strict consensus, soccer Asian-sharp enrichment, passive paper fill labels + FDR, capacity/depth unblock, and paper promotion only after gates pass.

- 2026-07-05 World Cup strict consensus adapter + Claude coverage audit repair: continued implementing Claude's sports advice by moving World Cup `KXWCGAME` markets into the timestamp-matched sharp no-vig consensus lane instead of leaving soccer only in the proxy/model lane. Added `predmarket/sports_consensus_soccer_adapter.py`, `scripts/kalshi_sports_consensus_soccer_adapter.py`, `tests/test_kalshi_sports_consensus_soccer_adapter.py`, and `make kalshi-sports-consensus-soccer-adapter`; wired the soccer adapter into `make kalshi-sports-consensus-refresh` before strict preflight. The adapter handles soccer's 3-way H2H structure by normalizing home/away/draw implied probabilities across each sharp book, then mapping each Kalshi binary outcome ticker to its selected 3-way probability versus the field. Real run: refreshed current public Kalshi `KXWCGAME` data (`21` markets, `7` games x `3` outcomes), captured current The Odds API `soccer_fifa_world_cup` rows, and emitted `84` strict soccer reference rows across `21` exact Kalshi tickers, `7` matched games, and `4` sharp/exchange providers (`pinnacle`, `betfair_exchange`, `matchbook`, `smarkets`) with zero skips. Latest preflight is `sports_consensus_preflight_ready_with_rejected_rows`: `53` valid candidates (`32` NFL, `21` World Cup) and `46` stale ATP/MLB candidates rejected by timestamp skew. Observation loop archived `21` new World Cup consensus observations, bringing consensus observations to `247`; falsification remains correctly blocked with `16` joined labels, `8` independent labels, `3` OOS labels, `0` tested hypotheses, and `0` FDR survivors. Provider audit now distinguishes strict-row coverage from mature provider-policy coverage: strict consensus rows exist for `4/5` target sports (`mlb`, `tennis`, `soccer`, `nfl`), while mature coverage remains `3/5` (`mlb`, `tennis`, `nfl`) because soccer still lacks an Asian sharp source (`SBOBet`/`Singbet`/`IBC`) and NBA has no strict feed. Sports evidence cycle reports `0` paper stake and `0` live-eligible rows. Verification: focused soccer/NFL/provider/cycle tests pass; `make test-unit` `1290 passed / 15 deselected`; `make test-integration` `14 passed`; `make lint-baseline-check` exits 0 (`lint 100/1422`, `format 8/94`); `make quality` exits 0 with existing advisory Ruff/deptry backlog; `git diff --check` has only existing line-ending warnings; soccer Kalshi manual capture, soccer adapter, preflight, observation loop, falsification, provider audit, and sports evidence cycle report exit 0. Tranche note: `docs/codex/tranches/2026-07-05-soccer-strict-consensus-adapter.md`. Honest Claude implementation estimate after this tranche: sports sharp-consensus wrapping is `80%` by target sport count (`4/5`), mature sharp-provider coverage is `60%` (`3/5`), and the whole Claude advice set is roughly `70%` implemented because label velocity, OOS/FDR survival, paper promotion, NBA, soccer Asian sharp enrichment, cadence hardening, and live-autonomous hygiene are still incomplete.

- 2026-07-05 NFL strict consensus adapter: continued implementing Claude's sports advice by moving NFL from raw sharp-provider availability into the strict timestamp-matched sports consensus chain. Refreshed current public Kalshi `KXNFLGAME` market data research-only (`66` markets, `0` series errors), added `predmarket/sports_consensus_nfl_adapter.py`, `scripts/kalshi_sports_consensus_nfl_adapter.py`, `tests/test_kalshi_sports_consensus_nfl_adapter.py`, and `make kalshi-sports-consensus-nfl-adapter`; wired the NFL adapter into `make kalshi-sports-consensus-refresh` before strict preflight. A real NFL sharp capture through The Odds API (`pinnacle,circa,bookmaker,betcris,betfair_ex_uk,matchbook,smarkets`) produced exact `KXNFLGAME` mappings for `16` NFL games, `64` strict reference rows, `32` unique Kalshi tickers, and `2` observed sharp/exchange books (`pinnacle`, `smarkets`). The combined strict reference now has `198` rows across MLB/ATP/NFL (`90` MLB, `44` tennis, `64` NFL), and provider audit now covers `3/5` target sports (`mlb`, `tennis`, `nfl`) with remaining gaps `soccer` and `nba`. Latest preflight is `sports_consensus_preflight_ready_with_rejected_rows`: `32` valid candidates, all current NFL; `46` stale MLB/ATP candidates rejected by `timestamp_skew_exceeds_policy`. The observation loop archived `32` new NFL consensus observations, bringing consensus observations to `226` over `92` distinct contracts. Falsification remains correctly blocked: `16` joined labels, `8` independent labels, `3` OOS labels, `0` tested hypotheses, `0` FDR survivors. No EV/paper/live promotion occurred. Verification: focused NFL/ATP/provider tests `18 passed`, touched-file Ruff check/format clean, NFL manual Kalshi capture, NFL adapter, consensus preflight, provider audit, observation loop, falsification, and sports evidence cycle report exit 0. Tranche note: `docs/codex/tranches/2026-07-05-nfl-strict-consensus-adapter.md`. Honest Claude implementation estimate after this tranche: sports sharp consensus coverage is about `60%` of the target sports surface by sport count (`3/5`) but still less than half of the full production system because soccer/NBA, continuous cadence controls, sufficient labels, and paper promotion remain incomplete; Claude's whole advice set is roughly `60%` implemented.

- 2026-07-04 sharp MLB consensus provider promotion: continued implementing Claude's sports advice by upgrading the production MLB sports consensus feed from secondary-only books to timestamp-matched sharp/exchange providers. Fixed `predmarket/sports_consensus_provider_policy.py` so nested raw The Odds API bookmaker rows inherit parent sport/market context, added bookmaker-targeted capture support to `predmarket/sports_consensus_reference_builder.py` and `scripts/kalshi_sports_consensus_reference_build.py`, added probe-only `make kalshi-sports-consensus-sharp-provider-capture`, and changed production MLB consensus defaults to `pinnacle,betfair_ex_uk,matchbook,smarkets`. The probe target exits 0 with 92 exact MLB reference rows, 32 unique Kalshi tickers, and 4 distinct books without overwriting production consensus. Latest collector-backed refresh exits 0: MLB strict rows `90`, ATP strict donor rows `44`, combined strict reference rows `134`, combined exact Kalshi tickers `46`, preflight `sports_consensus_preflight_ready`, valid candidates `46`, blockers `0`, and distinct books `5`. Latest provider audit now covers `2/5` target sports (`mlb`, `tennis`) with remaining gaps `soccer`, `nfl`, `nba`; MLB strict providers are `betfair_exchange`, `matchbook`, `pinnacle`, `smarkets`, with `pinnacle` as the strict anchor. Downstream falsification remains correctly blocked: `194` observations, `16` joined labels, `8` independent labels, `3` OOS labels, `0` tested hypotheses, `0` FDR survivors. Paper/live remain blocked: `0` paper-usable rows and `0` live-eligible rows. Verification: focused sports consensus/provider/cycle tests `41 passed`, touched-file Ruff clean, `make kalshi-sports-consensus-sharp-provider-capture`, `make kalshi-sports-consensus-refresh`, `make kalshi-sports-consensus-observation-loop KALSHI_SPORTS_CONSENSUS_PROBE_OBSERVED=1`, `make kalshi-sports-consensus-provider-audit`, `make kalshi-sports-consensus-falsification`, `make kalshi-sports-evidence-cycle-report`, and `make kalshi-always-on-collector-once` exit 0; `make test-unit` `1281 passed / 15 deselected`; `make quality` exits 0 with existing advisory Ruff/deptry backlog; `make lint-baseline-check` exits 0 (`lint 100/1422`, `format 8/94`). Tranche note: `docs/codex/tranches/2026-07-04-sharp-mlb-consensus-provider-promotion.md`. Next machine action: wrap raw NFL Pinnacle availability into strict exact Kalshi consensus when NFL markets are mappable, source legal soccer Asian sharp reference, and keep collecting labels until OOS/FDR can test the consensus lane.

- 2026-07-04 per-sport sharp consensus provider coverage: continued implementing Claude's sports advice by making sharp no-vig consensus coverage auditable per target sport instead of globally. Added per-sport coverage rows to `predmarket/sports_consensus_provider_policy.py`, added `--target-sports` and `make kalshi-sports-consensus-provider-audit`, and wired the provider audit into the sports consensus watch-once/evidence-cycle path. Latest provider audit exits 0 with `sports_consensus_provider_audit_ready_with_per_sport_gaps`: target sports `mlb,tennis,soccer,nfl,nba`; covered sports `tennis`; gap count `4`; MLB is `secondary_only`, soccer is `missing_asian_sharp_reference`, and NFL/NBA are `missing_strict_consensus`. Latest consensus falsification remains correctly blocked with `16` joined labels, `8` independent labels, `3` OOS labels, `0` tested hypotheses, and `0` FDR survivors. Paper/live remain blocked: `0` paper-usable rows, `$0` paper stake, `0` live-eligible rows. Verification: focused provider/evidence/collector tests `17 passed`, touched-file Ruff check/format pass, `make kalshi-sports-consensus-provider-audit`, `make kalshi-sports-evidence-cycle-report`, and `make kalshi-always-on-collector-once` exit 0. Tranche note: `docs/codex/tranches/2026-07-04-per-sport-sharp-consensus-provider-coverage.md`. Next machine action: upgrade real sharp reference coverage for MLB/soccer/NFL/NBA, especially soccer Asian sharp anchors, then keep collecting exact labels until OOS/FDR can test the consensus lane.

- 2026-07-04 always-on consensus collector target: continued implementing Claude's advice by wiring the default always-on collector directly into the sports sharp-consensus evidence loop. Added a `sports_consensus` collector target in `scripts/kalshi_always_on_collector.py` that runs `make kalshi-sports-consensus-observation-watch-once`; changed default collector targets to `sports_consensus,crypto`; left the older heavier `sports` paper burn-in target available but opt-in via `KALSHI_ALWAYS_ON_COLLECTOR_TARGETS=sports_consensus,sports,crypto` after a real run showed it can hit public endpoint `HTTP 429` bursts. Latest `make kalshi-always-on-collector-once` exits 0 with `kalshi_always_on_collector_ready`, targets `sports_consensus,crypto`, safe artifacts `2/2`, total due count `7745`, total label count `1234`, and 60-second cadence. The sports consensus target passed with artifact status `sports_consensus_observation_loop_label_rows_ready`: `104` total consensus observations, `26` new observations, `16` label rows, `16` new labels, `40` valid preflight candidates, and `28` due observation rows. Falsification remains correctly blocked: `sports_consensus_falsification_blocked_insufficient_labels`, `16` joined labels, `8` independent labels, `3` OOS labels, `0` tested hypotheses, `0` FDR survivors. Verification: collector tests `6 passed`, touched-file Ruff check/format pass, and real collector one-shot exits 0. Tranche note: `docs/codex/tranches/2026-07-04-always-on-consensus-collector-target.md`. Next machine action: keep running the collector until the consensus lane reaches at least `30` independent labels and `10` OOS labels, while expanding sharp provider coverage beyond ATP/secondary MLB.

- 2026-07-04 paper portfolio cap enforcement: continued implementing Claude's advice by turning paper portfolio cap diagnostics into an operational gate. Added hard cap enforcement in `predmarket/paper_decision_engine.py`, wired `scripts/kalshi_paper_decision_candidates.py` so the operational CLI/Make path enforces caps by default, added `KALSHI_PAPER_DECISION_MAX_CLUSTER_SHARE ?= 0.35`, and added a regression test proving a single-cluster paper candidate becomes a zero-stake blocked row with explicit `max_cluster_share` blocker text. Latest `make kalshi-paper-decision-candidates` exits 0 and changes the real paper artifact from cap-breached paper stake to `paper_decision_candidates_ready_all_rows_blocked`: `487` candidates, `0` paper-usable, `$0` total paper stake, pre-enforcement cap status `paper_portfolio_cap_breaches_present`, `4` cap-blocked candidates, final cap status `paper_portfolio_caps_observed`, and `0` final cap breaches. Verification: focused paper/autonomous + fee-aware + integration replay tests `84 passed` with pytest capture disabled (`-s`), touched-file Ruff check/format pass. Tranche note: `docs/codex/tranches/2026-07-04-paper-portfolio-cap-enforcement.md`. Next machine action: run `make kalshi-live-preflight` against the refreshed paper artifact, then continue the larger Claude objective by expanding timestamp-matched sharp consensus coverage and exact settlement labels.

- 2026-07-04 ATP donor strict consensus adapter: after Droid's fee/live substrate mission landed, moved the existing ATP/Wimbledon sharp-provider donor rows into the strict predmarket sports no-vig consensus chain without promoting EV, paper stake, or live eligibility. Added `predmarket/sports_consensus_atp_adapter.py`, `scripts/kalshi_sports_consensus_atp_donor_adapter.py`, `tests/test_kalshi_sports_consensus_atp_adapter.py`, and `make kalshi-sports-consensus-atp-donor-adapter`; consensus preflight and observation loop now use the derived combined Kalshi snapshot at `/home/mrwatson/manual_drops/predmarket/sports-consensus-kalshi-snapshot.json` so ATP exact tickers validate against donor Kalshi quote rows. Latest run: adapter `sports_consensus_atp_donor_adapter_ready`, `44` ATP strict rows, `14` exact ATP Kalshi tickers, `4` ATP providers (`pinnacle`, `betfair_exchange`, `matchbook`, `smarkets`), merged strict reference `108` rows, preflight `sports_consensus_preflight_ready_with_rejected_rows` with `14` valid ATP candidates and `32` stale MLB rows rejected by `timestamp_skew_exceeds_policy`, observation loop `sports_consensus_observation_loop_ready_waiting_settlement` with `14` new ATP observations archived, falsification still `sports_consensus_falsification_blocked_insufficient_labels`, and provider audit now `sports_consensus_provider_audit_ready_strict_anchor_present` with the remaining provider gap `soccer_asian_sharp_not_observed`. Verification: focused ATP/provider tests `11 passed`; touched-file Ruff check/format and py-compile clean; adapter, preflight, observation loop, falsification, and provider audit all exit 0. Tranche note: `docs/codex/tranches/2026-07-04-atp-donor-strict-consensus-adapter.md`. Next machine action: after ATP observed contracts settle, probe exact public Kalshi settlements and rerun sports consensus falsification; do not lower label/FDR thresholds.

- 2026-07-04 sports consensus observation archive: fulfilled root directive `NEXT_DIRECTIVE_2026-07-04_SPORTS_CONSENSUS_OBSERVATION_ARCHIVE.md`. Cleanup first: fixed `scripts/kalshi_sports_consensus_preflight.py` so temp output dirs no longer mutate global `latest-kalshi-sports-consensus-preflight.*` pointers, fixed the same temp-output leak in `scripts/kalshi_sports_consensus_falsification.py`, and added regression assertions. New work: added `scripts/kalshi_sports_consensus_observation_loop.py`, `tests/test_kalshi_sports_consensus_observation_loop.py`, `make kalshi-sports-consensus-observation-loop`, and `make kalshi-sports-consensus-observation-watch-once`. The loop archives valid timestamp-matched no-vig consensus candidates outside the repo under `/home/mrwatson/manual_drops/kalshi_sports_consensus_observations/`, attaches labels only from exact public Kalshi settlement payloads under `/home/mrwatson/manual_drops/kalshi_sports_consensus_labels/`, dedupes packet rows by stable `observation_id`, and preserves `research_only=true`, `usable=false`, `calibrated_probability=null`, `expected_value_per_contract=null`, and `execution_enabled=false`. `scripts/kalshi_sports_consensus_falsification.py` now dedupes packet rows before statistical counts, preventing stamped+latest packet copies from inflating label evidence. The sports evidence cycle now includes `latest-kalshi-sports-consensus-observation-loop.json` as a 23rd safe artifact and surfaces observation/label counts before falsification. Latest real run: `make kalshi-sports-consensus-refresh` restored real production consensus (`64` reference rows, `32` unique Kalshi tickers, `32` valid candidates); `make kalshi-sports-consensus-observation-loop` archived `32` new current MLB consensus observations; full `make kalshi-sports-evidence-cycle` exits 0 with `sports_evidence_cycle_ready_with_label_progress`, `23/23` safe artifacts, `64` total consensus observations, `32` new consensus observations, `0` consensus labels, falsification `sports_consensus_falsification_blocked_insufficient_labels`, `0` joined labels, `0` tested hypotheses, `0` FDR survivors, and live still `kalshi_live_blocked` with `0` live-eligible. Verification: focused sports consensus/preflight/falsification/cycle tests `26 passed`; touched-file Ruff clean; py-compile clean; `make kalshi-sports-consensus-refresh`, `make kalshi-sports-consensus-observation-loop`, `make kalshi-sports-consensus-falsification`, full `make kalshi-sports-evidence-cycle`, and `make kalshi-sports-evidence-cycle-report` exit 0; `make test-unit` `841 passed / 14 deselected`; `make test-integration` `14 passed`; `make quality` exits 0 with existing advisory Ruff/deptry backlog; `git diff --check` exits 0 with line-ending warnings only. Matching completion report: `COMPLETION_REPORT_2026-07-04_SPORTS_CONSENSUS_OBSERVATION_ARCHIVE.md`. Next machine action: after the current observed consensus contracts settle, run `make kalshi-sports-consensus-observation-watch-once` to probe exact public Kalshi ticker settlements, then rerun falsification; stop before EV/paper promotion unless OOS/FDR labels pass with no threshold lowering.

- 2026-07-04 sports consensus falsification ledger: promoted the working no-vig consensus surface from preflight inventory into a real falsification-gated sports signal ledger. Added `predmarket/sports_consensus_falsification.py`, `scripts/kalshi_sports_consensus_falsification.py`, `make kalshi-sports-consensus-falsification`, and a focused 9-test suite at `tests/test_kalshi_sports_consensus_falsification.py`. The ledger takes valid rows from `latest-kalshi-sports-consensus-preflight.json`, joins them to Kalshi mids and exact settlement labels by exact `contract_ticker`+`side`, collapses by exact contract_ticker, applies a chronological in-sample/OOS split (test_fraction=0.30, min_independent_labels=30, min_oos_labels=10, fdr_alpha=0.10), evaluates four pre-registered candidate rules (`kalshi_vs_consensus_favorite_underpriced`, `kalshi_vs_consensus_underdog_underpriced`, `kalshi_vs_consensus_fade_overpriced`, `sports_consensus_price_bucket_bias`) across the `0.005,0.01,0.015,0.02,0.025,0.03,0.04,0.05` threshold grid and the `[0.05,0.15),[0.15,0.30),[0.30,0.50),[0.50,0.70),[0.70,0.85),[0.85,0.95]` price buckets, and routes every testable hypothesis through Benjamini-Hochberg FDR. The artifact is research-only and emits one of `sports_consensus_falsification_ready_with_research_candidates`, `sports_consensus_falsification_ready_no_research_candidates`, `sports_consensus_falsification_blocked_insufficient_labels`, `sports_consensus_falsification_blocked_no_valid_consensus_rows`, or `sports_consensus_falsification_blocked_missing_inputs`; every output row remains `usable=false`, `research_only=true`, with no `calibrated_probability` or `expected_value_per_contract`. The CLI reads outside-repo archives at `/home/mrwatson/manual_drops/kalshi_sports_consensus_observations/` and `/home/mrwatson/manual_drops/kalshi_sports_consensus_labels/`. The target is wired into `make kalshi-sports-evidence-cycle` immediately after `kalshi-sports-consensus-preflight` and before EV ledger / paper sizing. `scripts/kalshi_sports_evidence_cycle_report.py` surfaces the falsification artifact as a 22nd safe artifact and exposes `sports_consensus_falsification_status`, joined label count, tested hypothesis count, FDR survivor count, and research candidate count. Latest `make kalshi-sports-consensus-refresh` exits 0: 40 reference rows, 20 unique Kalshi tickers, 2 distinct books, preflight `sports_consensus_preflight_ready` with 20 valid candidates. Latest `make kalshi-sports-consensus-falsification` exits 0 with `sports_consensus_falsification_blocked_insufficient_labels`: 0 joined labels (observation archive is still empty), 0 tested hypotheses, 0 FDR survivors, 0 research candidates — honest first-run state, no +EV claim. Latest full `make kalshi-sports-evidence-cycle` exits 0: `sports_evidence_cycle_ready_with_label_progress`, 22/22 safe artifacts, sports consensus falsification surfaced correctly, and live still `kalshi_live_blocked` with 0 live-eligible. Verification: focused sports consensus falsification + preflight + cycle tests 18 passed; `make test-unit` 833 passed / 14 deselected; `make test-integration` 14 passed; `make quality` exits 0 with advisory deptry backlog only; `make lint-baseline-check` OK (`lint 118/1422`, `format 93/94`); `git diff --check` clean of new whitespace errors. Tranche note at `docs/codex/tranches/2026-07-04-sports-consensus-falsification-ledger.md`. Next machine action: stand up a consensus observation accumulator that writes safe research-only packets per refresh and probes matching Kalshi tickers after settlement to populate the consensus label archive; re-run the falsification target once the joined label count reaches the minimums. Stop before any EV ledger promotion unless that gate is added with its own tests.

- 2026-07-04 sports consensus reference feed: fixed the missing feed behind `sports_consensus_preflight_blocked_missing_reference`. Added `predmarket/sports_consensus_reference_builder.py`, `scripts/kalshi_sports_consensus_reference_build.py`, `make kalshi-sports-consensus-reference-build`, and `make kalshi-sports-consensus-refresh`. The builder adapts the useful odds-surveillance-platform pattern without adding a donor runtime dependency: configured book set, source-age limit, raw provider payloads outside the repo, derived reference only, exact Kalshi ticker mapping, and no account/order/live path. Current operational default is MLB h2h game-winner consensus from `lowvig,betonlineag`; the derived reference is written to `/home/mrwatson/manual_drops/predmarket/sports-no-vig-consensus.json`, while raw current Odds API captures stay under `/home/mrwatson/manual_drops/odds_api/`. Also repaired `predmarket/sports_consensus.py` so preflight indexes the live universe scan's `candidates` key, not only older `all_scored`/`markets` shapes. Latest `make kalshi-sports-consensus-refresh` exits 0: reference build `sports_consensus_reference_built_with_warnings`, 44 reference rows, 22 unique Kalshi tickers, 2 distinct books, 3 skipped events, then consensus preflight `sports_consensus_preflight_ready` with 22 valid candidates and 0 blockers. Latest full `make kalshi-sports-evidence-cycle` exits 0: `sports_evidence_cycle_ready_with_label_progress`, 21/21 safe artifacts, 5 sports surfaces, 5,831 observations, 1,246 labels, 164 proxy labels, 22 valid sports consensus candidates surfaced, paper 445 candidates / 7 paper-usable rows / `$1,075.486942` paper stake, and live still `kalshi_live_blocked` with 445 decisions / 0 eligible. Verification: focused sports consensus tests 10 passed; touched-file Ruff check/format pass. Tranche note at `docs/codex/tranches/2026-07-04-sports-consensus-reference-feed.md`. Next machine action: promote the no-vig consensus surface from preflight inventory into its own falsification ledger: track exact-ticker Kalshi-vs-consensus divergence, settle outcomes, test OOS/FDR by sport/market/price bucket, and only then allow paper sizing.

- 2026-07-04 sports no-vig consensus doctrine/preflight: followed the Claude/Fable sports advice as a repo-level policy correction. For sports, the sharp timestamp-matched multi-book no-vig consensus line is now the primary probability-source intake; internal Elo/projection/simulation systems may remain metadata or separately falsified hypotheses, but cannot directly supply sports tradable probabilities. Added `predmarket/sports_consensus.py`, `scripts/kalshi_sports_consensus_preflight.py`, and `make kalshi-sports-consensus-preflight`; the preflight requires exact Kalshi ticker mapping, two-sided no-vig odds, at least two distinct books, and timestamp match to the Kalshi observation, while rejecting projection/model/Elo/simulation rows as primary sports probabilities. Wired the artifact into `scripts/kalshi_sports_evidence_cycle_report.py` as a fifth sports surface (`sports_no_vig_consensus`) and into `make kalshi-sports-evidence-cycle`; updated the Type 2 manual-drop contract to state that single-book Type 2 is legacy/manual research review, not the sports model. Hardened `scripts/kalshi_contract_ev_ledger.py` so old sports strength-model CCD rows are audit rows only (`blocked_projection_model_not_consensus`) even if capacity/correlation/decay fields pass; microstructure, decay, price-bucket-bias, and future no-vig consensus families remain eligible through their own gates. Latest real run: `make kalshi-sports-consensus-preflight` exits 0 with `sports_consensus_preflight_blocked_missing_reference`, 0 valid consensus rows, and a precise missing-feed blocker for `/home/mrwatson/manual_drops/predmarket/sports-no-vig-consensus.json`. Full `make kalshi-sports-evidence-cycle` exits 0: `sports_evidence_cycle_ready_with_label_progress`, 21/21 safe artifacts, 5 sports surfaces, 5,567 observations, 1,210 labels, 164 proxy labels, 0 consensus-valid candidates, EV ledger 383 rows / 23 usable rows, 443 paper candidates, 11 paper-usable rows, `$1,815.60097` paper stake, `paper_portfolio_caps_observed`, and live still `kalshi_live_blocked` with 443 decisions / 0 eligible. Guardrails unchanged: no live credentials, no account/order path, no threshold lowering, no single-book sportsbook shortcut, no projection-model promotion, and no manual approval queue. Verification: focused ledger/consensus/cycle tests 11 passed; `make test-unit` 820 passed / 14 deselected; `make test-integration` 14 passed; `make quality` exits 0; `make lint-baseline-check` passes (`lint 118/1422`, `format 93/94`); `make modularize` 2 kept / 0 broken; `make kalshi-sports-consensus-preflight` and full `make kalshi-sports-evidence-cycle` exit 0. Tranche note at `docs/codex/tranches/2026-07-04-sports-no-vig-consensus-doctrine-preflight.md`. Next machine action: build/ingest a timestamp-matched multi-book no-vig consensus feed at `/home/mrwatson/manual_drops/predmarket/sports-no-vig-consensus.json`, rerun consensus preflight and the sports cycle, then route passing rows through OOS/FDR/cost/capacity/cluster/decay; keep live blocked until paper P&L, calibration, retirement, and live-risk preflight justify an explicit live tranche.

- 2026-07-04 paper portfolio risk audit: set and pursued a `/goal` aligned to the north star after the always-on collector landed: make current paper-usable sports candidates auditable as a portfolio before any live work. Added `predmarket/paper_portfolio_risk.py` and wired portfolio diagnostics into `predmarket/paper_decision_engine.py`, `scripts/kalshi_paper_decision_candidates.py`, `scripts/kalshi_paper_settlement_reconcile.py`, `scripts/kalshi_sports_paper_burn_in_cycle.py`, and `scripts/kalshi_sports_evidence_cycle_report.py`. The artifacts now emit total paper stake, settled/unresolved/due stake, largest family/signal/cluster/contract exposure, contract/cluster cap status, and cap-breach rows. Latest refreshed real run (`make kalshi-paper-decision-candidates`, then `make kalshi-sports-paper-burn-in-cycle KALSHI_SPORTS_PAPER_BURN_IN_FETCH=1`) reports: paper decisions `paper_decision_candidates_ready_with_paper_sized_rows`, 14 paper-usable rows, `$890.54206` total paper stake, `paper_portfolio_caps_observed`, 0 cap breaches, largest cluster `mlb|KXMLBGAME-26JUL041335MINNYY|2026-07-04T20:35Z` with `$200` stake / `0.2245823179` share, and largest signal `microstructure_informed_flow|flow_depth_imbalance_settlement_directional|depth_imbalance_yes_abs_gt_0_25|predmarket-alpha` with `$890.54206` stake / `1.0` share. Paper settlement remains `paper_settlement_reconciliation_waiting_for_close`: 0 settled rows, 14 unresolved rows, 0 due unresolved rows, next paper close `2026-07-04T20:00:00Z`. Sports evidence remains `sports_evidence_cycle_ready_with_label_progress`; sports labels advanced to `sports_label_accumulation_oos_fdr_paper_candidates_ready` with 327 exact labels, 80 independent labels, and total deficit 10. Live remains fully blocked: `kalshi_live_blocked`, 0 live-eligible, $0 live stake. Guardrails: exact Kalshi settlement labels only, no threshold lowering, no manual approval queue, no account/order/live execution path. Verification: touched-file Ruff clean, focused paper/settlement/burn-in/evidence tests 34 passed, `make test-unit` 814 passed / 14 deselected, `make test-integration` 14 passed, `make quality` exits 0 with expected advisory Ruff/deptry backlog, and hard ratchets pass (`lint-baseline-check`, `tech-debt-check`, `file-sizes-check`, `modularize`). To restore the format ratchet without regenerating baseline, `predmarket/aggregator.py` was Ruff-formatted only; no logic change. Tranche note at `docs/codex/tranches/2026-07-04-paper-portfolio-risk-audit.md`. Next machine action: after `2026-07-04T20:00:00Z`, run `make kalshi-sports-paper-burn-in-cycle KALSHI_SPORTS_PAPER_BURN_IN_FETCH=1`; if rows settle, judge realized paper P&L/calibration/retirement before any live promotion. Separately, macro routing now points to crypto proxy cluster-control-ready paper overlay as the next non-sports lane.

- 2026-07-04 always-on collector control plane: acted on the Claude/Fable advice that label velocity is the binding constraint by adding `scripts/kalshi_always_on_collector.py`, `make kalshi-always-on-collector-once`, and `make kalshi-always-on-collector`. The collector is a research-only daemon/control plane over existing safe one-shot collectors: default targets are sports (`kalshi-sports-paper-burn-in-cycle` with exact due settlement probing armed) and crypto (`kalshi-crypto-proxy-observation-watch-once` with public settlement capture/probing armed). It writes `latest-kalshi-always-on-collector.{json,md,csv}` plus a systemd service/timer example, records per-target command status, artifact safety, due counts, label counts, next probe time, and chooses the next interval mechanically: 60 seconds when due settlement rows exist, 60 seconds near close/probe windows, otherwise 300 seconds. Latest real one-shot run: `make kalshi-always-on-collector-once` exited 0 with `kalshi_always_on_collector_ready`, 2/2 targets passed, 2/2 safe artifacts, total label count 1,545, total due count 6,231, next cadence `60` seconds because `crypto` has due settlement rows, sports artifact `sports_paper_burn_in_waiting_for_next_close` with 327 exact labels and next close `2026-07-04T20:00:00Z`, crypto artifact `crypto_proxy_observation_loop_label_rows_ready` with 1,218 labels and next public probe `2026-07-04T18:22:14Z`. Guardrails: `research_only=true`, `execution_enabled=false`, `market_execution=false`, `account_or_order_paths=false`, no live credentials, no orders, no non-Kalshi labels, no threshold lowering. Verification: focused collector tests 5 passed, touched-file Ruff clean, py-compile clean, `make kalshi-always-on-collector-once` exits 0, `make test-unit` 814 passed / 14 deselected, `make test-integration` 14 passed, and `make quality` exits 0 with expected advisory Ruff/deptry backlog. Next machine action: run `make kalshi-always-on-collector` under a long-lived supervisor when ready; until then use `make kalshi-always-on-collector-once` to keep feeding labels and paper settlement.

- 2026-07-04 sports paper burn-in cycle: added a self-advancing paper settlement and sports exact-label harvest wrapper at `scripts/kalshi_sports_paper_burn_in_cycle.py` plus `make kalshi-sports-paper-burn-in-cycle`. The target first runs the existing `kalshi-sports-label-accumulation-cycle` so MLB, World Cup/FIFA, and ATP exact public settlement probes refresh before the burn-in audit. It then optionally probes exact public Kalshi market payloads only for due paper-usable tickers when `KALSHI_SPORTS_PAPER_BURN_IN_FETCH=1`, refreshes `latest-paper-settlement-reconciliation`, feeds that enriched paper artifact into `latest-signal-decay-retirement-ledger`, refreshes the sports evidence and label reports, and writes `latest-kalshi-sports-paper-burn-in-cycle.{json,md,csv}`. Latest real run: `make kalshi-sports-paper-burn-in-cycle KALSHI_SPORTS_PAPER_BURN_IN_FETCH=1` exited 0 and produced `sports_paper_burn_in_waiting_for_next_close`: 14 paper-usable rows, 0 due-before-fetch, 0 due-after-fetch, 0 settled paper rows, next paper close `2026-07-04T20:00:00Z`, live eligible 0, retirement `signal_decay_retirement_ledger_ready` with 10 active / 0 retired, and sports evidence still `sports_evidence_cycle_ready_with_label_progress`. Label harvest state after the run: 327 exact labels, 80 independent labels, total deficit 10; MLB is 28/30 independent labels with deficit 2, World Cup/FIFA is OOS/FDR candidate-ready with 50 independent labels and deficit 0, ATP is 2/10 with deficit 8. The loop preserves `research_only=true`, `execution_enabled=false`, `market_execution=false`, `account_or_order_paths=false`, no threshold lowering, and no duplicate-contract shortcut. Verification: focused burn-in/label/settlement tests 36 passed, touched-file Ruff clean, py-compile clean, `make kalshi-sports-paper-burn-in-cycle KALSHI_SPORTS_PAPER_BURN_IN_FETCH=1` exits 0, `make test-unit` 809 passed / 14 deselected, `make test-integration` 14 passed, and `make quality` exits 0 with expected advisory Ruff/deptry backlog. Next machine action at or after `2026-07-04T20:00:00Z`: rerun `make kalshi-sports-paper-burn-in-cycle KALSHI_SPORTS_PAPER_BURN_IN_FETCH=1`; if rows settle, audit realized paper P&L/retirement before any live work; if still waiting, keep the exact public settlement loop running.

- 2026-07-04 paper settlement reconciliation lifecycle: added the missing forward-paper burn-in loop for the near-resolution informed-flow EV rows. New `scripts/kalshi_paper_settlement_reconcile.py` and `make kalshi-paper-settlement-reconcile` freeze every row from `latest-paper-decision-candidates`, join exact public Kalshi market payloads by contract ticker, compute selected-side paper contracts/P&L/ROI when settled, and emit `latest-paper-settlement-reconciliation.{json,md,csv}` while preserving `research_only=true`, `execution_enabled=false`, `market_execution=false`, and `account_or_order_paths=false`. The sports evidence cycle now runs paper settlement reconciliation after paper sizing, feeds the enriched paper artifact into signal decay retirement, and reports paper settlement status/P&L. Fixed `predmarket/signal_decay_retirement.py` calibration handling so selected-side probabilities are compared to selected-side win/loss outcomes; this prevents correct NO-side paper decisions from being retired by comparing P(NO) to a YES=0 settlement. Latest full-cycle real run: `paper_settlement_reconciliation_waiting_for_close` with 470 candidates, 10 paper-usable rows, `$717.725981` frozen paper stake, 0 settled rows, 10 unresolved rows, 0 due unresolved rows, and next close at `2026-07-04T18:05:00Z`. Retirement refreshed from the enriched artifact and remains `signal_decay_retirement_ledger_ready` with 10 active signals and 0 retired. Sports evidence cycle remains `sports_evidence_cycle_ready_with_label_progress`, now with 20/20 safe artifacts and live still `0` eligible. Verification: focused reconciliation/retirement/report/paper tests 56 passed; `make test-unit` 805 passed / 14 deselected; `make test-integration` 14 passed; `make quality` exits 0 with expected advisory Ruff/deptry backlog; touched-file Ruff clean; py-compile clean; `make kalshi-sports-evidence-cycle` exits 0. Next machine action after `2026-07-04T18:05Z`: run `make kalshi-paper-settlement-reconcile KALSHI_PAPER_SETTLEMENT_FETCH=1`, then rerun retirement and sports evidence cycle report; stop before live execution.

- 2026-07-04 near-resolution flow EV ledger and paper promotion: completed the sports mission by promoting the FDR-surviving `flow_depth_imbalance_settlement_directional` microstructure signal through official-terms capture, EV ledger promotion, and paper sizing while keeping live execution blocked. Added `scripts/kalshi_near_resolution_flow_terms_capture.py` and `make kalshi-near-resolution-flow-terms-capture`; it captures public Kalshi market-detail terms for pass-status flow capacity rows, writes raw official terms outside the repo under `/home/mrwatson/manual_drops/kalshi/kalshi_scored_sports_flow_terms_*.json`, and emits auditable latest artifacts at `docs/codex/macro/latest-kalshi-near-resolution-flow-terms-capture.{json,md,csv}`. `scripts/kalshi_contract_ev_ledger.py` now consumes the ready flow replay artifact plus official terms to create usable EV rows for `microstructure_informed_flow`, with conservative calibrated probability `0.7592066891`, FDR q-value `3.08e-8`, 198 independent labels, 4/4 passing decay buckets, 10 controlled clusters, and controlled positive-depth cost `$43,459.7562938467`. `predmarket/paper_decision_engine.py` now skips duplicate flow gate-evidence blockers once the replay status is ready for EV-ledger promotion, so paper sizing is sourced from the EV ledger only. `scripts/kalshi_sports_evidence_cycle_report.py` now treats the official-terms capture as a required safe artifact and reports `flow_terms_*` counts. The latest full `make kalshi-sports-evidence-cycle` run exits 0 and reports: terms capture ready with 18/18 current targets and 18 official-rule markets; EV ledger `kalshi_ev_ledger_ready_with_usable_contract_edges` with 392 rows, 30 usable rows, and 18/18 usable flow rows; paper candidates `paper_decision_candidates_ready_with_paper_sized_rows` with 460 candidates, 18 paper-usable rows, and `$2,006.686796` total paper stake; retirement ledger ready with 10 active signals and 0 retired; live preflight `kalshi_live_blocked` with 460 decisions, 0 live-eligible, and `$0` live stake. World Cup also advanced to one research candidate with 50 independent labels, but remains upstream research evidence until downstream gates promote it. Verification: focused tests 5 passed after the terms/report fixture fix, `make test-unit` 800 passed / 14 deselected, `make test-integration` 14 passed, `make quality` exits 0 with expected advisory Ruff/deptry backlog, touched-file Ruff clean, py-compile clean, and full sports evidence cycle exits 0. Tranche note at `docs/codex/tranches/2026-07-04-near-resolution-flow-ev-ledger-paper-promotion.md`. Next machine action: fix portfolio-level paper risk reporting/capping so clustered paper stake cannot look like unconstrained bankroll deployment, then build current-market-snapshot/external-preflight joins needed for live preflight without arming execution.

- 2026-07-04 near-resolution flow replay gates: made the highest-value next move after the FDR survivor by building a downstream replay/gate artifact for `flow_depth_imbalance_settlement_directional`. Added `scripts/kalshi_near_resolution_flow_replay_gates.py` plus `make kalshi-near-resolution-flow-replay-gates`; the target consumes the flow evidence artifact and sports microstructure rows, computes Wilson-lower conservative selected-side probability from the OOS survivor (`0.7931031818` from 30/33 OOS correct), replays exact-contract historical rows through Kalshi all-in cost normalization, selects fresh current candidates, measures top-of-book depth/capacity, applies mechanical cluster control, checks decay buckets, and emits `paper_decision_blocker_rows`. Hardened `scripts/kalshi_near_resolution_informed_flow_evidence_gate.py` so flow rows now carry event/series/surface, settlement time, quote, and depth fields needed for replay; `scripts/kalshi_paper_decision_candidates.py` now ingests the flow replay gate evidence by default; `scripts/kalshi_sports_evidence_cycle_report.py` now includes the flow replay artifact as the sports microstructure replay status. Latest real run: `near_resolution_flow_replay_gates_blocked_decay_survival`. Passing gates: research candidate present, conservative probability ready, 31/107 replay rows costed, 4 positive cost-adjusted replay rows, 22 fresh current candidates, 154,715.45 positive-depth contracts, $54,451.564096 positive-depth cost, and cluster control reduced raw largest-cluster share `0.7756268879` to controlled largest share `0.35` across 6 controlled clusters with $18,796.1029138467 controlled positive-depth cost. Blocking gate: decay survival, because evidence has 107 labels and recent accuracy `1.0`, but only 2 close-time buckets versus the required 3. Paper now includes flow blocker rows: latest paper candidates have 484 rows, 82 gate-evidence rows, 0 usable, $0 stake; flow rows are blocked only by `decay_survival not passing`. Live remains `kalshi_live_blocked`: 484 decisions, 0 eligible, $0 live stake. Verification: focused flow/cycle/microstructure/paper tests 23 passed; new flow tests 3 passed after cluster-control patch; touched-file Ruff clean; py-compile clean; `make kalshi-sports-label-accumulation-cycle`, `make kalshi-near-resolution-flow-replay-gates`, `make kalshi-paper-decision-candidates`, `make kalshi-live-preflight`, and `make kalshi-sports-evidence-cycle-report` exit 0. Tranche note at `docs/codex/tranches/2026-07-04-near-resolution-flow-replay-gates.md`. Next machine action: accumulate one more independent close-time decay bucket for this same flow rule, then rerun the gate; stop before lowering the 3-bucket/100-label decay thresholds.

- 2026-07-04 near-resolution informed-flow falsification: acted on the Claude advice that the compute-bound near-resolution flow family should be attacked immediately while directional sports labels keep accumulating on the calendar. `scripts/kalshi_near_resolution_informed_flow_evidence_gate.py` now tests four pre-registered microstructure candidates (`flow_quote_momentum_forward_quote`, `flow_depth_imbalance_forward_quote`, `flow_depth_delta_forward_quote`, and `flow_depth_imbalance_settlement_directional`) with exact contract-ticker independence, chronological OOS splitting, exact binomial scoring, and BH-FDR q-values. Added a synthetic positive-path regression test in `tests/test_kalshi_sports_microstructure_evidence.py` proving the gate can find a pre-registered survivor while still emitting `usable_row_count=0`. After refreshing with `make kalshi-sports-label-accumulation-cycle`, latest status is `near_resolution_informed_flow_research_candidates_ready`: 605 flow rows, 359 distinct contracts, 131 repeated-snapshot contracts, 107 settled contract labels, 56 forward-quote labels, 4 pre-registered candidates, 1 testable candidate, and 1 FDR survivor. The survivor is `flow_depth_imbalance_settlement_directional`: 107 independent contracts, 33 OOS labels, 30 OOS correct, OOS accuracy `0.9090909091`, p-value `7.006e-7`, q-value `7.006e-7`. The three forward-quote candidates remain blocked on insufficient independent labels. This is a research candidate, not a tradable signal: paper decisions remain `paper_decision_candidates_ready_all_rows_blocked` with 492 candidates, 0 usable, $0 stake; live preflight remains `kalshi_live_blocked` with 492 decisions, 0 eligible, $0 stake. No calibrated probability, EV row, paper stake, live eligibility, account path, order path, or execution side effect was introduced. Verification: flow script py-compile passed; focused sports microstructure tests 5 passed; touched-file Ruff passed; `make kalshi-sports-label-accumulation-cycle` exits 0; `make test-unit` 699 passed / 14 deselected; `make test-integration` 14 passed; `make lint-baseline-check`, `make tech-debt-check`, `make file-sizes-check`, `make modularize`, and `make quality` exit 0 with expected advisory deptry/Ruff backlog. Tranche note at `docs/codex/tranches/2026-07-04-near-resolution-informed-flow-falsification.md`. Next machine action: build the all-in cost, capacity/depth, correlation-cluster, and decay-survival replay for `flow_depth_imbalance_settlement_directional`; stop before paper stake unless every downstream gate passes.

- 2026-07-04 sports exact-label accumulation cycle: acted on the next bottleneck by turning "keep accumulating labels, then let OOS/FDR decide" into a first-class command and running it. Fixed stale ATP intake by making `scripts/kalshi_atp_proxy_observation_loop.py` and `KALSHI_ATP_PROXY_MATCH_SNAPSHOT` select the latest `/home/mrwatson/projects/atp-oracle/data/kalshi/matches-*.json` file, so the current July 4 Wimbledon snapshot enters the loop instead of the older July 3 file. Added `scripts/kalshi_sports_label_accumulation_cycle.py` plus `make kalshi-sports-label-accumulation-cycle`; the target runs the full sports evidence cycle, probes exact public Kalshi settlement labels, reruns family falsification/evidence gates, paper decisions, live preflight, and then writes an exact-label readiness report at `docs/codex/macro/latest-kalshi-sports-label-accumulation-cycle.{json,md,csv}`. Latest status is `sports_label_accumulation_waiting_more_exact_labels`: 8/8 safe artifacts, 744 directional observations, 58 exact public Kalshi labels, but only 14 independent labels counted for OOS/FDR. Deficits: MLB has 52 exact label rows but only 8 independent labels, needs 22 more; World Cup/FIFA has 6 independent exact labels, needs 24 more; ATP/Wimbledon has 0 labels, needs 10, with next public probe scheduled for `2026-07-04T06:00:00Z`. The nondirectional microstructure family improved materially: 485 historical observations, 249 distinct contracts, 306 settlement-label rows across 104 settled contracts, 236 forward quote pairs, and near-resolution informed flow advanced to `near_resolution_informed_flow_blocked_falsification_not_ready` with 104 settled contracts and 48 forward quote labels. Paper and live remain correctly blocked: 496 paper candidates, 0 paper usable, 496 live decisions, 0 live eligible, $0 stake. No thresholds were lowered, duplicate contract labels were not counted as independent evidence, quote-proxy labels were not mixed into exact settlement labels, and no account/order path was touched. Verification: `make kalshi-sports-label-accumulation-cycle` exits 0; focused tests 12 passed; `make test-unit` 698 passed / 14 deselected; `make test-integration` 14 passed; `make lint-baseline-check` OK (`lint 1407/1422`, `format 90/94`); `make tech-debt-check`, `make file-sizes-check`, `make modularize`, and `make quality` exit 0 with expected advisory deptry backlog. Tranche note at `docs/codex/tranches/2026-07-04-sports-exact-label-accumulation-cycle.md`.

- 2026-07-04 sports evidence velocity mission: ran a multi-hour Codex goal with read-only subagent audits for settlement labels, donor/model promotion, and paper/live eligibility, then implemented the highest-leverage sports bottleneck: converting current sports inventory into repeatable evidence velocity instead of one-off soft-watch rows. `scripts/kalshi_sports_microstructure_observation_loop.py` now consumes historical public orderbook observations, detects repeated snapshots/forward quote pairs, probes exact public Kalshi settlement payloads for due observed tickers, writes safe label packets outside the repo, and reports historical breadth/proxy-label readiness. `scripts/kalshi_near_resolution_informed_flow_evidence_gate.py` now consumes the external microstructure history plus settlement label packets and emits forward-quote labels while still blocking on missing settled contract labels. `scripts/kalshi_passive_liquidity_provision_evidence_gate.py` now builds virtual order sequences from repeated snapshots and emits counterfactual touch/adverse-selection proxy labels while still blocking without real fill labels. Added `scripts/kalshi_sports_evidence_cycle_report.py` plus `make kalshi-sports-evidence-cycle-report` and `make kalshi-sports-evidence-cycle`, a single safe cycle that refreshes universe, MLB, ATP, World Cup, microstructure, non-directional evidence, EV ledger, paper decisions, live preflight, and a consolidated cycle report. Latest refreshed cycle is `sports_evidence_cycle_ready_with_label_progress`: 16/16 safe artifacts, 7,329 sports stack candidates, 945 total observations, 43 exact/proxy labels, 21 due rows, 90 sports paper-gate blocker rows, 506 paper candidates, 506 blocked, 0 paper usable, 506 live decisions, 0 live eligible. Family specifics: MLB has 122 observations, 58 distinct contracts, 4 exact public settlement labels, but only 2 independent model labels; World Cup/FIFA has 362 observations, 260 distinct contracts, 9 exact labels, but only 6 independent model labels; ATP/Wimbledon has 96 observations and 0 labels pending settlement; microstructure has 365 historical observations, 151 distinct contracts, 110 repeated-snapshot contracts, and 214 forward quote pairs; informed-flow has 30 forward quote labels but 0 settled contract labels; passive-liquidity has 218 virtual orders and 10 counterfactual fill/adverse-selection proxy labels but 0 real fill labels. Guardrails unchanged: no threshold lowering, no sportsbook-as-label shortcut, no discretionary approval, no donor runtime dependency, no account/order side effects from evidence jobs, paper stake remains $0, and live eligibility remains fully blocked. Verification: `make kalshi-sports-evidence-cycle` exits 0; focused sports evidence tests 12 passed; `make test-unit` 694 passed / 14 deselected; `make test-integration` 14 passed; touched-file Ruff clean; hard ratchets green (`lint 1407/1422`, `format 89/94`, `tech-debt 22/22`, no new oversized files, import boundaries kept, `make quality` exits 0 with expected advisory deptry backlog). Tranche note at `docs/codex/tranches/2026-07-04-sports-evidence-velocity-cycle.md`.

- 2026-07-04 sports paper-blocker bridge + microstructure evidence substrate: pushed the sports stack one step closer to the north star by making current World Cup/FIFA, MLB, and ATP markets enter the shared paper/live gate chain as exact-contract audited blockers instead of remaining soft-watch inventory. `scripts/kalshi_sports_stack_sequencing.py` now emits 90 `paper_decision_blocker_rows` (30 World Cup/FIFA, 30 MLB, 30 ATP), each with exact ticker, side, family/model/signal/cluster identity, current market probability where available, `usable=false`, `execution_enabled=false`, and explicit blockers for missing labels/falsification/replay/CCD/cluster/EV-ledger promotion. `predmarket/paper_decision_engine.py` and `scripts/kalshi_paper_decision_candidates.py` now ingest those gate-evidence blocker rows by default, so the latest paper artifact has 478 candidates, 388 EV-ledger rows, 90 gate-evidence rows, 478 blocked, 0 paper-usable, and $0 stake. `make kalshi-live-preflight` mirrors that boundary with 478 live decisions, 0 live-eligible, and $0 live stake. Added a strict positive path in `scripts/kalshi_contract_ev_ledger.py`: future sports CCD rows can become usable EV-ledger rows only when sports CCD and correlation-cluster statuses are both ready, controlled depth is positive, verified terms exist, and all capacity/correlation/decay gates pass. Built the medium-term non-directional sports substrate as separate research families: `scripts/kalshi_sports_microstructure_observation_loop.py` captures public near-resolution sports orderbook observations, `scripts/kalshi_near_resolution_informed_flow_evidence_gate.py` emits flow evidence rows and currently blocks on missing settled labels, and `scripts/kalshi_passive_liquidity_provision_evidence_gate.py` emits counterfactual maker rows and blocks as proxy-only without real fill labels. New Make targets include `kalshi-sports-microstructure-observation-loop`, `kalshi-sports-microstructure-observation-watch-once`, `kalshi-near-resolution-informed-flow-evidence-gate`, `kalshi-passive-liquidity-provision-evidence-gate`, and `kalshi-sports-nondirectional-evidence-watch-once`. Latest non-directional statuses: `sports_microstructure_observation_loop_ready`, `near_resolution_informed_flow_blocked_missing_settled_labels` with 120 flow rows, and `passive_liquidity_provision_blocked_proxy_only_no_real_fill_labels` with 94 virtual order rows. Raw public orderbooks and observation packets are kept outside the repo under `/home/mrwatson/manual_drops/kalshi_sports_microstructure_orderbooks/` and `/home/mrwatson/manual_drops/kalshi_sports_microstructure_observations/`. Guardrails unchanged: no sportsbook shortcut, no discretionary candidate approval, no threshold lowering, no donor runtime dependency, no account/order path from research artifacts, and all current sports promotion rows remain blocked until OOS/FDR, cost, capacity, cluster, decay, and live-risk gates pass. Verification: focused tranche tests 9 passed; `make test-unit` 690 passed / 14 deselected; `make test-integration` 14 passed; hard ratchets green (`lint 1407/1422`, `format 88/94`, `tech-debt 22/22`, no new oversized files, import boundaries kept); `make quality` exits 0 with expected advisory Ruff/deptry backlog. Tranche note at `docs/codex/tranches/2026-07-04-sports-paper-blockers-microstructure-evidence.md`.

- 2026-07-04 sports stack sequencing + current-depth cap_i preflight + medium-term family instrumentation: acted on the sequencing memo by separating near-term sports surfaces instead of treating "sports props" as one bucket. Added `scripts/kalshi_ghost_listing_depth_diagnostic.py` and `make kalshi-ghost-listing-depth-diagnostic` to probe current public orderbooks before any `cap_i` lock; latest status is `ghost_listing_depth_diagnostic_current_depth_ready` with 120/120 orderbooks captured, positive-depth fraction `1.0`, ghost-listing fraction `0.0`, and `cap_i_lock_allowed=true`. Added `scripts/kalshi_sports_stack_sequencing.py` and `make kalshi-sports-stack-sequencing`; latest status is `sports_stack_sequencing_ready_current_depth_passed`, ranking `world_cup_soccer -> mlb -> atp -> nfl -> nba`, with 3,109 near-term sports candidates (78 World Cup/FIFA, 2,935 MLB, 96 ATP), NBA explicitly offseason/deprioritized, and adaptation constrained to the output/gate layer, not shared feature reuse. Refreshed World Cup observation/falsification artifacts: 193 total World Cup observations, 0 labels, still correctly `world_cup_proxy_observation_loop_ready_waiting_settlement` / `world_cup_proxy_feature_model_falsification_blocked_missing_labels`. Extended `scripts/kalshi_hypothesis_registry.py` with `near_resolution_informed_flow` and `passive_liquidity_provision` as separate falsification-gated families with their own acceptance metrics (`pre_close_flow_lead_lag_survival`, `maker_fill_net_ev_after_adverse_selection`), not the directional-signal bar. Wired the new sequencing/depth artifacts into `scripts/kalshi_signal_factory_status.py` and `scripts/codex_macro_router.py`; latest factory status and macro route now point to `signal_factory_sports_stack_sequencing_ready_current_depth_passed`, and the recommended tranche is World Cup/FIFA first, MLB second, ATP third while consuming current depth for cap_i inputs. Guardrails unchanged: all artifacts remain research-only, `usable=false`, no stake/order/account execution path. Verification: targeted router/status/sequencing/depth/registry tests 102 passed; `make test-unit` 686 passed / 14 deselected; `make test-integration` 14 passed; hard ratchets green (`lint 1407/1422`, `format 88/94`, `tech-debt 22/22`, no new oversized files, import boundaries kept); `make quality` exits 0 with expected advisory Ruff/deptry debt. Tranche note at `docs/codex/tranches/2026-07-04-sports-stack-sequencing-ghost-depth-flow-liquidity.md`.

- 2026-07-03 World Cup/FIFA evidence loop: moved the current soccer/World Cup soft-watch bottleneck from passive inventory into a real research evidence lane. Added `scripts/kalshi_world_cup_proxy_observation_loop.py`, which filters universe-scan `other_sports` rows to the explicit World Cup/FIFA soccer series allowlist, archives exact Kalshi ticker observations with quote-time market-structure features, writes observation packets outside the repo under `/home/mrwatson/manual_drops/kalshi_world_cup_proxy_observations/`, probes exact observed tickers after settlement, and labels only from public Kalshi settlement payloads. Added `scripts/kalshi_world_cup_proxy_feature_model_falsification.py`, which preserves World Cup proxy fields and runs two pre-registered market-structure rules (`world_cup_market_consensus_directional_accuracy`, `world_cup_longshot_fade_directional_accuracy`) through the existing chronological OOS + binomial + BH-FDR falsification spine. New Make targets: `kalshi-world-cup-proxy-observation-loop`, `kalshi-world-cup-proxy-feature-model-falsification`, and `kalshi-world-cup-proxy-observation-watch-once`. Latest public universe scan has 6,587 candidates, including 133 `other_sports`; the World Cup loop archived 115 distinct contracts across `KXWC1HTOTAL`, `KXWC2HTOTAL`, `KXWCBTTS`, `KXWCGAME`, and `KXWCTCORNERS`. Latest status is honest and blocked: `world_cup_proxy_observation_loop_ready_waiting_settlement`, 115 observations, 0 labels; falsification is `world_cup_proxy_feature_model_falsification_blocked_missing_labels`, 0 independent labels, 0 research candidates. Every row remains `usable=false`, `calibrated_probability=null`, `expected_value_per_contract=null`; no soccer handicap model, sportsbook probability, paper stake, live order, or account path was introduced. Verification: focused World Cup + universe tests 24 passed; `make kalshi-world-cup-proxy-observation-watch-once`, `make kalshi-world-cup-proxy-observation-loop`, and `make kalshi-world-cup-proxy-feature-model-falsification` exit 0; latest World Cup artifacts are valid JSON; `make test-unit` 677 passed / 14 deselected; `make test-integration` 14 passed; hard ratchets green (`lint 1408/1422`, `format 90/94`, `tech-debt 22/22`, no new oversized files, import boundaries kept). Tranche note at `docs/codex/tranches/2026-07-03-world-cup-proxy-evidence-loop.md`.

- 2026-07-03 maker-first Kalshi live execution policy + World Cup sports discovery: implemented the market-structure advice that taker fees are a real edge hurdle and passive liquidity should be the default. `kalshi_live.execution_strategy` now defaults to `maker_first` with env/config overrides for `maker_first`, `taker_cross`, and `taker_if_decay_justifies`; passive orders use post-only `good_till_canceled` V2 order payloads with a bounded expiration and configurable price improvement. Taker IOC is explicit-only, carries `post_only=false`, and `taker_if_decay_justifies` blocks unless a measured edge-decay rate clears the maker/taker fee-savings hurdle. The V2 adapter also now converts NO outcome prices to Kalshi's YES-book ask price (`1 - no_price`) before submission. Live decisions, intents, order records, markdown, CSV, and preflight summaries expose modeled limit price, execution strategy, fee mode, maker/taker fee estimates, fee savings, time-in-force, post-only, and expiration. Also expanded focused sports universe discovery to current World Cup/FIFA soccer series (`KXWCGAME`, spread/total/BTTS/half/team-prop series, and `KXFIFA*` game lines) as `other_sports` soft-watch rows. Latest public universe scan: 5,108 candidates, 912 model-routed (840 MLB, 72 ATP), 4,196 soft-watch, including 81 `other_sports` rows with live World Cup and cricket inventory. Refreshed EV/paper/live artifacts preserve the safety boundary: EV ledger has 388 rows and 12 upstream usable rows, but `paper_decision_candidates_ready_all_rows_blocked` has 388/388 blocked with $0 paper stake, and live preflight has 388 decisions, 0 live-eligible, $0 live stake, 388 maker-first/post-only decisions. Verification: focused live/client/cost tests 19 passed; universe-scan tests 13 passed; `make test-unit` 666 passed / 14 deselected; `make test-integration` 14 passed; lint/tech-debt/file-size/import-boundary ratchets green; `make kalshi-universe-scan`, `make kalshi-ev-ledger`, `make kalshi-paper-decision-candidates`, and `make kalshi-live-preflight` exit 0. Tranche notes at `docs/codex/tranches/2026-07-03-maker-first-kalshi-live-execution-policy.md` and `docs/codex/tranches/2026-07-03-world-cup-sports-discovery-soft-watch.md`.

- 2026-07-03 weather station-provenance hardening + broad family cycle: attacked the current signal-factory bottlenecks without weakening any gate. Fixed crypto routing precedence so `crypto_proxy_capacity_correlation_decay_blocked_no_current_candidates` maps to `signal_factory_crypto_proxy_current_candidates_missing` instead of being misreported as missing depth; after `make kalshi-crypto-proxy-observation-watch-once`, crypto restored 60 current candidates and remains correctly blocked on decay survival (`recent_bucket_below_random`, recent accuracy `0.3333333333`, cumulative `0.6`). Ran the current family cycles: sports now has 58 current MLB observations waiting settlement; ATP remains `atp_proxy_evidence_gate_blocked_waiting_settlement_labels`; weather runs through feature/observation/falsification/replay/CCD/cluster with no usable rows. Found and fixed a severe weather provenance bug: current Kalshi weather tickers encode city in the series (`KXHIGHMIA`, `KXLOWTSFO`), while the feature builder only understood old hyphenated synthetic tickers and defaulted every city to `KNYC`. Added current ticker city-code parsing and station aliases for 20 weather stations; latest weather feature packet is 480 rows across 20 stations (24 rows each) instead of 480 `KNYC` rows. Hardened weather falsification to reject any label row whose `station_id` conflicts with the contract ticker; the latest artifact now honestly reports 360 raw historical weather label rows, 360 station mismatches, 0 valid labels, and status `weather_proxy_feature_model_falsification_blocked_missing_labels`. This resets weather evidence rather than learning from poisoned labels; future post-fix settled rows will enter normally. Also trimmed decorative banners from `scripts/kalshi_sports_proxy_feature_packet.py` to bring it below the file-size ratchet without baseline churn. Verification: focused weather/status/sports tests pass; `make quality` exits 0; `make test-unit` 659 passed / 14 deselected; `make test-integration` 14 passed. Tranche note at `docs/codex/tranches/2026-07-03-weather-station-provenance-and-family-cycle.md`.

- 2026-07-03 ATP donor evidence gate + strict preflight admission: added `scripts/kalshi_atp_proxy_evidence_gate.py` and `make kalshi-atp-proxy-evidence-gate`, now chained after `kalshi-atp-proxy-observation-loop` in `kalshi-atp-proxy-observation-watch-once`. The gate reads predmarket ATP observations plus ATP-oracle forward-OOS, liquidity, bettable-line, and price-observation artifacts, emits only research evidence, and explicitly blocks before falsification/probability/EV/sizing/orders. Latest status is `atp_proxy_evidence_gate_blocked_waiting_settlement_labels`: 24 ATP observation rows, 0/10 settled predmarket labels, 2/10 forward-OOS resolved for probe, 2/25 resolved for first stake, 6/25 executable-liquidity candidates, bettable-line gate blocked, and no EV/sizing/execution output. Source inventory now registers all five ATP donor artifacts; the external artifact wrapper now recognizes ATP `market_ticker` evidence rows and the `observations`/`latest_observations` payload shapes. Latest external preflight: 16 donor artifacts, 15 safe, 974 safe rows; all ATP donor artifacts pass strict preflight (forward-OOS report, liquidity, bettable-line gate, price observations, match snapshot). The only blocked donor artifact is the optional missing MLB manual model drop at `/home/mrwatson/manual_drops/mlb_platform_signal_features/mlb_platform_sports_model_latest.json`. Verification: `make test-unit` 655 passed / 14 deselected; `make test-integration` 14 passed; focused ATP/paper/live tests 26 passed after formatting; `make lint-baseline-check` green (`lint 1409/1422`, `format 90/94`); refreshed ATP evidence gate, source inventory, external preflight, paper decisions, EV ledger, signal-factory status, and live preflight. Tranche note at `docs/codex/tranches/2026-07-03-atp-donor-evidence-gate-preflight.md`.

- 2026-07-03 sports settlement-horizon repair + ATP evidence loop: fixed the remaining sports horizon bug after the MLB/Wimbledon discovery repair. The universe scanner now filters sports by effective settlement/probe horizon rather than blindly by administrative `close_time`: sports prefer `expected_expiration_time` / `expiration_time` / `settlement_time`; ATP date-only match tickers can fall back to a conservative event-date label-probe schedule; the targeted sports series fetch is widened to 720h while the report still trims to the configured settlement window. Baseball feature selection and sports CCD selection now use the same effective sports horizon, so rows are not dropped downstream after discovery. Added `scripts/kalshi_atp_proxy_observation_loop.py` plus Make targets `kalshi-atp-proxy-observation-loop` and `kalshi-atp-proxy-observation-watch-once`; ATP/Wimbledon donor snapshots from `/home/mrwatson/projects/atp-oracle/data/kalshi/` are archived only as research observations, with labels still coming only from exact public Kalshi settlement. Latest 48h universe scan: 3,552 candidates, 88 ATP-routed, 214 MLB-classified, 302 model-routed total. Latest baseball feature packet: 118 current game rows (58 MLB ready, 60 KBO/LMB proxy-source unavailable); sports observation loop archived 58 MLB observations and remains waiting for settlement labels. Latest ATP loop archived 24 Wimbledon observations from 12 matches and remains waiting for settlement labels, next public label probe `2026-07-04T06:00:00Z`. EV/paper/live safety remains blocked: 490 paper candidates, 490 blocked, 0 paper usable, 0 live eligible. Verification: touched-file Ruff clean; focused sports/ATP tests 78 passed; `make test-unit` 650 passed / 14 deselected; `make test-integration` 14 passed. Known inherited issue: `make lint-baseline-check` still fails on repo-wide format ratchet (`format 98 > 94`), and the format offender list does not include this tranche's touched files. Tranche note at `docs/codex/tranches/2026-07-03-sports-settlement-horizon-atp-evidence-loop.md`.

- 2026-07-03 sports-game discovery repair for Wimbledon + MLB: fixed the Kalshi universe scanner so sports game/match markets no longer depend on the broad `/markets` page order. The scanner now runs targeted public series fetches for baseball and tennis (`KXMLBGAME`, `KXLMBGAME`, `KXKBOGAME`, `KXATPMATCH`, ATP set/game series, Wimbledon series), merges by exact ticker, and routes direct prefixes to `mlb-platform` or `atp-oracle` even when Kalshi omits `series_ticker`. Latest 720-hour universe scan: 10,182 candidates, 470 ATP-routed, 785 MLB-routed, including 24 `KXATPMATCH`, 88 `KXMLBGAME`, and 68 `KXLMBGAME` rows. Current MLB rows now reach the sports feature packet (`176` baseball game rows: 88 MLB, 68 LMB, 20 KBO) and are carried into the EV ledger/paper decisions as blocked current-game rows. ATP Wimbledon match snapshots from `/home/mrwatson/projects/atp-oracle/data/kalshi/matches-2026-07-03.json` now emit both YES contracts per match into the EV ledger/paper decisions as exact, blocked rows. No probabilities were promoted: all new sports rows remain `usable=false`, `paper_stake=0`, and live-ineligible until calibrated contract probability, verified terms, falsification/replay evidence, capacity, correlation, and decay gates pass. Verification: focused tests 38 passed; `make test-unit` 640 passed / 14 deselected; `make test-integration` 14 passed; refreshed universe, sports feature/gate chain, EV ledger, paper decisions, live preflight, and signal-factory status all exit 0. Known inherited issue: `make lint-baseline-check` still fails on pre-existing format ratchet overage (`format 98 > 94`), while touched Python files are Ruff-formatted. Tranche note at `docs/codex/tranches/2026-07-03-sports-game-discovery-wimbledon-mlb-routing.md`.

- 2026-07-03 live-autonomous Kalshi engine: added the first unattended execution layer capable of promoting paper-autonomous EV rows into live Kalshi V2 order intents, sizing them under strict caps, submitting buy-only limit orders when armed, reconciling fills/rejections after restart, and halting new buys on anomalous state. New modules: `predmarket/kalshi_live_client.py` (current REST V2 authenticated client with RSA-PSS headers, `/portfolio/events/orders`, deterministic client order IDs, and `bid=yes` / `ask=no` direction mapping), `predmarket/kalshi_live_engine.py` (live eligibility boundary, `LiveRiskLimits`, `LiveStateStore`, risk snapshots, kill switches, one-shot trader, reconciliation), and `predmarket/kalshi_live_artifacts.py` (audit artifact writers). New scripts/targets: `kalshi-live-preflight`, `kalshi-live-demo`, `kalshi-live-trader`, `kalshi-live-reconcile`, and `kalshi-live-risk-snapshot`. Config now has `kalshi_live.execution_mode` (`disabled|demo|live`, default `disabled`) and strict initial caps: $250 max open exposure, $25 per contract, $100 per family, $50 per cluster, $100 daily gross buys, $50 daily loss, 300s no-new-entry buffer, 60s unreconciled-order timeout, and 5 max orders/run. Production live requires config execution enabled, valid credentials, and both documented production-live arming environment variables; no manual approval queue is introduced. Latest default artifacts are correctly blocked because the repo is unarmed and no account snapshot is available in unarmed mode: live preflight/trader both have 348 decisions, 0 live-eligible, and $0 live stake; risk snapshot has 0 exposure and no kill switches; reconcile is blocked without a valid authenticated client. No live order was submitted. Verification: focused live tests 20 passed; `make test-unit` 638 passed / 14 deselected; `make test-integration` 14 passed; `make kalshi-signal-factory-status`, `make kalshi-ev-ledger`, `make kalshi-external-artifact-preflight`, `make kalshi-live-preflight`, `make kalshi-live-trader`, `make kalshi-live-reconcile`, and `make kalshi-live-risk-snapshot` exit 0; `make quality` exits 0 with advisory backlog; tech-debt/file-size/import-boundary gates pass. Known nuance: `make lint-baseline-check` currently reports `format 100 > 94`, caused by six already-present untracked paper/donor-foundation files under `predmarket/`; the new live-engine files are formatted and are not in Ruff's format backlog. Tranche note at `docs/codex/tranches/2026-07-03-live-autonomous-kalshi-engine.md`.

- 2026-07-03 external donor artifact manifest wraps: wrapped the highest-value donor outputs in strict external artifact manifests and moved preflight from all-blocked to mostly-admitted. Added `predmarket/external_artifact_wrappers.py` and `scripts/kalshi_external_artifact_wrap.py`; `make kalshi-external-artifact-wrap` writes safe wrappers outside the repo under `/home/mrwatson/manual_drops/predmarket_external_artifacts/`, and `make kalshi-external-artifact-preflight` now auto-wraps by default before validating through the bridge. Embedded `external_manifest` records source repo, source path, source SHA256, family id, model id, artifact kind, generated time, and exact-contract-mapping requirement; `predmarket/external_artifact_bridge.py` now validates the embedded manifest and fails if the donor source hash changes. Source inventory now includes the existing NFL contract-mapping overlay, NFL calibrated-probability overlay, current NFL historical-line backtest, and NFL calibration overlay. Latest wrap status: 13 source artifacts, 12 wrapped, 910 wrapped rows, 1 blocked missing optional MLB-platform live model drop. Latest preflight status: 12/13 artifacts safe, 910 safe rows admitted; admitted surfaces include MLB closing/settlement evidence, NFL overlays/fair-line/backtest/calibration, ATP OOS/watch artifacts, NBA market-claim gate evidence, and us-statarb public-source readiness adapters. No donor repo is imported as a runtime dependency; no live execution, account, order, approval queue, or trading path was added. Verification: focused wrapper/preflight tests 14 passed; `make test-unit` 631 passed / 14 deselected; `make test-integration` 14 passed; `make quality` exits 0 with advisory deptry findings only. Tranche note at `docs/codex/tranches/2026-07-03-external-artifact-manifest-wraps.md`.

- 2026-07-03 paper-autonomous Kalshi EV engine foundation: added the control-plane layer for moving from research artifacts toward a systematic paper-autonomous operation without live execution. New modules: `predmarket/source_inventory.py` inventories donor repos as typed sources with git/provenance state and no runtime dependency; `predmarket/external_artifact_bridge.py` defines the generic external artifact manifest and validator; `predmarket/signal_formula.py` adds a safe formula DSL for machine-generated weak signals with multiple-testing accounting; `predmarket/paper_decision_engine.py` emits unified paper decision candidates with fractional/correlation-aware Kelly math forced to zero unless all falsification, cost, capacity, cluster, and decay gates pass; `predmarket/signal_decay_retirement.py` builds the signal survival/retirement ledger. The optional MLB-platform bridge now routes through the generic artifact bridge while preserving its old optional/unsafe/missing behavior. New Make targets: `kalshi-source-repo-inventory`, `kalshi-external-artifact-preflight`, `kalshi-signal-formula-registry`, `kalshi-paper-decision-candidates`, and `kalshi-signal-decay-retirement`. Latest artifacts report: source inventory ready across 8 donor repos and 10 candidate artifacts; external preflight ready but 0/10 artifacts admitted because current donor drops lack the strict manifest/safety contract; formula registry ready with 2 safe formulas and 2 multiple-testing hypotheses; paper candidates ready with 348 ledger rows, 348 blocked, 0 usable, and $0 paper stake; candidate rows now preserve `signal_key`, `signal_formula_key`, close bucket, predicted outcome, and settlement outcome for closed-loop decay; decay retirement ready with 4 active signals and 0 retired. No account, order, execution, approval queue, or live trading path was added. Verification: focused paper-autonomous tests 9 passed; focused paper-autonomous integration replay tests 2 passed; focused MLB bridge tests 2 passed; `make test-unit` 629 passed / 13 deselected; `make test-integration` 13 passed; `make kalshi-signal-factory-status`, `make kalshi-ev-ledger`, and all five new Make targets exit 0; `make quality` exits 0 with advisory deptry findings only. Tranche note at `docs/codex/tranches/2026-07-03-paper-autonomous-ev-engine-foundation.md`.

- 2026-07-03 MLB-platform sports model bridge: predmarket now has an optional, research-only artifact bridge for richer MLB-platform baseball model outputs without importing or depending on `/home/mrwatson/projects/mlb-platform`. `predmarket/mlb_platform_bridge.py` validates a safe external JSON artifact at `/home/mrwatson/manual_drops/mlb_platform_signal_features/mlb_platform_sports_model_latest.json`; `scripts/kalshi_sports_proxy_feature_packet.py` joins rows by exact contract ticker first, then league/game/selected-team fallback keys, and attaches `mlb_platform_model_probability`, side, model id, match key, and artifact hash fields. The sports observation/label loop carries those fields into label packets; sports falsification now evaluates `mlb_platform_model_directional_accuracy` beside the greenfield `strength_win_prob_directional_accuracy`; replay auto-selects the best FDR-passed candidate by q-value unless `KALSHI_SPORTS_PROXY_REPLAY_PREFERRED_MODEL_ID` is set. Missing or unsafe MLB-platform artifacts are optional warnings, not blockers. Every row remains `usable=false`; no paper overlay, sizing, execution, account, order, or DB path was introduced. Verification: `make test-unit` 620 passed / 11 deselected, `make test-integration` 11 passed, lint ratchet green (`lint 1421/1422`, `format 92/94`), tech-debt/file-size/import-boundary/feature-flag/AGENTS gates green. Tranche note at `docs/codex/tranches/2026-07-03-mlb-platform-sports-model-bridge.md`.

- 2026-07-03 formal Agentic Falsification Architecture theorem doctrine landed: wrote the full formal theorem to `docs/codex/macro/kalshi-agentic-falsification-architecture-theorem.md` as the authoritative architecture doctrine. The document reproduces all three Axioms (No Discretion, Breadth Dominance, Capacity Boundedness with Pr[cap>0]~0.18 and $5K-$15K), both Lemmas (Kelly not Markowitz with the Kalshi-recovering-Kelly-native-case remark; the significance bar rises in K with the agentic-generation remark), the Theorem (minimal construction with the formal argmax, feasibility, and minimality proofs), the Corollary (sequencing: sports first, admit slower categories only once FDR stabilizes), and the Closing Remark (the object of value is the operator itself). The mission scope boundary is explicitly stated: the Kelly sizing layer is blocked in this mission (research-only; usable=false throughout). The existing earlier condensed notes (`kalshi-agentic-falsification-architecture.md`, `kalshi-signal-factory-north-star.md`) are identified as historical precursors; the new theorem document is the single source of truth. **No executable code changed.** Tranche note at `docs/codex/tranches/2026-07-03-formal-theorem-doctrine.md`; completion report at repo root.

- 2026-07-03 weather falsification/replay/CCD/cluster-control gates (M4 follow-up): completed the weather lane through the full falsification/replay/CCD/cluster-control chain via the SignalFamily engine. Added (1) `scripts/kalshi_weather_proxy_feature_model_falsification.py` — runs WeatherFamily's `weather_prediction_rule` + `WEATHER_MODEL_EVALUATORS` through `engine.build_falsification()`, producing FDR-controlled evaluations with `weather_bracket_directional_accuracy` and `weather_market_yes_ask_probability_baseline`; (2) `scripts/kalshi_weather_proxy_research_candidate_replay.py` — Wilson lower-bound calibration via `engine.build_replay_calibration()`, all-in Kalshi cost via `normalize_kalshi_execution_cost`, EV/margin math, `station|bracket|date` cluster key via `weather_cluster_key_composer`; (3) `scripts/kalshi_weather_proxy_capacity_correlation_decay.py` — probes public Kalshi orderbook depth for weather contracts via engine's `ask_levels`/`build_capacity_row`, station|bracket|date cluster key, decay via `engine.build_decay_summary`; (4) `scripts/kalshi_weather_proxy_correlation_cluster_control.py` — 35% max-share cap via engine's fully generic `controlled_capacity_rows`. Added Make targets `kalshi-weather-proxy-feature-model-falsification`, `kalshi-weather-proxy-research-candidate-replay`, `kalshi-weather-proxy-capacity-correlation-decay`, `kalshi-weather-proxy-correlation-cluster-control`, wired into `kalshi-weather-proxy-observation-watch-once`. **Zero spine edits** (engine.py, shared_helpers.py, signal_family.py unchanged). Every row `usable=false`. 27 new artifact-replay tests pass (importlib load + synthetic fixtures + off-line). Crypto + sports characterization tests unchanged (616 total pass). All 5 binding quality gates green.

- 2026-07-03 weather family through engine (Milestone 4): added the weather family as the THIRD signal family through the SignalFamily engine, proving the abstraction makes adding families cheap (zero edits to the generic spine). Created `predmarket/weather_family.py` (WeatherProxyFamily descriptor: `family_id=weather_proxy`, `classification_tag=[KXHIGH,KXLOW]`, `status_prefix=weather_proxy`, `official_settlement_source=NWS Daily Climate Report (CLI product)`, station/gridpoint resolver via `api.weather.gov` keyless API, NWS forecast + observation bracket feature model, `station|bracket|date` cluster key, `weather_bracket_directional_accuracy` + market baseline model evaluators, mechanical `bracket_probability - yes_ask >= tau` prediction rule). Built `scripts/kalshi_weather_proxy_feature_packet.py` (filters KXHIGH/KXLOW contracts, resolves NWS station/gridpoint, fetches forecast + observations, computes bracket probability, every row `usable=false`) and `scripts/kalshi_weather_proxy_observation_loop.py` (archives observations deduped by observation_id, emits labels from Kalshi public settlement only, never from NWS proxy). Added Make targets `kalshi-weather-proxy-feature-packet`, `kalshi-weather-proxy-feature-watch-once`, `kalshi-weather-proxy-observation-loop`, `kalshi-weather-proxy-observation-watch-once`. **M3-ENGINE-HARDENING (scrutiny-surfaced):** added `status_prefix` field to `SignalFamily` descriptor and fixed `engine._falsification_status()` to use it instead of substring matching on `family_id` — prevents weather being misrouted to a sports/crypto prefix. Updated `crypto_family.py` (status_prefix="crypto_proxy"), `sports_family.py` (status_prefix="sports_proxy"), and `weather_family.py` (status_prefix="weather_proxy"). Updated `kalshi_signal_factory_families.py` with weather family ID, capability names, tranche map, capability builder, and status computation; updated `kalshi_signal_factory_status.py` with weather artifact paths, weather data loading, and three-family status selection. Real public-data run produces honest research-only artifacts (168 feature rows, all `usable=false`, `research_only=true`, `execution_enabled=false`). All 600 unit tests pass (26 new weather tests + 29 status tests); crypto (143) + sports (90) characterization tests unchanged — passing without modification. Three families (crypto + sports + weather) coexist in status/scout/router. All 5 binding quality gates green.

- 2026-07-03 strangler-fig migration completion (Milestone 3): completed the sports family migration onto the SignalFamily engine. All shared statistical helpers (benjamini_hochberg, binomial_survival, wilson_lower_bound, chronological_split_index, independent_contract_rows, outcome_value, bucket_time, gate, safety_flags, read_json_or_empty, outside_repo, plus 15+ type/IO helpers) single-sourced from `predmarket.shared_helpers` — the duplicated definitions in `scripts/kalshi_falsification_replay_shared.py` were removed and replaced with imports. M1 tech debt addressed: (a) sports observation-loop blocked-label reason split into `pending_contract_not_settled_in_snapshot` (ticker-absent) vs `settlement_outcome_missing` (outcome-unparseable), matching crypto's diagnosability; (b) `path_is_within(MACRO_DIR, out_dir)` guard standardized across all families' `write_*` functions (3 scripts updated to import from `predmarket.shared_helpers`); (c) dedicated `test_lmb_team_resolver_via_espn` test added mirroring the KBO test for hardening; (d) crypto `proxy_state_prediction` (raw-string signature) replaced with row-based `crypto_prediction_rule(row)[0]` across all 3 crypto call sites (falsification, replay, CCD) — the 3 local `proxy_state_prediction` definitions removed. Behavioral invariance verified: 563 unit tests pass, all 143 crypto characterization + macro router tests pass without assertion changes, all 89 sports tests pass. Net code reduction of 171 lines (120 insertions, 291 deletions). All 5 binding quality gates green (lint 1417/1422, tech-debt 22/22, no new oversized files, modularize 2/0, AGENTS.md valid). Engine spine confirmed closed for modification: adding weather (next milestone) requires only a new descriptor + family-specific modules with zero edits to the generic spine. Full report at `docs/audits/COMPLETION_REPORT_2026-07-03_STRANGLER_FIG_MIGRATION_COMPLETE_FULFILLED.md`; tranche note at `docs/codex/tranches/2026-07-03-strangler-fig-migration-complete.md`.

- 2026-07-03 engine extraction (Milestone 3, strangler-fig): extracted the generic pipeline spine into a family-parameterized SignalFamily engine under `predmarket/`. Created `predmarket/signal_family.py` (SignalFamily descriptor dataclass with 9 plug-in fields: family_id, classification_tag, official_settlement_source, reference_source_registry, fetcher, feature_definitions, prediction_rule, model_evaluators, cluster_key_composer), `predmarket/shared_helpers.py` (single-sourced statistical helpers: benjamini_hochberg, binomial_survival, wilson_lower_bound, normalize_kalshi_execution_cost, the cluster-control binary search, chronological_split_index, independent_contract_rows, plus IO/safety/type helpers — all per-family duplication removed), and `predmarket/engine.py` (generic spine stages for falsification, calibration, decay, and cluster control). Built `predmarket/crypto_family.py` (CryptoProxyFamily: ASSET_CONFIG, crypto_prediction_rule above->YES/below->NO, crypto_cluster_key_composer asset|family|close_bucket, crypto model evaluators proxy_state_directional + market_yes_ask_baseline) and `predmarket/sports_family.py` (SportsBaseballFamily: strength-win-prob evaluator, league|game_winner|date cluster key). Refactored the 6 crypto proxy scripts to import shared helpers from `predmarket.shared_helpers` instead of defining them locally — the scripts retain the same public API and produce identical output. Import boundary verified: `predmarket/` has zero imports of `scripts/`. 20 invariance tests prove the engine route produces correct output. 573 tests pass; all 5 binding quality gates green (lint ratchet improved 1422→1417, modularize 2 kept/0 broken, no new oversized files, tech debt unchanged). Engine ready for weather-family addition (next milestone) — adding a family touches only a new descriptor + family-specific modules with zero edits to the generic spine.

- 2026-07-03 sports CCD + cluster control + decay gates (Milestone 2, M2 complete): Completed the sports lane end-to-end through the full gate chain. Built `scripts/kalshi_sports_proxy_capacity_correlation_decay.py` (sports CCD gate: public Kalshi orderbook depth probe, YES/NO reciprocal-ask depth derivation, round-robin cluster selection by `league|event_ticker|date` so each game is an independent cluster, capacity prior Axiom 3 at ~0.18 positive-depth fraction, capacity_row computation, decay gate from replay artifact) and `scripts/kalshi_sports_proxy_correlation_cluster_control.py` (fully generic binary-search 35% max-share capping reused on sports capacity_rows; family-agnostic). Extended the signal-factory families module with falsification/replay/CCD/cluster-control capability detection and extended the Artifacts dataclass and CLI with the 4 new sports artifact paths. Updated the Makefile with `kalshi-sports-proxy-capacity-correlation-decay` and `kalshi-sports-proxy-correlation-cluster-control` targets, both wired into the watch-once chain. 21 new artifact-replay tests for the CCD gate and 8 for cluster control. Real public-data E2E run (`make kalshi-sports-proxy-observation-watch-once`) produces honest gated artifacts: feature packet blocked (no open fast sports contracts), observation loop blocked, falsification blocked (missing labels), replay blocked (no candidate), CCD blocked (missing replay candidate), cluster control blocked (upstream CCD) — every row `usable=false`, no forced pass. Crypto invariance confirmed: all 103 crypto characterization + router + cost tests + 542 total unit tests pass unchanged. All 5 binding quality gates green. KBO/LMB ESPN endpoints still return HTTP 400 (off-season/drift); map re-verification carried forward, non-blocking.

- 2026-07-03 generalized falsification + replay for sports (Milestone 2): generalized the falsification harness and research-candidate replay so they accept a family's model-evaluator list + prediction rule, then the sports strength-win-probability model runs through them. Extracted a shared companion module `scripts/kalshi_falsification_replay_shared.py` with the generic statistical core (Benjamini-Hochberg FDR, exact binomial survival, chronological OOS split, contract-key independence collapse, Wilson lower-bound calibration, all-in cost via `predmarket.kalshi_execution_cost`) plus generic label-loading, replay math, decay bucketing, and writer scaffolding. Built `scripts/kalshi_sports_proxy_feature_model_falsification.py` (sports falsification: strength_win_prob_directional evaluator, sports prediction rule via `predicted_side`, collapsed by contract_ticker, chronological OOS split, BH q-value at alpha=0.10 -- promotion and block both tested) and `scripts/kalshi_sports_proxy_research_candidate_replay.py` (sports replay: Wilson lower-bound calibration <= raw OOS accuracy, all-in cost via shared `normalize_kalshi_execution_cost`, margin = calibrated - break_even, EV = calibrated - all_in_cost, sports cluster key `league|ticker|date`, every row usable=false). Added Make targets `kalshi-sports-proxy-feature-model-falsification` and `kalshi-sports-proxy-research-candidate-replay`, both wired into the sports watch-once chain. Crypto invariance confirmed: all 12 crypto falsification/replay tests + 143 crypto characterization suite tests pass UNCHANGED. 27 artifact-replay tests cover both promotion and block branches; `make test-unit` 521 passed; all 5 binding quality gates green. Full report at `docs/audits/COMPLETION_REPORT_2026-07-03_GENERALIZED_FALSIFICATION_REPLAY_FULFILLED.md`; tranche note at `docs/codex/tranches/2026-07-03-generalized-falsification-replay.md`.

## Current Landing

- 2026-07-03 sports observation/label loop (Milestone 1): built `scripts/kalshi_sports_proxy_observation_loop.py`, the sports analog of `scripts/kalshi_crypto_proxy_observation_loop.py`, reusing its ~85%-generic machinery (snapshot ready feature rows, dedupe by `observation_id`, match observed Kalshi tickers after game end against public settlement, emit label rows). Sports-specific: reads `latest-kalshi-sports-proxy-feature-packet.json` rows where `feature_status == "sports_proxy_features_ready"`, probes observed Kalshi tickers after `expected_expiration_time`, and emits labels from KALSHI PUBLIC SETTLEMENT (the contract's actual settlement outcome; the official game result / league box score is Kalshi's documented settlement source, but the LABEL OF RECORD is what Kalshi settled). Cosmetic constants changed: `observation_id` prefix `sports_obs_`, `packet_type` strings `kalshi_sports_proxy_feature_observations`/`_labels`, output dirs `manual_drops/kalshi_sports_proxy_observations/` and `/kalshi_sports_proxy_labels/`. Every observation + label row is `usable=false`, `calibrated_probability=null`, `expected_value_per_contract=null`; the strength-model `win_probability` is a model FEATURE and is never the label. The optional crypto-proxy orderbook-depth enrichment flag is dropped (crypto-proxy-specific). Added Make targets `kalshi-sports-proxy-observation-loop` + `kalshi-sports-proxy-observation-watch-once`. 13 artifact-replay tests pass (importlib load + injected `fetch_json`); crypto characterization + router suite unchanged (120 passed); `make test-unit` 494 passed; all 5 binding quality gates green. `make kalshi-sports-proxy-observation-loop` exits 0 with an honest research-only artifact (`sports_proxy_observation_loop_blocked_no_observations` against the current scan: no open sports contracts right now). Full report at `COMPLETION_REPORT_2026-07-03_KALSHI_SPORTS_OBSERVATION_LABEL_LOOP_FULFILLED.md`; tranche note at `docs/codex/tranches/2026-07-03-sports-observation-label-loop.md`.

- 2026-07-03 sports observation/label loop (Milestone 1): built `scripts/kalshi_sports_proxy_observation_loop.py`, the sports analog of `scripts/kalshi_crypto_proxy_observation_loop.py`, reusing its ~85%-generic machinery (snapshot ready feature rows, dedupe by `observation_id`, match observed Kalshi tickers after game end against public settlement, emit label rows). Sports-specific: reads `latest-kalshi-sports-proxy-feature-packet.json` rows where `feature_status == "sports_proxy_features_ready"`, probes observed Kalshi tickers after `expected_expiration_time`, and emits labels from KALSHI PUBLIC SETTLEMENT (the contract's actual settlement outcome; the official game result / league box score is Kalshi's documented settlement source, but the LABEL OF RECORD is what Kalshi settled). Cosmetic constants changed: `observation_id` prefix `sports_obs_`, `packet_type` strings `kalshi_sports_proxy_feature_observations`/`_labels`, output dirs `manual_drops/kalshi_sports_proxy_observations/` and `/kalshi_sports_proxy_labels/`. Every observation + label row is `usable=false`, `calibrated_probability=null`, `expected_value_per_contract=null`; the strength-model `win_probability` is a model FEATURE and is never the label. The optional crypto-proxy orderbook-depth enrichment flag is dropped (crypto-proxy-specific). Added Make targets `kalshi-sports-proxy-observation-loop` + `kalshi-sports-proxy-observation-watch-once`. 13 artifact-replay tests pass (importlib load + injected `fetch_json`); crypto characterization + router suite unchanged (120 passed); `make test-unit` 494 passed; all 5 binding quality gates green. `make kalshi-sports-proxy-observation-loop` exits 0 with an honest research-only artifact (`sports_proxy_observation_loop_blocked_no_observations` against the current scan: no open sports contracts right now). Full report at `COMPLETION_REPORT_2026-07-03_KALSHI_SPORTS_OBSERVATION_LABEL_LOOP_FULFILLED.md`; tranche note at `docs/codex/tranches/2026-07-03-sports-observation-label-loop.md`.

- 2026-07-03 sports feature packet (Milestone 1): built the baseball feature-packet builder `scripts/kalshi_sports_proxy_feature_packet.py`, the sports analog of the crypto feature packet, reusing the crypto scaffolding (report envelope, `safe_research_artifact`, `safety_flags`, `build_gates`, `write_*` + `latest-*` pointers, close-time-windowed selection). New sports plug-in: (1) per-league team-identity resolver (MLB Stats API `gamePk` + team ids; ESPN event/competitor ids for KBO/LMB) with a regression guard that admits only game-winner series and treats YES as "selected team wins outright" (moneyline) so the prior run-line sign-convention bug cannot recur; (2) a keyless feature fetcher (statsapi schedule+standings for MLB, ESPN scoreboard for KBO/LMB) with injectable `fetch_json`; (3) a pre-game strength model (standings win% + pythagorean -> sigmoid `p_home_win`) whose coefficients are fit ONCE then FROZEN in a checked-in artifact (`docs/codex/macro/sports-baseball-strength-coefficients.json`) and never refit at decision time; (4) a mechanical `|p_win - yes_ask| >= tau` prediction rule. Added Make target `kalshi-sports-proxy-feature-packet`. Every row is `usable=false`, `calibrated_probability=null`; raw public payloads stay outside the repo under `manual_drops/kalshi_sports_proxy_features/`; derived artifacts + `latest-*` pointers under `docs/codex/macro/`. It is a DIFFERENT artifact from Type 2 (model features + prediction, not a sportsbook-reference margin). `make test-unit` 481 passed (+28 artifact-replay tests); crypto characterization + router suite unchanged (111 passed); all 5 binding quality gates green; `make kalshi-sports-proxy-feature-packet` exits 0 with an honest research-only artifact (`sports_proxy_feature_packet_blocked_no_open_fast_sports_contracts` against the current scan). Full report at `docs/audits/COMPLETION_REPORT_2026-07-03_KALSHI_SPORTS_FEATURE_PACKET_FULFILLED.md`; tranche note at `docs/codex/tranches/2026-07-03-sports-feature-packet.md`.

- 2026-07-03 family-aware orchestration: generalized the three single-family orchestration scripts to report and route MULTIPLE signal families (crypto_proxy + sports_baseball) without being hardcoded to crypto. All changes are ADDITIVE (strangler-fig precursor). (1) `scripts/kalshi_signal_factory_status.py` + new companion `scripts/kalshi_signal_factory_families.py`: the status report now iterates a family registry, emitting per-family capability groupings (`report["families"]["crypto_proxy"]` and `report["families"]["sports_baseball"]`) plus a multi-family summary rollup. The `Artifacts` dataclass carries family-keyed sports paths (crypto defaults preserved). Top-level status is selected across families by advancement rank with a documented `status_selection` note. (2) `scripts/kalshi_probability_breadth_scout.py`: added a `sports_baseball_fast_label_route` for KXMLBGAME/KXKBOGAME/KXLMBGAME classification that is not shadowed or starved by crypto; sports and crypto both surface when both present (order-independent); summary now carries `sports_fast_candidate_count`. (3) `scripts/codex_macro_router.py` (additive only): every `signal_factory_sports_*` status is now in the `apply_scheduling()` priority-override set (priority >= 0, tier parity with crypto), has a sports-specific tranche + stop condition, and has a `PARKED_UNLOCKS` entry; sports artifact paths added to `predmarket_status()`. Crypto characterization tests (143) stay green; `make test-unit` 453 passed; all 5 binding quality gates pass. Latest status report shows both families: crypto at `capacity_depth_blocked` (leading), sports at `blocked_missing_feature_packet`. Latest scout shows 2 sports candidates alongside 9 crypto + 192 weather. 28 new tests added. Full report at `docs/audits/COMPLETION_REPORT_2026-07-03_FAMILY_AWARE_ORCHESTRATION_FULFILLED.md`; tranche note at `docs/codex/tranches/2026-07-03-family-aware-orchestration.md`.

- 2026-07-02 Agent-Readiness fix verification landing: re-evaluated all 82 Factory Agent-Readiness criteria against the working tree and ratified the readiness-fix tranche. All 7 hard gates exit 0 (`make quality`, `lint-baseline-check` at 1421/1422 + 94/94, `tech-debt-check` 20/20, `file-sizes-check` 2 known, `modularize` 2 kept/0 broken, `deptry` 152 advisory, `test-unit` 425 passed). The 5 quality modules import cleanly with real (non-stub) symbols and 15 passing tests. No-regression confirmed: `kalshi_dataset.py` adds only data-retrieval params + `fetch_series_list`, `requirements.txt` is exact-pinned (adds tenacity/sentry-sdk/SQLAlchemy), `conftest.py` adds only the `QueryCounter` N+1 fixture. CI/governance audited as safe (release job intended + scoped, error-triage de-duped, security read-only, Semgrep/gitleaks present) and all three ratchet baselines match live counts (not inflated). Refreshed readiness level: **Level 4 (conservative, 55/70 = 78.6%), reaching Level 5 (60/70 = 85.7%) under a lenient criterion-text reading** -- both strictly above the Level 2 baseline (27.5%). ~41 criteria flipped FAIL->PASS with file-path evidence; 5 are "partial" (infrastructure exists but not wired into the live app: feature_flag_infrastructure, log_scrubbing, error_tracking_contextualized, circuit_breakers, error_to_insight_pipeline). **TOP RISK: all fixes are uncommitted -- local HEAD == origin/main == 4c0d41f, so the refreshed score is working-tree-only and a fresh checkout/CI run sees none of it.** Full classified report at `docs/audits/COMPLETION_REPORT_2026-07-02_AGENT_READINESS_VERIFICATION.md`; tranche note at `docs/codex/tranches/2026-07-02-readiness-fix-verification.md`. No source/config/baseline/CI files were modified.
- 2026-07-02 code-quality tranche verification + repair + decay evidence auditability: verified the quality-tranche wiring (pyproject.toml with ruff/mypy/vulture/import-linter, pre-commit hooks, CI code-quality job, ratchet baselines, scan_tech_debt and check_file_sizes scripts) and all four hard quality gates pass (`make lint-baseline-check`, `make tech-debt-check`, `make file-sizes-check`, `make modularize`). Repaired 3 pre-existing failing tests in `test_kalshi_crypto_proxy_capacity_correlation_decay.py`, `test_kalshi_crypto_proxy_correlation_cluster_control.py`, and `test_kalshi_signal_factory_status.py`: the `path_is_within(out_dir, CONTROL_REPO)` heuristic was too broad and matched pytest tmp dirs inside the repo; narrowed it to `path_is_within(out_dir, MACRO_DIR)` so latest-pointer writing only fires for canonical macro output paths. Then enriched crypto proxy decay evidence for full auditability: `decay_summary()` now returns per-bucket detail (bucket key, label count, correct count, accuracy, pass_threshold), total_decay_labels, passing_bucket_count, cumulative_decay_accuracy, and recent_bucket_key/label_count -- without changing any gate threshold. The replay and CCD gate reasons now include the full per-bucket breakdown. Latest replay decay evidence shows 3/4 buckets pass >= 0.5, cumulative accuracy 0.6 across 95 labels, but the recent bucket (9 labels, accuracy 0.333) is still below random, so the decay gate remains correctly blocked. All 410 unit tests pass. Zero usable rows, no paper overlay, no sizing, no execution, no account/order paths.

## Current Landing

- 2026-07-02 CI green and routing brain hermeticity: the CI built in the prior quality tranche was red on day one (1362 ruff lint errors, 77 format violations, 9 failing signal-factory-status tests). Fixed all three: (1) introduced `Artifacts` frozen dataclass in `scripts/kalshi_signal_factory_status.py` so `build_signal_factory_status()` takes a single `artifacts` bundle instead of 13 path params with on-disk defaults -- tests can no longer leak real disk state through a forgotten default; (2) added `.ruff-baseline.json` + `scripts/ruff_baseline_check.py` ratchet gate so CI lint is green today and fails only when violation counts increase; (3) all 390 unit tests and 11 integration tests pass.

## Current Landing

- Latest observed commit: `4c0d41f docs: document Kalshi hard paper blockers`.
- Latest goal work produced a clean Kalshi research desk with replay, paper-ledger, event-history, integrity, hard blockers, and `make kalshi-verify`.
- The federated macro control plane now lives under `docs/codex/macro/`.
- Active universe is exactly five repos: predmarket, MLB, ATP, NBA, and NFL.
- `scripts/codex_macro_router.py route --write` produced `latest-status.json`, `latest-decision.json`, and `latest-decision.md`.
- Macro routing is active from this repo. Run `make macro-route` before selecting the next repo unless the user overrides it.
- Latest refreshed macro decision reports `all_lanes_parked=false` and routes to predmarket because the crypto proxy research candidate has now been replayed against all-in cost, but capacity-depth, correlation-cluster, and decay-survival gates still block any paper probability overlay or usable-edge claim.
- 2026-07-01 new north star: the operation is now explicitly a Kalshi signal factory whose mission is to extract and exploit event-contract mispricings before the crowd corrects them. Axioms: no discretion, signal breadth over depth, and structural capacity discipline. The recorded doctrine is `docs/codex/macro/kalshi-signal-factory-north-star.md`.
- 2026-07-01 signal-factory status foundation: `make kalshi-signal-factory-status` now writes `docs/codex/macro/kalshi-signal-factory-status-latest/kalshi-signal-factory-status.{json,md}` plus latest pointers. Initial status was `signal_factory_hypothesis_registry_ready_falsification_blocked_missing_labeled_oos_evidence`: universe scanner, deterministic routing, contract EV ledger, and HypothesisCandidate registry existed, while all 36 hypotheses were blocked by missing labeled out-of-sample cost-aware FDR evidence. This foundation has been superseded by the 2026-07-02 crypto proxy label/model/replay chain below; the current routed blocker is capacity-depth, correlation-cluster, and decay-survival gates before any paper probability overlay.
- 2026-07-01 HypothesisCandidate registry: `make kalshi-hypothesis-registry` now writes `docs/codex/macro/kalshi-hypothesis-registry-latest/kalshi-hypothesis-registry.{json,md,csv}` and `kalshi-falsification-gate.{json,md}`, plus latest pointers under `docs/codex/macro/`. Current registry status is `hypothesis_registry_ready_falsification_blocked_missing_labeled_oos_evidence`: 36 hypotheses, 0 tested, 0 promoted, 0 rejected, 36 blocked. Route counts are `soft_market_research_backlog=30`, `mlb-platform=4`, and `nfl_quant_glm51_greenfield=2`. Feature-family counts are `low_liquidity_ghost_listing=8`, `stale_metadata_update_lag=8`, `wide_spread_decay=8`, `near_settlement_probability_decay=7`, `simple_rule_external_reference=3`, `calibrated_probability_decay=1`, and `legacy_positive_margin_survival=1`. This is registry/falsification infrastructure only; it does not compute usable EV or authorize trades.
- 2026-07-01 labeled OOS backtest harness: `make kalshi-labeled-oos-backtest` now writes `docs/codex/macro/kalshi-labeled-oos-backtest-latest/kalshi-labeled-oos-backtest.{json,md,csv}` and `kalshi-oos-falsification-gate.{json,md}`, plus latest pointers. The safe label-packet schema is `docs/codex/macro/kalshi-labeled-oos-observation.schema.json`. Current status is `labeled_oos_backtest_blocked_missing_labeled_observations`: 36 hypotheses, 0 label packets, 0 valid observations, 0 testable hypotheses, 0 research promotions.
- 2026-07-01 labeled observation builder: `make kalshi-labeled-observation-builder` and `make kalshi-labeled-observation-watch-once` now write `docs/codex/macro/kalshi-labeled-observation-builder-latest/kalshi-labeled-observation-builder.{json,md,csv}` plus latest pointers. Pending packets are written outside the repo under `/home/mrwatson/manual_drops/kalshi_oos_pending/`; public settled-market snapshots are written outside the repo under `/home/mrwatson/manual_drops/kalshi_oos_settlements/`; settled label packets, when available, go under `/home/mrwatson/manual_drops/kalshi_oos_labels/`. Current status is `labeled_observation_builder_pending_observations_waiting_settlement`: 44 model-backed pending observations, 1,000 public settled markets checked, 0 settled label rows. The signal-factory status is now `signal_factory_oos_pending_observations_waiting_settlement`.
- 2026-07-02 probability breadth scout: `make kalshi-probability-breadth-scout` now filters the broad finance/crypto bucket to configured crypto assets before routing to the crypto proxy lane. The latest run selected `crypto_proxy_fast_label_route`: 1,538 candidates close within 6 hours, including 1,239 actual crypto candidates and 194 weather candidates. A buglet was found and fixed: 17 AAA U.S. gas-price rows (`KXAAAGASD`) had been counted as crypto-like because they live in the broad finance/crypto classification. They are now excluded from crypto proxy work orders. Coinbase BTC/ETH/SOL and Kraken BTC public proxy probes all succeeded, with raw public payloads outside the repo under `/home/mrwatson/manual_drops/kalshi_probability_sources/`. Guardrail: Kalshi crypto settles from CF Benchmarks RTI; Coinbase/Kraken are proxy model features only, not official settlement labels or EV evidence.
- 2026-07-02 crypto proxy feature packet: `make kalshi-crypto-proxy-feature-watch-once` now refreshes the public Kalshi universe, refreshes probability breadth, and writes contract-keyed crypto proxy feature packets. Latest status is `crypto_proxy_feature_packet_ready`: 1,239 feature rows, 1,239 feature-ready rows, 9 proxy-covered assets, 0 partial rows, and all 6 gates pass. Raw public proxy payloads stayed outside the repo under `/home/mrwatson/manual_drops/kalshi_crypto_proxy_features/`; latest raw pointer is `/home/mrwatson/manual_drops/kalshi_crypto_proxy_features/crypto_proxy_feature_capture_latest.json`. The packet explicitly sets `usable=false`, `calibrated_probability=null`, and `ev_status=not_evaluated_proxy_feature_packet_only` on every row.
- 2026-07-02 crypto proxy observation loop: `make kalshi-crypto-proxy-observation-watch-once` now runs the full safe chain: public universe scan, probability breadth scout, crypto proxy feature packet, observation/label loop, feature-model falsification, research-candidate replay, signal-factory status, and macro route. Latest status is `crypto_proxy_observation_loop_label_rows_ready`: 6,195 total observation rows, 2,496 distinct contracts, 1,191 deduped label rows, 1,164 new label rows in the latest settlement probe, and 0 blocked/fail gates. Observation packets stay outside the repo under `/home/mrwatson/manual_drops/kalshi_crypto_proxy_observations/`; public settled/observed market snapshots stay outside the repo under `/home/mrwatson/manual_drops/kalshi_oos_settlements/`; labels stay outside the repo under `/home/mrwatson/manual_drops/kalshi_crypto_proxy_labels/`. The loop probes exact observed Kalshi tickers after expected expiration, writes only genuinely new observations/labels, and every row remains `usable=false`, `calibrated_probability=null`, and `expected_value_per_contract=null`.
- 2026-07-02 crypto proxy feature-model falsification: `make kalshi-crypto-proxy-feature-model-falsification` now reads true Kalshi crypto labels, collapses repeated observations by exact `contract_ticker`, and evaluates first-pass feature families under OOS/FDR rules. Latest status is `crypto_proxy_feature_model_falsification_ready_with_research_candidates`: 2,355 raw label rows, 309 independent settled contract labels, 2,046 duplicate label rows, 1 research candidate, 92 OOS scored rows, 54 OOS correct, raw OOS accuracy `0.5869565217`, and BH q-value `0.0586850794`. This is a research candidate only; it emits no calibrated probability, EV, sizing, or execution instruction.
- 2026-07-02 crypto proxy research-candidate replay: `make kalshi-crypto-proxy-research-candidate-replay` now replays the surviving proxy-state rule against historical all-in Kalshi costs. It uses the Wilson lower-bound selected-side probability (`0.5012128514`, not the raw `0.5869565217`) and the execution-cost normalizer (`YES` cost from `yes_ask`, `NO` cost from `1 - yes_bid`, plus official fee estimate). Latest status is `crypto_proxy_research_candidate_replay_blocked_predeployment_gates`: 306 costed replay rows, 156 positive cost-adjusted rows, mean expected value per contract `0.0186471651`, historical paper result sum `19.3349`, 0 usable rows. The lane is blocked because capacity-depth rows are 0, the largest correlation cluster is `BTC|range|2026-07-02T02:00Z` with 140 rows, and decay evidence is still early. Next tranche is capacity-depth, correlation-cluster, and decay-survival gates before any paper overlay.
- 2026-07-02 crypto proxy capacity/correlation/decay gate: `make kalshi-crypto-proxy-observation-watch-once` now refreshes the full safe chain through public universe scan, feature packet, observation labels, model falsification, replay, CCD, correlation-cluster control, signal-factory status, and macro route. CCD candidate selection is now cluster-round-robin rather than time-first truncation, so the bounded orderbook probe samples independent asset/family/close-time clusters before taking multiple rows from one bucket. Latest CCD status is `crypto_proxy_capacity_correlation_decay_blocked_correlation_concentration`: 9 current candidates across 9 clusters/assets, 9 public orderbooks, 0 orderbook errors, 3 positive-depth rows, 41,177.87 positive-depth contracts, `16219.659089` positive-depth cost notional, capacity status `capacity_depth_positive`, and 0 usable rows. Raw public orderbook payloads stay outside the repo under `/home/mrwatson/manual_drops/kalshi_crypto_proxy_orderbooks/`.
- 2026-07-02 crypto proxy correlation-cluster control: `make kalshi-crypto-proxy-correlation-cluster-control` consumes the latest CCD report and writes `docs/codex/macro/latest-kalshi-crypto-proxy-correlation-cluster-control.{json,md,csv}` plus a latest output directory. Current status is `crypto_proxy_correlation_cluster_control_blocked_upstream_ccd`: 3 positive clusters (`BTC`, `ETH`, `XRP` fifteen-minute up/down buckets), 3 required clusters under the 35% max-share policy, `16219.659089` positive-depth cost, `1409.966306669` controlled-depth cost, largest controlled share `0.35`, and 0 usable rows. Cluster breadth/share now pass; the upstream blocker is decay survival, because latest replay decay status is `recent_bucket_below_random` with CCD decay status `decay_survival_blocked`.
- The command center now has a single federated Kalshi contract EV ledger: `make kalshi-ev-ledger` writes `docs/codex/macro/kalshi-contract-ev-ledger-latest/kalshi-contract-ev-ledger.json`, `.md`, and `.csv`, plus latest pointers under `docs/codex/macro/`. The ledger asks the contract-level question for every repo: executable contract price, known fee/slippage cost, calibrated contract probability, margin, gates, and usability. Reference probabilities are recorded separately.
- 2026-07-01 Kalshi universe scanner: `make kalshi-universe-scan` now treats public Kalshi markets as the front door before any model repo gets involved. It writes raw public snapshots outside the repo under `/home/mrwatson/manual_drops/kalshi_universe/` and derived artifacts under `docs/codex/macro/kalshi-universe-scan-latest/` plus latest pointers. Current status is `universe_scan_ready_with_model_routes`: 6,764 candidates in the default 72-hour window, 1,420 model-route candidates for `mlb-platform`, and 5,344 soft-watch candidates routed to `soft_market_research_backlog`. Classification counts are `finance_crypto=3790`, `mlb=1420`, `unknown_soft_watch=678`, `weather=519`, `entertainment=200`, `macro_econ=95`, `other_sports=49`, and `politics_policy=13`. This is inventory only; the EV ledger remains the only place that compares calibrated probability to all-in break-even.
- Latest Kalshi EV ledger status is `kalshi_ev_ledger_ready_with_usable_contract_edges`: 5 repo feeds, 348 rows, 348 verified official Kalshi resolution-rule rows, 32 calibrated NFL overlay rows, 12 positive-margin usable rows, and 20 calibrated NFL rows rejected because they do not clear all-in cost. The first ranked queue row is `KXNFLGAME-26SEP13MIALV-MIA`: executable all-in break-even `0.3965`, calibrated probability `0.6462354523607845`, margin `0.24973545236078448`, and expected ROI `0.629849816798952`. This is not an execution instruction or tradable claim.
- 2026-07-01 north-star EV review queue: `make kalshi-ev-review-queue` now writes `docs/codex/macro/kalshi-ev-review-queue-latest/kalshi-ev-review-queue.{json,md,csv}` plus latest pointers. Current status is `kalshi_ev_review_queue_positive_candidates_need_robustness`: 12 queued positive rows, 5 positive-watch rows above the 2pp robust-margin threshold but still caveated, 7 thin-positive-watch rows, and 0 robust candidates. The caveat on current positive-watch rows is that cost uses the official fee estimate from executable price rather than an actual ticket all-in cost; next useful work is repeat snapshots, actual all-in ticket-cost confirmation, forward context, or independent validation.
- 2026-07-01 EV queue robustness: one bounded public Kalshi `KXNFLGAME` repeat market-data capture wrote raw JSON outside the repo at `/home/mrwatson/manual_drops/kalshi/kalshi_mlb_game_series_20260701T221233Z.json` and refreshed `/home/mrwatson/manual_drops/kalshi/kalshi_nfl_game_series_latest.json`. `make kalshi-ev-queue-robustness` now writes `docs/codex/macro/kalshi-ev-queue-robustness-latest/kalshi-ev-queue-robustness.{json,md,csv}` plus latest pointers. Current status is `kalshi_ev_queue_robustness_repeat_positive_cost_caveated`: 2 distinct snapshots, 12 queue rows, 12 repeat-positive rows, 0 missing quotes, and 0 robust candidates. Plain English: the positive NFL gaps persisted across two snapshots, but every row still depends on public YES ask plus official fee estimate, so the next blocker is actual all-in ticket-cost confirmation without submitting an order, plus forward context and independent validation.
- The ledger deliberately blocks all current rows because they use sportsbook/reference probabilities rather than calibrated contract probabilities or fail timing/mapping/review gates. The EV hurdle is the best captured all-in execution cost basis: explicit all-in cost first, fee-inclusive payout multiplier second, gross ticket/order payout multiplier plus explicit or official fee estimate third, executable price plus explicit or official fee estimate as fallback. Reference probabilities do not create margin or EV. This is now the canonical guard against confusing a "positive-looking row" with a usable edge.
- 2026-07-01 execution-cost normalizer: `predmarket.kalshi_execution_cost` now centralizes Kalshi cost math. It uses Kalshi's orderbook reciprocal ask logic plus the official June 29, 2026 fee schedule: general taker `0.07 * C * P * (1-P)`, maker `0.0175 * C * P * (1-P)`, and INX/NASDAQ100 taker `0.035 * C * P * (1-P)`, rounded up to the nearest centicent. If an order ticket says `1.34x`, the gross fill break-even is `1 / 1.34 = 0.7463`; the ledger then adds explicit or official estimated fees unless the cost source is explicitly fee-inclusive.
- 2026-07-01 row-contract hardening: every EV ledger row now emits `resolution_rule`, `resolution_rule_source`, `resolution_rule_status`, `resolution_rule_source_artifact`, and `resolution_rule_source_sha256`. The ledger reads local Kalshi JSON snapshots only, uses exact ticker matches, and upgrades rows to `verified_official_terms` when `rules_primary`/`rules_secondary` are present. Current Predmarket/MLB rows are 316/316 verified against `/home/mrwatson/manual_drops/kalshi/kalshi_mlb_game_series_latest.json`; no raw payload was copied into the repo. A usable row still requires clean timing/mapping, fee-aware all-in cost, and calibrated probability.
- 2026-07-01 EV blocker summary hardening: the ledger now emits `missing_calibrated_probability_row_count`, `blocked_row_reason_counts`, and ordered `top_blocked_row_reasons`. Current top blocker is `calibrated contract probability is missing` on 316/316 rows. This keeps the next tranche aimed at the real gap instead of re-solving contract identity, fee math, or resolution rules.
- 2026-07-01 EV readiness matrix: each ledger repo feed now emits `ev_readiness` with contract mapping, official terms, execution cost, calibrated probability, row gate status, exact next input, and next local command. Predmarket/MLB have exact contracts, verified official terms, and fee-aware costs but lack calibrated contract probabilities. NFL has calibrated model probabilities in its fair-line packet, but no exact Kalshi contract mapping/quote/official-terms mapping yet. ATP/NBA remain blocked by their previously named evidence/model-signal gaps plus missing Kalshi contract mapping.
- 2026-07-01 calibrated-probability overlay contract: `make kalshi-ev-ledger` now reads safe local JSON overlays from `/home/mrwatson/manual_drops/kalshi_ev_probabilities/`, keyed by exact `contract_ticker` + `side`, and records `calibration_status`, `calibrated_probability_source_artifact`, and SHA-256 on rows that use an overlay. The contract is documented at `docs/codex/manual-drops/kalshi-ev-calibrated-probability-contract.md`. Current overlay row count is 1, created by the NFL overlay assembler from validated NFL fair-line model output.
- 2026-07-01 contract-mapping overlay contract: `make kalshi-ev-ledger` now reads safe local JSON contract mappings from `/home/mrwatson/manual_drops/kalshi_ev_contract_mappings/`. This lets zero-row repos such as NFL emit exact Kalshi contract EV rows once a local mapping supplies ticker, side, official rules, timing status, and executable quote. The contract is documented at `docs/codex/manual-drops/kalshi-ev-contract-mapping-contract.md`. Current contract-mapping overlay row count is 1, mapping `KXNFLGAME-26SEP13ARILAC-ARI` to the NFL fair-line model row.
- 2026-07-01 overlay preflight: `make kalshi-ev-overlay-preflight` now validates contract-mapping and calibrated-probability overlays before the ledger uses them. It writes `docs/codex/macro/kalshi-ev-overlay-preflight-latest/kalshi-ev-overlay-preflight.json` plus latest pointers. Current status is `overlay_preflight_usable_ev_rows_present`: 1 mapping file, 1 probability file, 1 exact join, and 1 usable overlay EV row.
- 2026-07-01 calibration work order: `make kalshi-ev-calibration-work-order` now writes the exact current contract queue that needs validated calibrated probabilities plus a non-evidence overlay template. Latest status is `calibration_work_order_ready_source_gated`: 316 ledger rows, 312 non-temporal-mismatch candidates with verified official terms and all-in break-even costs, 25 selected rows, 0 direct pass-ready candidates, and 0 usable ledger rows. This means probabilities alone will not make those current predmarket/MLB rows usable because their source gates are still watch-only/below-threshold.
- 2026-07-01 NFL contract-mapping work order: `make kalshi-ev-contract-mapping-work-order` now bridges the more passable lane: NFL has calibrated Week 1 model probabilities but lacks exact Kalshi contract facts. Latest status is `contract_mapping_work_order_ready`: 16 NFL fair-line model rows, 32 selected contract sides, 2 validation artifacts, and templates at `docs/codex/macro/latest-kalshi-ev-contract-mapping-template.json` and `docs/codex/macro/latest-kalshi-ev-contract-mapped-probability-template.json`. These templates are `template_only=true` with TODO statuses, so they are not evidence until exact Kalshi ticker, official terms, clean timing, and executable cost are filled outside the repo.
- 2026-07-01 local contract-evidence scout: `make kalshi-ev-local-contract-evidence-scout` now scans local JSON only and writes `docs/codex/macro/kalshi-ev-local-contract-evidence-scout-latest/kalshi-ev-local-contract-evidence-scout.json` plus latest pointers. Latest status is `local_contract_evidence_ready_for_overlay_fill`: 11 local JSON files, 10,708 Kalshi-like contract rows, 132 NFL contract-evidence rows, 140 ready target matches, and 32 selected NFL work-order sides. The scout now derives `pregame_clean` only from known capture time plus a future expiration/close buffer, and it requires the contract ticker/YES side to match the selected team before declaring a ready target.
- 2026-07-01 NFL public market capture: one bounded public Kalshi market-data capture for `KXNFLGAME` wrote raw JSON outside the repo at `/home/mrwatson/manual_drops/kalshi/kalshi_nfl_game_series_latest.json` and the timestamped snapshot `/home/mrwatson/manual_drops/kalshi/kalshi_mlb_game_series_20260701T185756Z.json` (legacy filename prefix from the generic capture helper). It captured 66 open game markets, 0 series errors, no account/order paths, and no market execution.
- 2026-07-01 NFL overlay assembler: `make kalshi-ev-nfl-overlay-assembler` now consumes the local contract-evidence scout and NFL contract-mapping work order, then assembles safe contract-mapping/probability overlays outside the repo only when a target match has exact ticker, official terms, executable cost, and clean timing. Latest status is `nfl_overlay_assembler_overlays_written`: 1 ready target match consumed, 1 assembled overlay pair, 1 mapping overlay row, 1 probability overlay row, and overlays written under `/home/mrwatson/manual_drops/kalshi_ev_contract_mappings/` and `/home/mrwatson/manual_drops/kalshi_ev_probabilities/`.
- 2026-07-01 overlay idempotency hardening: NFL overlay filenames are now stable by assembled contract key, and `make kalshi-ev-ledger` deduplicates contract-mapping overlays by `source_repo_id + contract_ticker + side`. Re-running the assembler leaves 1 mapping overlay file, 1 probability overlay file, and 32 joined NFL overlay rows instead of double-counting duplicates.
- 2026-07-01 macro blocker audit: `make macro-blocker-audit` now proves whether the long-running EV goal is stopped by exact missing inputs rather than vague parked-state language. Latest status is `macro_blocker_audit_incomplete` because predmarket is no longer blocked: the EV ledger has 1 usable row and the NFL overlay assembler wrote overlays. The other lanes still retain their named evidence blockers.
- 2026-07-01 EV router/scout integration: `make macro-route` now surfaces `universe_scan_ready_with_model_routes` as the predmarket command-center status with priority 34, `all_lanes_parked=false`, 21 pass gates, 2 warn gates, 0 blocked gates, and the next tranche: inspect public-market inventory, route core NFL/MLB/NBA/ATP candidates to model-specific work orders, and route soft-watch candidates only after naming an external evidence source and probability method.
- MLB now has a no-provider pregame drop intake runner and a clean adjudicated same-slate pregame review bundle.
- A fresh Kalshi MLB market-data manual drop was captured at `2026-06-29T00:17:44Z`: 376 markets across KXMLBGAME/KXMLBTOTAL/KXMLBSPREAD/KXMLBF5/KXMLBF5TOTAL/KXMLBF5SPREAD, with 0 series errors. Snapshot lives outside the repo at `/home/mrwatson/manual_drops/kalshi/kalshi_mlb_game_series_20260629T001744Z.json`; latest pointer is `/home/mrwatson/manual_drops/kalshi/kalshi_mlb_game_series_latest.json`.
- The fresh Kalshi snapshot was checked against the existing June 20 sportsbook drop through MLB's no-provider intake runner. That local pair is invalid for Type 2: `blocked_invalid_operator_drop`, 0 candidate rows, because the sportsbook side is stale/not same-slate with the Kalshi capture.
- MLB now has a guarded current sportsbook capture command and a clean same-slate pregame review bundle. One explicit current The Odds API `baseball_mlb` market-data request at `2026-06-29T00:28:40Z` wrote raw+sidecar outside the repo. A pregame subset removed one already-started game and produced `docs/codex/artifacts/2026-06-29-current-pregame-subset-intake/` with `ready_pregame_pair`, 332 candidate rows, 58 review-ready candidates, 0 temporal suspicious rows, and 0 readiness failing gates.
- MLB review-packet adjudication is now complete for that clean bundle: `review-adjudication.json` reports `review_adjudication_ready`, 58 review-ready rows, 8 run-line clusters across 8 games, all away `+1.5`, all 9 gates pass, and all outputs remain review-only.
- MLB repeatability OS now has three clean same-slate packets. The third bounded current capture wrote one fresh Kalshi snapshot and one fresh sportsbook snapshot outside the repo, produced `docs/codex/artifacts/2026-06-29-third-current-intake/` with `ready_pregame_pair`, 435 candidate rows, 81 review-ready rows, 10 adjudicated run-line clusters, and 0 temporal suspicious rows. The latest ledger reports `repeatability_ready_for_research_review`: 3 clean packets, 28 cluster rows, 3 repeated descriptor shapes, and 2 descriptor shapes recurring across the three-packet threshold.
- MLB repeatability research review is now complete at `docs/codex/artifacts/type2-repeatability-research-review-latest/type2-repeatability-research-review.json`: status `repeatability_research_review_ready`, 2 stable recurring descriptor shapes, same-slate date count 1. The learned pattern is concentrated in away `+1.5` run-line descriptors; the blocker is cross-slate or settled/closing-line validation evidence, not more same-slate coding.
- MLB run-line contract audit superseded the two bullets above as evidence. The repeated away `+1.5` pattern was a contract-sign mismatch: Kalshi spread YES contracts represent selected-team `-1.5`, not selected-team `+1.5`. After the corrected mapping, all regenerated packets have 0 review-ready rows and the latest research review is `repeatability_research_review_blocked_threshold_not_met`.
- 2026-06-29 (late pregame refresh + clean no-signal salvage): one bounded late current sportsbook request and one bounded Kalshi capture were made outside the repos at `/home/mrwatson/manual_drops/odds_api/baseball_mlb_current_20260629T224907Z.json` and `/home/mrwatson/manual_drops/kalshi/kalshi_mlb_game_series_20260629T224907Z.json`. Predmarket rebuilt the mapped sportsbook reference to 52 rows; preflight was `reference_ready`; paper matching produced 52 watch-only candidates and 0 review candidates at the current threshold. Threshold sensitivity found 48 timing-clean candidates, 27 positive-net candidates, max positive review-only net divergence 0.0161, and 0 rows at threshold 0.1000.
- MLB salvaged the same late pair with a new clean-subset tool: 14 candidate games kept, 13 game ids removed (2 post-start, 11 unverified match). The clean subset intake at `/home/mrwatson/projects/mlb-platform/docs/codex/artifacts/2026-06-29-late-current-clean-subset-intake/` reports `ready_pregame_pair`, 264 candidate rows, 0 review-ready rows, 0 temporal suspicious rows, and 0 readiness failing gates. The latest MLB ledger is now `repeatability_no_signal_clean_packets`: 4 clean no-signal packets, 0 positive clean packets, 0 repeated descriptors.
- Predmarket now has a paper-only Type 2 matcher for explicit Kalshi-to-sportsbook references: `make type2-paper-matcher`.
- Predmarket now has a local Type 2 sportsbook reference intake preflight: `make type2-reference-preflight`.
- A derived local predmarket sportsbook reference now exists outside the repo at `/home/mrwatson/manual_drops/predmarket/type2-sportsbook-reference.json`.
- The derived predmarket sportsbook reference was rebuilt to remove duplicate ticker mappings; it now has 42/42 unique explicit `kalshi_ticker` mappings and a maximum event-match delta of 60 seconds against the local Kalshi occurrence-time convention.
- Predmarket now has a repeatable local Type 2 reference builder: `make type2-reference-build`.
- The latest reference-builder report is `reference_built_with_warnings`: 42 rows, 42 unique tickers, 0 duplicates, max event-match delta 60 seconds, and 1 skipped event with no matched Kalshi event.
- The latest predmarket Type 2 reference preflight is `reference_ready`: 42/42 valid explicit `kalshi_ticker` mappings, 0 blockers, 0 provider calls.
- The latest predmarket Type 2 paper matcher reports `review_candidates_present`: 42 review-only candidates, 2 pass and 40 watch.
- The latest predmarket Type 2 candidate disposition reports `candidate_disposition_all_passes_downgraded`: 0 timing-safe review candidates, 6 timing mismatches downgraded, and 0 manual timing unknowns.
- Predmarket's macro status now treats the absence of timing-safe review candidates as a real blocked gate and parks this lane until cleaner mapped reference evidence exists.
- 2026-06-29 (timing-safe reference unlock): after explicit user instruction to solve the blocker, made one bounded current The Odds API `baseball_mlb` request through MLB's guarded capture command and one bounded Kalshi MLB manual-drop capture. Raw files stayed outside repos at `/home/mrwatson/manual_drops/odds_api/baseball_mlb_current_20260629T033913Z.json` and `/home/mrwatson/manual_drops/kalshi/kalshi_mlb_game_series_20260629T033913Z.json`. The sportsbook request used 3 credits (`x-requests-used=48`, `x-requests-remaining=452`), did not print the key, and wrote only a sidecar plus raw file outside the repo.
- The new predmarket reference build from those same-timestamp captures produced 24 mapped moneyline rows, 24 unique tickers, 0 duplicate mappings, max event-match delta 60 seconds, and 1 skipped event. Preflight is `reference_ready` with 24/24 valid mappings and 0 blockers.
- The new paper matcher/disposition solved the prior timing blocker: `candidate_disposition_watch_only`, 24 watch-only candidates, 0 kept review candidates, 0 temporal downgrades, and 0 manual timing unknowns. The new blocker is not timing; it is that no candidate cleared the review threshold.
- 2026-06-29 (threshold sensitivity): added `predmarket/type2_threshold_sensitivity.py`, `tests/test_type2_threshold_sensitivity.py`, and `make type2-threshold-sensitivity`. Latest report at `docs/codex/artifacts/type2-threshold-sensitivity-latest/type2-threshold-sensitivity-latest.json` is `threshold_sensitivity_no_current_threshold_candidates`: 24 timing-clean candidates, 10 positive-net candidates, current threshold 0.1000, max positive review-only net divergence 0.0177, gap to current threshold 0.0823, and 0 current-threshold candidates. Hypothetically, 1 row appears at threshold 0.015, 2 rows at 0.010, and 6 rows at 0.005; the report does not lower thresholds or promote candidates.
- 2026-06-30 (MLB threshold-policy command-center refresh): MLB added a local threshold-policy review at `docs/codex/artifacts/type2-threshold-policy-review-latest/type2-threshold-policy-review.json`. It reports `threshold_policy_hold_current`: 4 clean packets, 1 slate date, 1,278 candidate rows, 0 rows at the current 0.1000 threshold, max absolute net difference 0.0277, and lower-threshold recurrence only on the same 2026-06-29 slate. The macro router now surfaces MLB as `primary_type2_threshold_policy_hold_current`.
- 2026-06-30 (MLB settled-validation command-center refresh): MLB added local settled-outcome validation at `docs/codex/artifacts/type2-settled-outcome-validation-latest/type2-settled-outcome-validation.json`, using a local final-score file outside the repo at `/home/mrwatson/manual_drops/mlb_results/mlb_scoreboard_2026-06-29_mlb_com.json`. It reports `settled_validation_no_policy_change_same_slate`: 1,239 settled directional rows, 45.0% directional correctness, 0 current-threshold rows, and only one slate date. The macro router now surfaces MLB as `primary_type2_settled_validation_no_policy_change_same_slate`.
- 2026-06-30 (MLB closing-proxy command-center refresh): MLB added local closing-line-style proxy validation at `docs/codex/artifacts/type2-closing-proxy-validation-latest/type2-closing-proxy-validation.json`. It reports `closing_proxy_same_slate_support_insufficient`: 819 paired later-snapshot rows, 0 current-threshold rows, 1 slate date, and favorable lower-threshold proxy movement (0.0200 threshold: 50 support, 0 against, 8 flat). The macro router now surfaces MLB as `primary_type2_closing_proxy_same_slate_support_insufficient` because this is useful but insufficient same-slate proxy evidence, not a true closing line.
- 2026-06-30 (MLB public BetExplorer closing-data command-center refresh): MLB found and imported a public BetExplorer moneyline source without provider/API calls or paid data. Raw public-web snapshots stayed outside repos under `/home/mrwatson/manual_drops/public_web/`. The full-slate import at `docs/codex/artifacts/type2-betexplorer-full-slate-public-import-latest/type2-betexplorer-full-slate-public-import.json` has 13 events and 78 moneyline rows. The comparison report at `docs/codex/artifacts/type2-betexplorer-moneyline-closing-comparison-latest/type2-betexplorer-moneyline-closing-comparison.json` is `betexplorer_moneyline_closing_comparison_ready_no_policy_change`: 22 date-matched direct BetMGM moneyline rows, 16 converged, 6 diverged, 17 direction-support, 5 direction-against, and 0 current-threshold rows. The macro router now surfaces MLB as `primary_type2_betexplorer_moneyline_closing_comparison_no_policy_change`.
- 2026-06-30 (MLB public BetExplorer multi-market command-center refresh): MLB expanded the public BetExplorer importer to fetch moneyline, totals, and run-line tabs without provider/API calls or paid data. Raw public-web snapshots stayed outside repos under `/home/mrwatson/manual_drops/public_web/`. The full-slate import at `docs/codex/artifacts/type2-betexplorer-full-slate-public-import-latest/type2-betexplorer-full-slate-public-import.json` now has 13 events, 953 public rows, 3 books, and market counts `moneyline=78`, `run_line=203`, `total=672`. The comparison report at `docs/codex/artifacts/type2-betexplorer-market-closing-comparison-latest/type2-betexplorer-market-closing-comparison.json` is `betexplorer_market_closing_comparison_ready_no_policy_change`: 24 date-matched direct BetMGM rows (`ml=22`, `run_line=2`, `total=0`), 16 converged, 8 diverged, 18 direction-support, 6 direction-against, and 0 current-threshold rows. The macro router now surfaces MLB as `primary_type2_betexplorer_market_closing_comparison_no_policy_change`.
- The first MLB Type 2 coding tranche landed: `mlb_platform.market.type2_edge`, `mlb_platform.cli.type2_evidence`, and `make type2-evidence TYPE2_CANDIDATES=/path/to/candidates.json`.
- The second MLB Type 2 coding tranche landed: `mlb_platform.backtest.type2_candidates`, `mlb_platform.cli.type2_candidates`, and `make type2-candidates ODDS_PATH=/path/to/odds EXCHANGE_JSON=/path/to/exchange.json`.
- The third MLB Type 2 coding tranche landed: `mlb_platform.reports.type2_audit`, `mlb_platform.cli.type2_audit`, and `make type2-audit ODDS_PATH=/path/to/odds EXCHANGE_JSON=/path/to/exchange.json`.
- Current predmarket follow-up: wait for a new timing-safe reference/slate that produces a review-threshold candidate, or a future evidence-backed threshold-policy change. The timing-safe reference problem itself is solved for the 2026-06-29T03:39Z pair, and the threshold study shows the clean pair is far below the current review threshold.
- 2026-07-01 (NFL forward-context availability command-center refresh): NFL added `make forward-context-availability`, producing `docs/codex/artifacts/nfl-forward-context-availability-latest/forward-context-availability.json`. It reports `forward_context_availability_not_yet_due`: earliest cached Week 1 game date `2026-09-09`, 16 games, 0 missing inputs due now, depth charts present, and weather/injuries/official starting QBs/closing reference not yet due. Next due input is weather on `2026-08-30`.
- The macro router now surfaces NFL as `line_readiness_profiled_slate_forward_context_not_yet_due_research_only` with priority `-5`; this prevents Codex from repeatedly trying to collect July injury/weather/official-QB inputs that cannot responsibly exist yet.
- Current macro follow-up: predmarket is active on the signal-factory lane. The immediate blocker is no longer registry creation, harness creation, pending-packet construction, identifying the fastest learning family, building contract-keyed crypto proxy features, wiring the observation loop, collecting enough independent labels, building the first model falsification gate, replaying the first research candidate against all-in costs, creating the capacity/depth/decay gate, creating the correlation-cluster control, or fixing artificial cluster starvation in the CCD selector. The routed next tranche is repeated settled-bucket accumulation and decay monitoring; stop before lowering decay/sample thresholds, paper overlay, sizing, execution, or account/order paths.
- Macro parked-state routing is now explicit: if every repo priority is `<= 0`, `make macro-route` recommends predmarket as command center and writes a blocker summary instead of cycling through parked repos.
- Macro unlock scouting is now available via `make macro-unlock-scout`; it writes `docs/codex/macro/latest-unlock-scout.json` and `.md`.
- Latest predmarket macro status reports `signal_factory_crypto_proxy_capacity_depth_blocked` with priority 34 (decay evidence is now fully auditable: 3/4 buckets pass, cumulative accuracy 0.6, but recent bucket still below random so decay gate remains blocked; with 0 current candidates after universe expiry, capacity-depth is the surfaced blocker). Latest MLB macro status reports `primary_type2_betexplorer_market_closing_comparison_no_policy_change` with priority -4. Latest NFL macro status reports `line_readiness_profiled_slate_forward_context_not_yet_due_research_only` with priority -5. Latest macro route recommends predmarket as the active Kalshi signal-factory command center and the next tranche is orderbook depth accumulation for current crypto proxy candidates while keeping the crypto proxy signal blocked until capacity, correlation, and decay gates all pass.

## Durable Guardrails

- Keep Kalshi work research-only unless the user explicitly asks for execution.
- Do not touch live execution or approval queues while improving paper research loops.
- Prefer replayable artifacts over live-only state.
- Keep promotion-readiness gates hard and non-overridable unless a new tranche explicitly changes the contract.
- Prefer future tranche notes under `docs/codex/tranches/`.
- At the start of a future macro session, run `make macro-route` here or `make macro-status` in the target repo before choosing work.
- One Codex session touches one repo unless the task is strictly status aggregation.
- Phase 1 remains paper/research only: no live money, market execution, provider calls, paid historical calls, or DB writes by default.

## Verification Starting Points

- `make check-env`
- `make kalshi-verify`
- `make kalshi-manual-drop-capture`
- `make kalshi-universe-scan`
- `make kalshi-universe-watch-once`
- `make kalshi-signal-factory-status`
- `make kalshi-hypothesis-registry`
- `make kalshi-labeled-observation-builder`
- `make kalshi-labeled-observation-watch-once`
- `make kalshi-labeled-oos-backtest`
- `make kalshi-probability-breadth-scout`
- `make kalshi-crypto-proxy-feature-packet`
- `make kalshi-crypto-proxy-feature-watch-once`
- `make kalshi-crypto-proxy-observation-loop`
- `make kalshi-crypto-proxy-observation-watch-once`
- `make kalshi-crypto-proxy-feature-model-falsification`
- `make kalshi-crypto-proxy-research-candidate-replay`
- `make kalshi-crypto-proxy-capacity-correlation-decay`
- `make kalshi-atp-proxy-observation-loop`
- `make kalshi-atp-proxy-evidence-gate`
- `make kalshi-atp-proxy-observation-watch-once`
- `make type2-reference-build`
- `make type2-reference-preflight`
- `make type2-paper-matcher`
- `make macro-status`
- `make macro-route`
- `make macro-unlock-scout`
- `make kalshi-ev-ledger`
- `make kalshi-source-repo-inventory`
- `make kalshi-external-artifact-wrap`
- `make kalshi-external-artifact-preflight`
- `make kalshi-signal-formula-registry`
- `make kalshi-paper-decision-candidates`
- `make kalshi-signal-decay-retirement`
- `make kalshi-ev-overlay-preflight`
- `make kalshi-ev-calibration-work-order`
- `make kalshi-ev-contract-mapping-work-order`
- `make kalshi-ev-queue-robustness`
- focused pytest for touched `predmarket/kalshi_*`, `predmarket/type2_*`, `tests/test_type2_reference_intake.py`, and `tests/test_type2_paper_matcher.py`
- full pytest when shared store or CLI behavior changes

## Next Resume Protocol

1. Read this file, `AGENTS.md`, `docs/codex/macro/latest-decision.md`, `docs/codex/macro/latest-kalshi-contract-ev-ledger.md`, and the latest file under `docs/codex/tranches/`.
2. Preserve the research-only boundary.
3. Run `make macro-route` before selecting a repo unless the user overrides the route.
4. Use `/goal` for longer autonomous improvement runs.
5. Update this file, refresh `docs/codex/macro/latest-*`, and add a compact tranche note when done.

## Dispatch Template

```text
Use /home/mrwatson/projects/predmarket-alpha as the macro command center. Review docs/codex/current-state.md and docs/codex/macro/latest-decision.md, preserve research-only boundaries, execute the routed narrow tranche, and update the state files plus completion report.
```
