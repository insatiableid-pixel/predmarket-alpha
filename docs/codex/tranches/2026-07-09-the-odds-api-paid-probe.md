# The Odds API Paid Historical Probe

Date: 2026-07-09

## Outcome

- Installed the paid key outside the repo at
  `/home/mrwatson/.secrets/the-odds-api/key.txt` with mode `0600`.
- Added key-file references to the canonical ignored `.env`; no secret value was
  written to tracked files or printed.
- Verified paid historical access with `baseball_mlb`, `h2h`, and region `us`.
- Corrected the probe to query at `target_time + 150s` and to gate readiness on
  measured target-to-snapshot skew, not only the theoretical five-minute cadence.
- Corrected probe result: target `12:00:00Z`, provider snapshot `12:00:37Z`,
  absolute target skew `37s`, gate ceiling `180s`.
- Provider quota after two narrow probes: `20` used, `19,980` remaining. Each
  probe cost the documented `10` credits for one region and one market.

## Status

Feasibility now reports
`kalshi_sports_historical_consensus_feasibility_ready_for_backfill`. The bulk
historical archive has not been purchased or generated by this tranche, and no
backfill, account, order, paper sizing, or live execution path was run.

## Verification

- Focused tests: `7 passed`.
- Ruff on the touched script and tests: pass.
- Corrected paid probe: pass, with `api_key_printed=false`.
- Claude advice audit: exits cleanly with open clock/statistical items.
