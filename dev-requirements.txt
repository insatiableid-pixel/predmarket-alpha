# Development tooling for predmarket-alpha.
# Install with:  pip install -r dev-requirements.txt
# (the runtime venv is created by `make setup`).

# Linting + formatting (single tool, configured in pyproject.toml [tool.ruff])
ruff==0.15.17

# Static type checking ([tool.mypy] strict)
mypy==2.1.0
types-PyYAML
types-requests

# Dead-code detection ([tool.vulture])
vulture==2.16

# Architectural boundary enforcement ([tool.importlinter])
import-linter==2.12

# Unused dependency detection
deptry==0.23.0

# Flaky test detection
pytest-rerunfailures==16.4

# Duplicate-code detection is provided by jscpd (Node). Run via `make dup-check`
# which uses `npx --yes jscpd`; no Python install required. See .jscpd.json.
